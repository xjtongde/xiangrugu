# AGENTS.md —— 香如故项目 · AI 助手入口约束

本文件提供给 DSH、Codex 及其他 AI 编程助手。进入本项目后必须优先阅读本文件。

> **定位**：本文件只做两件事——声明**项目目标**与**必读链**。一切做事方法纪律（口令/挂账/三本专职账/单一源/变动日志/每轮对齐核查）的权威展开在 `rules.md`，本文件一律留指针不复述。一切文档的地图与权威顺序在 `docs/index.md`。

## 1. 项目目标

**待用户定义。**（骨架开设于 2026-09-22，按用户指令先立文档纪律、项目本体后议。）

- 目标未定义前：**不进行任何编码、选型、搭环境等开发动作**（联动 R-01 动手前须口令）。
- 目标定型时：由用户给出项目定位与核心思想，写入本节，并同步确立开发标尺（R-03 归属判定的尺子）。
- 本节是项目目标与核心思想的**唯一记载处**（R-04 单一源）；他文引用一律指针。

## 2. 环境与代码地图

运行环境、代码地图、部署位置一律见 `docs/codemap.md`（唯一展开处，R-04）。当前已定事实：

- 项目唯一权威工作区＝本目录 `/root/xiangrugu/`（文档＋源码＋测试＋Git 仓库）。
- 远程代码源仓库、构建主机、生产部署方式＝**待用户裁决**，见 codemap §2。

## 3. 必读链（新会话）

```text
AGENTS.md（本文件）
↓
rules.md（做事方法纪律总纲，逐条 R-xx）
↓
docs/index.md（全部文档索引＋权威优先级＋阅读顺序）
↓
docs/codemap.md（代码地图与部署位置）
↓
三本专职账按需：docs/bugs.md / improvements.md / features.md
```

任何文档之间出现表述冲突：**停止猜测，明确指出冲突并报告用户**，按 `docs/index.md` 权威优先级裁定。
