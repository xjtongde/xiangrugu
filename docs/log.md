# log.md —— 文档变动日志（R-05）

> **定位（2026-09-22 用户指令"加一个 log.md 文档用来跟踪文档变动情况"）**：跟踪文档本身的**结构性变动**——开设、迁移、退役、成批修订、账本改道、每轮对齐核查结论。日常逐条记账（某账添一条 B/I/F）**不回填本文件**，那是各账自身的格式。
> 格式一行一事，**新条目加最上面**；旧行不回改，勘误以补正注前向指路（R-04）。
> 行格式：`日期 | 文件 | 变动性质 | 依据 | commit 指针`。

---

## 变动记录

- **2026-09-25 | `docs/cbdb.md` §一 许可行 ＋ `docs/features.md` F-11 ＋ 远程 wiki 仓《古籍开源项目抄》§七 | 许可链勘正批（同批 R-06 ② 触发）** | 依据：古籍开源项目调研中一手实测出的**四条许可事实**，属项目实用信息，按 R-04 须由主仓承载（外围层留正文，主仓留口径）。**① 勘正旧口径**：§一 原写"HF 快车道件为 **CC BY-NC-SA 4.0**"，本轮实测**分层不同**——HF `cbdb/cbdb-sqlite` 是 `license: other`＋`cbdb-data-licensing-terms`，**条文 URL 实测 403、Wayback 零快照**（⇒ 不得写作 CC0／CC BY，亦不得再发布）；`cbdb/*` 七模型与 `chgis-map` 才是 CC BY-NC-SA 4.0；Dataverse 逐集自定义（`BWIBNL` 原值 CC BY-NC-SA 4.0）。**② 新事实**：GitHub `cbdb-project` 内 `sentence-segmentation-…`(★32) 与 `named-entities-…`(NECHR, ★26) **仓内 LICENSE 为 CC BY-NC-SA 4.0 全文 20,849B，而 GitHub 标签显 NOASSERTION**；反例 ★225 的 `cbdb_sqlite` 仓**确无 LICENSE**（双 404，README 仅 1,801B 指向 HF）⇒ **整条 SQLite 发布链无一处可读到条款正文**，此点直接约束我们的再发布边界。**③ 判许可的通则**：只读仓内文件，不读标签——同一标签值 `NOASSERTION` 在两仓分别意味着"有完整条款但 GitHub 认不出"与"真的没有"。**④ F-11 补前置事实**（状态仍挂账未动）：本地 1.86GiB 两件＝Dataverse `BWIBNL`（NC 条款），其**上游代码 `zhouliupku/LGTaggingApp` LICENSE 全文＝MIT**⇒ **代码层与模型·数据层许可不同，讨论"怎么用"前须先说清指哪一层**；并记马普所 LoGaRT 本体在 GitHub `q=LoGaRT` 八命中中无一命中，不得据 GitHub 判其在否。外围层同批改号（原 §七起顺延），红线由十四条增至十六条并自检页内 §引用无悬空。| wiki 仓 commit `6e6e444`／本 commit

- **2026-09-25 | 远程 wiki 仓（非本工作区文件，按数据落盘纪律立行指路）| 外围层加页批**（依据：用户"你再找一找，还有什么中国古典书籍有那些开源的项目"） | 新增《**古籍开源项目抄**》（约 3.1 万字节）；`Home` 词条目录增"三、外部生态"节、`_Sidebar` 同步；`好主意讨论区` 立 **D-06**（用 ctext Data Wiki 反照 CBDB 人↔书边表 52.5% 角色"未詳"）与 **D-07**（以 Unihan `kFanqie`＋切韵系 CC0 数据做异读规范化），两条均注"不产生主仓承诺、须另行口令"。页内另立**三问判据**（代码／数据可再分发／通道是否在位）与**取证方法红线十四条**。正文一律 git 通路落地（B-03／B-04 未解）。| wiki 仓 commit `0ab17aa`（`git ls-remote origin main` 已核；`GET /wiki/pages` 实测 **9 件在册**）

- **2026-09-25 | `docs/features.md` F-15 ＋ `docs/codemap.md` §2 外围层行 | 去冗余批（R-06 ① 触发）** | 原状：主仓**两处各自枚举** wiki 页面清单，且都停在"六页在册"，与同日已加的 9 件脱节——同一事实两处展开＋一处过期，正为 R-04 单一源所禁。**改法**：两处一律撤枚举，只留指针（清单唯一展开处＝wiki `Home` 词条目录＋`_Sidebar`）＋带时点的规模数（2026-09-25 实测 9 件，含站级三件）＋远端 HEAD 指针；F-15 补一句"本层加页加行，主仓不随之改动"。**效果**：外围层此后增长不需回改主仓，过期面归零。| 本 commit

- **2026-09-25 | 同上 | 对齐核查（古籍开源项目批收口）** | R-06 四项。**① 冗余**：查出并消掉上述"页面清单双处枚举"（本批唯一冗余）；主仓／外围层分工经再核仍成立——古籍项目的许可条款与通道实测属外围知识，而《古》页 §三 的 CBDB 数字系**本地件一手统计的新事实**（主仓此前未载），不属复述。**② 矛盾（本批主要收获：一处判案＋一处认错）**：(a) **认错**——我方上轮把 `TEXT_CODES` 称作"四库全书存目表"，本轮官方检索实测 `q=存目` → **total=0**（而 `q=四庫全書`→14、`q=宋史`→17、`q=墓誌`→30），据实改判为"CBDB 全站文献／出处代码表"，页内明记"这是一次改错"；(b) **判案**——Ext B 收字两说互斥（42,720 vs 42,711），我方整包下载 Unihan 逐块实测得"块定义 42,720／实收 42,720"，判明**两数皆真、口径不同**（块域大小 vs 较早版次已收数），并把"块定义 ≠ 字数"立为红线一条；(c) 上游转述里的**伪字段名与伪前缀**（`kIRGKangXi`、`kIRGDaeJaweon`、`Mandarin_HANS-CN`、`Cantonese_HK`、`GVT7`）实测一律 0 命中，全部拒入并登记为 ❌ 黑名单；(d) 主仓侧无新增矛盾。**③ 锚点**：《古》页指向的主仓节（`rules.md` R-04／R-01、`docs/cbdb.md` §九 0 值哨兵专节、`docs/codemap.md` §2）均实存；`Home`／`_Sidebar`／`好主意讨论区`／新页四处 `[[]]` 互指无空挂；主仓 F-15↔codemap↔rules↔index 四指针成环且**不再指任何具体页名**。**④ 账目闭环**：F-15 依"加页不另立项"未改状态（仅去冗余）；D-06／D-07 明写"未动手、须口令"，**未越权**；B-03／B-04 绕行仍有效（本批正文仍全走 git、回读靠 git 工作副本）；`/tmp` 克隆与全部外部下载件（含 8,340,649 字节 Unihan.zip、ctext TTL 切片）批末清除，工作区零残留。| 结论：**本批一致，可收口**。遗留四项——① **须口令**：D-06／D-07 任何一条若要动手、D-02 报上游 issue、升 Gitea 复测；② ⚠ **未复验**：维基数据 P497 总数 418,005、P4711 5,672（distinct 5,643）系子代理经 WDQS 所取，我方仅复核 rank／正则／实值与 formatter 在位性；③ ⚠ **许可真空**：`cbdb/cbdb-sqlite` 条文 URL 实测 403 且 Wayback 零快照、TGAZ 全站未见许可声明——**两者一律不得写作"可再发布"**；④ ⚠ 网页端渲染仍未经真人会话核验（承前批）。

- **2026-09-25 | 补正注（前向指路，不改上批各）**：上批 log 行所记 wiki 仓 commit `322561d` 之后，同批内容又经两次自查修正——① 规模与治理名单的一手核证订正（四处，见 R-06 行）；② **外围层自律删错一条**：CHGIS 页"示例 ID"处曾有一句未经核证的表述，已替换为本轮直取的实值（`hvd_32180` ＝ **婺州**，`feature_type` 州／prefecture，`temporal` 758~1275，`spatial` `POINT (119.64992, 29.10471)`，今地今浙江金华市），wiki 仓现 HEAD **`7a533e`**（`git ls-remote origin main` 已核）。**教训同记入该页自律语**：写不出处的句子一律删——外围层虽非权威，亦不许含未证之语。主仓无涉（该页数字皆指向主仓，未新增事实）。| wiki 仓 commit `7a533e`

- **2026-09-25 | `rules.md`（R-04 单一源清单增"外围知识层（Gitea wiki）"行＋新立"外围知识层的边界"段）＋ `docs/index.md`（新立"外围知识层（Gitea wiki，**非权威**）"节）＋ `docs/codemap.md` §2（新增"外围知识层＝Gitea Wiki 仓"行，含通路逐项实测）＋ `docs/bugs.md`（新立 B-03）＋ `docs/features.md`（新立 F-15）| 外围知识层立法与登记批** | 依据：用户定性 *"既然是百科，当然不会是项目离开它它就不能工作……总之是项目的非必要材料。用学习与了解。"* ＋口令 *"初始化一个吧。就把哈佛关于这个项目的来龙去脉收集一下记进去……有了好主意也可以放进去大家讨论。"*。wiki 仓本不存在（实测 `/wiki` 与 `xiangrugu.wiki.git` 皆不可用），当日以 `POST /wiki/new` 生成，分支 `main`。**边界立法口径**：非权威、不入必读链、不参与权威优先级；主仓已载事实只许"摘要＋指针"；正文不留本地副本；想法须回主仓挂账方可执行。| commit `5ff1fc4`

- **2026-09-25 | 远程 wiki 仓 `deepseekharness/xiangrugu.wiki.git`（非本工作区文件，按数据落盘纪律在此立行指路）| 外围层内容批** | 在册 8 页：`Home`（定位声明＋词条目录＋四条约定：数字必带时点／句句可溯源／三档 ✅⚠❌／想法不越权）＋`_Sidebar`＋`_Footer`＋`数字人文名词抄`＋`好主意讨论区`（D-01…D-05）＋`CBDB-来龙去脉`＋`CBDB-谁在使用`＋`CHGIS-姊妹项目速览`。**正文一律 `git push` 落地**（API 写正文失效＝B-03；API 单页读对 CJK 页名 404＝B-04）。内容全部来自 2026-09-25 外部调研批，逐条带出处。| wiki 仓 commit `322561d`（`git ls-remote origin main` 已核＝本地 HEAD）

- **2026-09-25 | `docs/cbdb.md`（§四 增"官方对外口径沿革时点链"＋两条引用陷阱；§五 增 API 双轨与官方 MCP 桥；新增 9.4(7a) `CHGIS_PT_ID` 桥的真实强度）＋ `docs/codemap.md` §2（登记 wiki 单页读限制）＋ `docs/bugs.md`（新立 B-04，并按"新条目在最上"归位 B-04/B-03 顺序）| 外部调研批一手核证入账** | 入账纪律：**凡进主仓的数字与结论一律本轮自取原文／快照直证**，上游报告未能自证者（如 H-ASIA 94,000）**拒入主仓、仅在外围层标 ⚠**。新事实三条：① 官方从未定义 `CHGIS_PT_ID` 语义（schema 无注释、User's Guide 零提及），故"对应 CHGIS 某版本"是使用者的假定；② 该桥实际经由 TGAZ（`hvd_`+pt_id），本地覆盖率仅 **10,996/30,157＝36.5%**，且一对多与"同名同起止年双胞胎记录"为固有性质（TGAZ 实测三例）；③ 官方 535,181→641,568 的 10.6 万跳升经快照上下夹逼，**落在 2024-06～09 之间**。| commit `6ace997`

- **2026-09-25 | 同上 | 对齐核查（外围知识层批收口）** | 按 R-06 四项交叉核查。**① 冗余**：主仓与外围层分工经实地核对再厘清——数字与判断留主仓，沿革／人名／思想脉络留外围层；外围层 CHGIS 页涉 CBDB 字段处属"跨库关系"的新事实而非主仓既有事实的复述，判定合格，无回改。**② 矛盾（本轮主要收获，四处全在我自己写的外围层页内）**：(a) 执委会／指导委员会名单被上游报告带偏——罗新误入"历任"、邓小南误入执委，据官方 2026-02-23 快照原文全部改回；(b) 两线 PM 的机构归属写错（柳舟实为北大、向帆实为清华，王通／刘邦栋的 PM 任次在该快照未见→降级 ⚠ 待核）；(c) CGED-Q 被我写成连续 1760–1912，官方《项目发展史》原文系 **1760-1798／1850-1864／1900-1912 三段断代**，已照改；(d) 快照时点标注不实（core-institutions 页实为 2026-02-23 而非 2025-09-24），已订正。**教训入页内"订正记录"**：名单类内容不可凭二手报告改写，必须回到原文逐项点验。主仓侧无新矛盾。**③ 锚点**：外围层 `[[]]`（Home／CBDB-来龙去脉／CBDB-谁在使用／CHGIS-姊妹项目速览／好主意讨论区）与 `_Sidebar` 页名互指无空挂；外围层指向的主仓节号（`docs/cbdb.md` §四／§五／§六／§九）与 `rules.md` R-04 均实存；主仓新增 9.4(7a) 与 §五／§四 交叉引用、B-04↔B-03↔codemap 行三处闭环。**④ 账目闭环**：F-15 已启用且有边界立法；B-03／B-04 各附即时成例的绕行方案（正文走 git、回读走 git 工作副本）；D-02（向 Gitea 上游报 issue）与"升 Gitea 复测"均属 35 主机服务变更，**继续挂账待口令**，未越权动手；`/tmp` 下 wiki 克隆与全部外部下载件本轮末尾清除，工作区零残留（合数据落盘纪律）。| 结论：**本轮文档与工作区／远程实况一致，可收口**。遗留两项——① 须口令：D-02 报 issue、Gitea 升级复测；② ⚠ 未验：外围层页面的**网页端渲染**（`[[...]]` 链接与 CJK 页名显示）尚未由真人会话确认，仅 git 与 API 列页两面已证。

- **2026-09-25 | `docs/cbdb-load.md`（§14 整体重写为 v2 全量装载案＋状态行更新）| 成案修订批（用户"能装尽装"令；**v1〔f452374/d91997b〕作废**）** | 依据：用户令照录入 §14.0——*"人家给我们的，肯定是有目的。我们自己不要在这里胡想八想的。除了象ZZZ这样重复的东西，我们肯定不装……能装的肯定要装上啊。只有装上了我们才知道用这些东西干什么啊？所以要搞清楚我们有什么？"*——**v1 之病如实认**：按"用处大小"挑拣（Hartwell 拆研读缓装/DEM 条件跳/v5_time 跳装/范围自限四挂账项）＝用户所诫"胡想八想"。**三铁律立案**：能装尽装（不预判用处）／唯实证重复不装（跳装必附 sha/内容级证据，四类：派生可重建・编码变体・格式重打包・时序旧快照）／先搞清楚有什么（**65 树全量点验总账入 §14.1**：逐树件数/字节/扩展名/标题，八大类各定处置）。**本轮实勘三翻案**：① HHVVHX/ST5KKM/T27RQO 内系 **v6_ 前缀现行代** 1820/1911/CITAS 层（M7WEFY 之 v5_ 同名层系旧代——两代并装，非重复）；② M7WEFY `-1/-2` 变体 **sha 全不相同**＝内容有别之重发布（跳装无据→内容级对验后定）；③ **postgis_raster 3.6.4 available 实测**（一条 CREATE EXTENSION 即通，DEM 无障碍）；**Hartwell 解析定案**：v5_2010＝352 shp 层/6 年份(741–1900)/8 层型（cnty78 pref77 jin58 circ51 indp37 liao19 prov17 chin15）→**归并 8 表**（+yr+src_file 列，append 归并，对账＝层型和==表数）；v1_2002＝353 层+408 MapInfo＋Reprojection PDF→对验规则定装否（纯重投影派生→不装留证）。**新货入账**：明驿路站网/茶马 3/德川 4/TGAZ/藏区 3/谭图索引/ArcChina/GNS 码/明卫所/BGIS/DCW/气管道/高铁/北京古迹 1875/中瑞考察/新中国全图/1999 县级人口/国标 GBT-2260-91/V2 V3 V4 历代矢量 1.19GB/V3 MDB（驱动探测）。**货单腿 D–L＋验收八项＋两夜窗（2a≤2h/2b≤2.5h）＋差异项十条**（版本堆存档不装/v1 Hartwell 对验/纯扫描存档/schema 三分 chgis-harv-public/编码变体只装一份/KMZ-MapInfo 核证不装/hillshade 派生不装/-1-2 对验/直读镜像盘/MDB 缺驱动记录缓装）。**工作量答**：机器 1–3 小时、全程两晚；无未知深水区。R-06 四项：冗余查（65 树总账唯一展开＝§14.1，datasets.tsv 系 NAS 货单不回录）；矛盾查（v1 作废声明在 §14 头、前批"范围边界声明"随 v1 整体替换无残留；"已装首批⑦件"与 §13 实账核同）；锚点查（DOI 尾号/件数/字节/sha 前 12 位/352 层型分布与两轮实勘输出逐字核同）；账目闭环（cbdb-load✓ 本账✓；features/codemap/cbdb.md 联动列 §14.8 执行批兑现）。**候"同意"开工（可逐条改裁差异项）。** | commit `dbd3e22`

- **2026-09-25 | `docs/cbdb-load.md`（§14.1 范围边界声明补记）| 澄清入账批（答用户问"现在是不是除了ZZZ其余都装？我感觉你每次查一查都有新东西出来"）** | 依据：用户问句；吸取"解释不只留对话"教训随批入账。**答**：非"全装"——装＝29 表（④V5 矢量 28：1820 族 9＋1911 族 7＋省级/专题 11＋gns 30 省并 1；②addr_xy 19,249 行；纪年表 china_chron）；探测后定＝DEM（postgis_raster 有无）；不装四类＝③ZZZ（定案）/①Hartwell（拆研读子案：约 400 层逐年切片＋MapInfo 混装，硬装＝数百张表）/v5_time_*（V6 等价件已在）/GBK 重复・合并包・-1-2 重复产物。**"每次都有新东西"之因**：harvard-full 65 集/370 件初盘只到六分法粗粒度，本批实勘钻至 M7WEFY 单文件级故细颗粒事实层出（纪年表/DEM 栅格/三专题层/Hartwell 混装定性）；**定心丸＝货单封盘**（101 zip 全数点名，执行按单装不再冒新货，余仅层内编码/几何细节、验收兜住）。**边界声明入 §14.1**：本批＝CBDB/CHGIS 家族；harvard-full 其余独立收藏（扫描栅格 2.72G/TGAZ/日本 HGIS/俄藏地图/LoGaRT 1.86G/CBDB 版本堆——codemap"净新增≈3.5G"判词在案）**不在范围，装否另案另令**。R-06 四项：冗余查（边界声明唯一处＝§14.1，本行摘要）；矛盾查（29 表数与 14.2 货单逐项核同：9+7+11+1+1）；锚点查（六分法/净新增判词回指 codemap 在册行）；账目闭环（cbdb-load✓ 本账✓，他账不动）。**§14 全案仍候"同意"开工。** | commit `d91997b`

- **2026-09-25 | `docs/cbdb-load.md`（§14 第二批成案新立＋状态行更新）| 成案批（用户令"把余下的装上去。这个工作量大吗？"；同令前半"它自己收口。你别动"＝wiki 会话 6 处未提交改动零触碰照办）** | 依据：用户令＋家规成案程序（先案后"同意"后开工）。**装前实勘（32 镜像盘只读）**：M7WEFY V5 树全量 **101 zip**（UTF 可用约 24 层；v5_time_* 系 V6 前身→跳装；GBK 变体/合并包/-1-2 重复产物不另装）；**v5_dem＝GeoTIFF 栅格**（80.7MB，需 postgis_raster，探测后定）；**gns 30 省仅 GBK 合计 10MB**（转码可装）；**Hartwell 29302 定性**＝v1 2,866 条目 shapefile＋MapInfo 混装、v5_2010 2,467 条目**逐年切片约 400 层**（硬装＝数百张表→**拆独立研读子案**，本批只研不装）；**ADDR_XY＝19,249 行×6 列**（只读实测，样本首行 x=y=0.0——`0` 哨兵实在，照装原样＋统计入账不改数）；**China_Periods_ReignDates＝现成纪年对照表**（china_chron，意外丰收随批装）；新见三专题层（PhysiogMacroregions/SMR/Manshu）。**成案要点**：腿 D（24 矢量层＋gns 并单表 chgis.v5_gns）／腿 E（addr_xy→public，腿 A 管道复用）／腿 F（china_chron）／庚（Hartwell 研读子案）；验收七项（逐层三方对账/ADDR_XY 三方＋哨兵统计/中文抽验/GiST 清点/**桥补 3,871 探索项不设承诺**〔V5/V6 id 制式或不同，成败皆入账〕/1911 县面判县空间复测/压力门禁）；压力六措施承继、**max_wal_size 不动**（总量约 200MB 远小于首批）；**工作量答**：机器时间 <30 分钟、全程 1–1.5 小时＝首批零头；差异项六条默认裁定（DEM 探测/gns 并表/Hartwell 拆案/v5_time 跳装/三专题装/编码承 UTF-8＋WGS84）＋**数据源默认直读镜像盘 harvard-full**（第二批货不在 usedata 工作集，扩围候改裁）；回滚＝逐张 DROP（首批产物不动）。R-06 四项：冗余查（第二批唯一成案处＝§14，§2 缓办段系挂账原文不回改）；矛盾查（§2③ ZZZ 不装定案与 14.1 处置表一致；14.6 工作量与 §13 首批实测基数〔594MB/562 万行/36 分钟〕核同）；锚点查（101 zip/19,249 行/2,866+2,467 条目/10MB gns 与实勘输出逐字核同；表名从源文件名校验）；账目闭环（cbdb-load✓ 本账✓；features/codemap/cbdb.md §9 联动列 14.8 执行批内兑现——成案批不动他账）。**另记**：wiki 会话 6 处未提交改动（bugs/cbdb/codemap/features/index/rules）本批全程未触碰、提交皆指名 add（git status 实证在案）；工作树余 6 处系该会话待收口之物、非本批残留。**候"同意"开工。** | commit `f452374`

- **2026-09-25 | `docs/cbdb-load.md`（§12.2 版本注记＋§12.4 出处行改写）| 查证补正批（答用户问"《User's Guide》下载了吗？放到 NAS 上了吗？"）** | 依据：用户问句＋答前实勘义务。**实况三查**：① 开发机 /tmp 两件在位——`cbdb_guide.pdf` 8,531,184 B（sha256 `cab7d155…`，153 页）＋`cbdb_guide.txt` 192,924 B（09-24 wayback 所获，落盘纪律下当时注记"仅 /tmp 不入工作区"，**正式归宿始终未定**）；② **NAS 本有官方旧版**——`harvard-full/doi_10_7910/DVN/P8U8RC/CBDB_Users_Guide_2018.pdf`（5,478,089 B，sha256 `bd1c7461…`，与 CBDB_aw_20180831 Access 包同树）＝**2018 版，与我件不同文件不同版本**；③ **账面误记自纠**——初录"rev. 2021-10"经 PDF 元数据实测（CreationDate **2023-10-06**，Acrobat PDFMaker 17；扉页修订日期因字体编码文本层丢失）证伪，补正为"2023-10 生成之修订版"；本账一切页码/行号引用（ZZZ §6 清单等）**钉 2023 修订版**、与 NAS 2018 版不通用——此声明已入 §12.2。**风险提示如实报**：/tmp 易失（重启即丢），丢后 ZZZ 蓝图内容不损（11 表清单已全量入账 §2③）但引用锚（153 页/行号）不可复核。**处置三选呈报候令**（落盘纪律：用户给位置或删，二者择一后方动）：(a) 推荐＝收 `usedata/harvard/cbdb/`（sha＋README 出生纸＋账面登记，锚永久可核）；(b) 改钉 NAS 2018 版（删 /tmp，引用按 2018 版重核）；(c) 删除（账面注记锚失效、内容以 §2③ 转录为准）。R-06 四项：冗余查（版本注记唯一展开＝§12.2，§12.4 指针）；矛盾查（旧"rev. 2021-10"两处措辞已整体改写、无双口径残留；log 历史行不改——追加体例，以本行为补正记录）；锚点查（两件 sha/大小/页数/元数据日期与实测输出逐字核同；P8U8RC 路径实勘在案）；账目闭环（cbdb-load✓ 本账✓；codemap 不动——usedata 行候处置令后方更新；bugs 无涉）。| commit `186e16f`

- **2026-09-25 | `docs/cbdb-load.md`（§2③ ZZZ 处置定案＋增益块口径同步）| 答复入账批（用户问"ZZZ 是干什么的，只是 Access 用的？我们不需要，没有什么新东西？"）** | 依据：用户问句＋处置口径"我们不需要"。**答前查证**：家中账 cbdb.md §9.3（现状/替代包）＋**《User's Guide》153 页全文**（前批 wayback 所获，/tmp 文本尚在）grep ZZZ——命中官方定义章 §6 "Denormalized Tables"＋查询章解说，逐段实读。**结论三条**：① ZZZ＝官方**反规范化派生表**——基础关系表只存 ID 码，ZZZ 预 join 多表把码填成人名/地名/官名/类型描述，官方原话用途＝"simplify the process of writing queries"（Access Query Designer 补丁）；② **零新数据**——指南自证派生（例：ZZZ_BIOG_ADDR_DATA＝BIOG_MAIN＋ADDR_CODES＋BIOG_ADDR_CODES 拼成），78 表全在我库、任何 ZZZ 表一条 CREATE VIEW 可重建，PG 毫秒级 join 令其存在理由蒸发（§13 探针实证）；③ **"蓝图"亦无须下载**——官方 11 表配方清单（每张 fills-in 何物）已从指南**全量提取入账 §2③**，将来建视图照账面写 SQL 即可，215MB 7z 包彻底无关。**处置定案＝不装**（用户口径＋上述实证），§2③ 重写＋增益块"建议组合"行同步（"取图纸不装货"→"已定不装，蓝图已入账"）。R-06 四项：冗余查（ZZZ 唯一展开＝§2③，cbdb.md §9.3 系数据边界账不动、口径相容）；矛盾查（前批"大概率不必装/取蓝图"两处旧措辞已随定案整体改写，无双口径残留）；锚点查（11 表名与指南 1497–1523 行清单逐字核同；fills-in 描述与原文对译）；账目闭环（cbdb-load✓ 本账✓；features/codemap/bugs 不动——无新事实层，处置系既有缓办项之定案）。**第二批余项（④V5 族/②ADDR_XY/①Hartwell）仍候另令。** | commit `01cd6c2`

- **2026-09-25 | `docs/cbdb-load.md`（§2"装上增益与开发支撑"块新立）| 答复入账批（用户问"装了我们会多出什么功能来，为以后开发项目能提供什么帮助与信息"）** | 依据：用户问句；吸取前批教训（展开不留对话），答复摘要随批入账、与"不装损失"块互为表里不复述。**要点**：④V5 族＝价值最大（1911 县面＋1990 CITAS→点能判县/三期对比/县级图；1820/1911 底图族含乡镇点＋河流湖泊海岸线→省府县镇四级体系＋漕运经济史专题；v4_time_prov 省级聚合；dem 地形因子；GNS 古今地名对照）；②ADDR_XY→可落图地址 7,125→约 2 万+（污染剔除法在 cbdb.md §9）；①Hartwell 独门用处＝5,333 件逐年切片系时间轴动画现成素材；③ZZZ＝"取蓝图不占仓"（结构作视图设计参考，货不装）；全栈图景＝五类未来项目底座（人物空间化/历史地图平台/量化历史/知识图谱家谱/专题史）；成本＝货在本地镜像盘、首批已验证管线、每层分钟级、GBK 少数层转码法已实证；建议组合＝④＋②。**无新事实层（纯既有实勘事实之分析组织），他账不动。仍候另令方动第二批。**R-06 四项：冗余查（增益块与损失块互补分工明记、无重叠展开）；矛盾查（"约 2 万+"与损失块"3,871＋19,161"口径核同〔桥 7,125＋ADDR_XY 增量，去重后约 2 万级〕，措辞已留"约"）；锚点查（层名/文件名与 454126c 实勘清单核同）；账目闭环（cbdb-load✓ 本账✓）。| commit `5da2410`

- **2026-09-25 | `docs/cbdb-load.md`（§2 缓办段④重写＋"功能损失"块新立）| 实勘修正批（答用户问"如果我不装你说的四项，会缺少什么功能"）** | 依据：用户问句＋答前实勘义务（只读 find/ls 两轮，零写动作）。**实勘推翻账面一处**：§2④ 原文"县面（cnty_pgn）、1911/1820 层……都在本地 chgis-v6 树里"——实测 **chgis-v6 树只有首批已装三件＋数据字典＋纪年表**；县面/1911/1820 实在 harvard-full `DVN/M7WEFY`＝**CHGIS V5 树**（V6 官方从未发布时序县面）；29302＝Hartwell 包 DOI 目录在位实证。**V5 树全清单入账**：v5_1911 族（**cnty_pgn＝本地唯一历史县面**/cnty_pts/pref/prov/twn_pts）、v5_1820 族（底图要素齐全：cnty_pts/pref/prov/twn_pts/coast_lin/coded_rvr_lin/lks_pgn）、v4_time_prov（省时序面）、1926/1997 省面、**1990 CITAS 县/府面**、**v5_dem**、gns_*（仅 GBK）、ChinaW_pts、Manshu index、tibet_twns；主要层 GBK/UTF 双变体（编码裁定同差异项⑤）。**功能损失分析入账**（用户问答照录摘要，防再犯"展开只在对话"之疏漏）：已具备面（关系查询/府级 GIS/桥 7,125/三小件）不受影响；损失①**县级边界面**（判府不判县——最重）；②标准年代底图（无乡镇点/水文/海岸线）；③坐标覆盖面（桥余 3,871＋无 pt_id 之 19,161 地址）；④清单外新见（DEM/GNS/省时序）；**ZZZ 与 Hartwell ≈零损失**（PG 视图替代/V5 族重叠）；决策参考：真补缺口者＝④V5 族＋②ADDR_XY，①③可长不装。R-06 四项：冗余查（V5 清单与损失分析唯一展开＝§2，本行摘要）；矛盾查（④原文错误句已整体重写、无双口径；"3,871 多半指向④/①"句与 V5 点层事实仍相容保留）；锚点查（M7WEFY/29302 路径、文件名与实勘输出逐字核同）；账目闭环（cbdb-load✓ 本账✓；features/codemap 不动——F-13"CHGIS 其余层"系指针级措辞与新事实相容，codemap harvard-full 行本就记 CHGIS 树全量）。**仍候另令方动第二批。** | commit `454126c`

- **2026-09-25 | `docs/cbdb-load.md`（§2 缓办段重写为四条明细＋§13 收尾加指针）| 挂账补录批（第二批缓办清单入账）** | 依据：用户先问"挂账候令的，你记在那个文档里了？"、再令"**你的四条缓办，我在cbdb-load.md没有看到。你应该一字不差的写到里面的。**"——查实：§2 缓办段原只有一行简述（成案时旧文），四条详细展开（各自何物/何在/为何缓办/装来何用＋ZZZ 两条新判断＋"可补桥余 3,871"释义）只在对话答复里、**未入账＝账目疏漏（R-02 挂账义务未尽），如实自认**。本批照录补齐：四条明细一字不差入 §2（仅时间指代词"昨晚"换算为绝对日期/节号，注记在段首）；ZZZ 两判断随之入账（不在 2024-02 档案件——§13 no-table 清单可证；唯一须另下载件 `latest_ZZZ_tables.7z` 215MB；评估：PG 建视图可替代、大概率不必装）；原简述余项（扩展补数据）保留、已并入项注记（1911/1820/GBK/西安80→④）；§13 收尾"第二批挂账不变"句加"四条明细唯一展开＝§2"指针。R-06 四项：冗余查（四条明细唯一展开＝cbdb-load §2；F-13/log/§13 皆一行指针不复述）；矛盾查（旧一行简述已被新块整体替换、无双口径残留；对话与账面数字核同 7,125/3,871/10,996/5,333/215MB）；锚点查（§9.2/§源流/§13 节号与 harvard-full、chgis-v6 树位与 codemap 在册行核同）；账目闭环（cbdb-load✓ 本账✓；features/codemap/bugs 不动——F-13 状态行已含"第二批候另令"指针、无新事实层）。**仍候另令方动第二批。** | commit `04cfd42`

- **2026-09-25 | `docs/cbdb-load.md`（§13 执行记录＋状态行收口）＋`docs/pg32b.md`（§12 实例事实）＋`docs/features.md`（F-13 状态推进）＋`docs/codemap.md`（树行/工作集行/实例行）＋32 侧 README/compose | 首批装数执行批（开工→验收全过→收口）** | 依据：用户令"**同意，另外你要考虑到32主机的压力**"（＝差异项九条全按＋压力约束入案）。**执行摘要**：方案先修（§8 压力防护六条：顺序单流零并行/nice+ionice 全程/compose `cpu_shares: 512` 生产 2:1 让路/maintenance_work_mem 512→**256MB** 降档/步间压力闸门 loadavg＞4 或 pg32 非 healthy 即停/夜间窗口≤2.5h）→23:48 开工→00:24 收工，**历时 36 分钟（预算 24%）**。**战果**：`cbdb` 库建成——**84 用户表**（CBDB 78＋chgis 空间 3＋TSV 小件 3；另工具辅表 2）／**总行 5,686,842**＝CBDB **5,623,075（sqlite==COPY==PG 三方逐表零差异，与 cbdb.md 官方总数逐位全同）**＋CHGIS 19,578 要素（**县点 10,522＝家中账标答**）＋TSV 44,189；**索引 321**（官方 2024-02 档案件 370 条 DDL 机械迁移：可套用 308＋no-table 46＋col-drift 16 全列人工复核清单——0919 活库件天生零索引即账载"370→0"缺项就此销案；FK 补 6 含 0919 新表 merged_person_data 二键；max_wal_size 1→4→1GB 实测起落）；**验收七项全过**含桥验证（chgis_pt_id 10,996→府点 997＋县点 6,128 并 7,125/64.8%；ST_Within 跨腿三点"歷山書院→濮州"语义直中、x=经度实证）与性能探针（**sqlite 时代"60 秒未出"之 exists 实测 0.124s**，~500 倍）；中文零乱码三处实样；**pg32 生产零感知**（healthy 全程、canary 0.092→0.089s、峰值 loadavg 1.87＜阈 4.0）；36 不可达零触碰天然成立。**事件六起全披露全当批解决**（ident 大小写→重建 DDL；索引名带空格→消毒+IF NOT EXISTS；Polygon/MultiPolygon→`-nlt`；python I/O 写法→修；查询级猜错两处→重跑；conf-LOG＝镜像 entrypoint 固有怪癖观察——reload 实弹定位，无害，pg32 同血统潜伏 0 触发）——库 log 10 条 ERROR/LOG 全数归因，**零数据完整性问题**。32 侧：README 装数节＋/tmp 审计件已清（再生法在 §13）；容器 /tmp 零残留。R-06 四项：冗余查（执行唯一展开＝cbdb-load §13，pg32b §12 只记实例级事实互不复述，F-13/codemap 指针）；矛盾查（"84 表 vs pg_tables 86"差额实测归位＝2 工具辅表注记两处账；308 vs 账载 307 差 1 如实注记口径；§4.5 原"实测条数为准"条款与 0 索引实况的衔接在 §13 索引节明记）；锚点查（5,623,075/10,522/661,969/30,157 与 cbdb.md 及家中账参证全同；漂移 16 条列名与 cbdb.md 在账例逐字互证）；账目闭环（cbdb-load✓ pg32b✓ features✓ codemap✓ 32-README✓ 本账✓；bugs 无涉——六起事件皆本批自制当批解决非宿疾，conf-LOG 怪癖系观察注记非故障）。**候另令**：第二批（Hartwell 29302／ADDR_XY/ZZZ／CHGIS 其余层——可补桥余 3,871）；36 恢复后 pg36 核验＋同构/迁移另议。| commit `23cfa2d`＋`1767e98`

- **2026-09-24 | `docs/cbdb-load.md`（§12.2/§12.3/§12.4 补验更新）| 调研补验（两处官方原文全文批）** | 依据：用户令"你再试一试，现在应该能打了"——前批两处未抓到的官方原文重试。**结果：两处全文到手**——① **PostGIS 3.6 官方手册第 4 章 §4.7 装载章**（postgis.net 直连已通〔用户放开网络〕，web_fetch 截断→curl 全文 161KB 本地按锚切片）：官方内建装载法两条（SQL 语句／shp2pgsql 装载器），关键旗标 `-D`（dump＝COPY 快速模式，官方原话"Use this for very large data sets"）/`-I`（GiST）/`-W`（DBF→UTF8）/`-s`（SRID）逐条对照本方案 ogr2ogr 路——**功能全覆盖**（.prj 自动 SRID≈-s、GiST 自建≈-I、UTF-8 变体≈-W 从根免转、/vsizip 直读 zip 为 shp2pgsql 所无）；**差异记录**：若严格按手册原生工具可加装 PGDG `postgis` 包（F-13③(a) 候选），默认不变；② **CBDB《User's Guide》全文**（Fuller rev.2021-10，**153 页**；harvard 直连仍 403＝服务器端反爬非本地网络→wayback 存档 `20240914131845id_` 取 PDF 8.5MB＋本机 `pdftotext` 转文）：官方支持面＝序言原话 **Access（主）＋SQLite（"for quantitative researchers and Mac users"）**＋历史附录 CBDB_SS（SQL Server Express，绕 Access 文件大小限制产物）；**全文检索 PostgreSQL/PostGIS/MySQL 命中＝0**——§12.2"官方无入 PG 指引"由推断**升级为全文实证**。方法注记：web_fetch 不解析 PDF（"unsupported content type"如实记）；临时文件仅 /tmp 不入工作区（落盘纪律）。R-06 四项：冗余查（补验只更新 §12 原节，不新开账）；矛盾查（前批"未抓到如实记"字样已随实证改写，不留双口径）；锚点查（153 页/8.5MB/命中 0/旗标原文与实抓输出核同）；账目闭环（cbdb-load✓ 本账✓；他账不动——纯调研补验无新事实层）。| commit `cb624fd`

- **2026-09-24 | `docs/cbdb-load.md`（§12 官方与社区经验对照新立＋§4.5 索引段修订＋§11 执行记录指针改号 §13）| 调研记账（装数方案开工前对照批）** | 依据：用户令"你查一下官方对入新库有没有什么建议？或者网上有没有其它人有类似经验，可供我们参考"。**全程只读调研（web 检索/抓取），32/36/NAS 零动作**。**要点**：① **PostgreSQL 18 官方文档 §14.4《Populating a Database》九条建议逐条对照本方案**（经 web.archive 实抓全文）——七条天然吻合（COPY 管道／先灌后建索引／无 FK〔官方原话百万行级触发器队列可溢出"necessary, not just desirable"＝保真策略再加背书〕／maintenance_work_mem／ANALYZE／单事务／pg_dump 要点转指针给 36 迁移腿）；**一条补入**：max_wal_size 临时抬高（§4.5 修订：索引段 `ALTER SYSTEM SET max_wal_size='4GB'`＋reload，毕 RESET 复原，前后 SHOW 实测——pg32b 专库无生产流量、盘余 196G 安全）；**一条有据不采纳**：wal_level=minimal 禁归档（须重启＋废备库预案＋archive_mode 本 off＋600MB 级收益小——差异如实记）。② **CBDB 官方对入 PG 无任何指引**——HF 数据卡实抓只有下载点＋许可 CC BY-NC-SA 4.0（与本账旧记一致）；含义：管线属自建，§7 对账验收七项即安全网。③ 社区：**sqlite→PG 大路工具＝pgloader**（Neon/Netbird/Render 指南一致推荐；Debian 有官方包）——本案不用（宿主装机须另令＋python 标准库管道零新装且类型/BLOB/NULL 全可控），**备选地位记录在案**；社区踩坑清单（类型亲和/BLOB/标识符大小写/布尔）经核 §4.2 映射表逐条已覆盖；**CBDB 专向 PG 迁移开源项目检索未见**（与②互证）；CHGIS 编码历史坑（postgis-devel #1303）→本案 UTF-8 变体从根绕开。④ **未抓到的两处如实记**：CBDB 用户指南 PDF（403 反爬）与 PostGIS 手册装载章（沙箱 DNS 挡＋存档代理签名失效）——不影响判定（工具选择已在 32 本机实证 `ogr2ogr --formats`）。出处五组链接列 §12.4。R-06 四项：冗余查（调研唯一展开＝§12，log 只摘要）；矛盾查（§4.5 修订与差异项⑧"配置文件不动"口径冲突自查——max_wal_size 系 ALTER SYSTEM 临时抬高＋毕即 RESET，非常驻改配置，§4.5 已注明"不动配置文件"仅指 maintenance_work_mem 会话级，ALTER SYSTEM 段自带复原步骤，无矛盾残留）；锚点查（九条建议序号/原文关键句与实抓页面核同）；账目闭环（cbdb-load✓ 本账✓；features/codemap/bugs 不动——纯调研无新事实层）。| commit `eac52e0`

- **2026-09-24 | `docs/cbdb-load.md`（§0 工作集事实新注＋§2 源路径改判＋§5.1/§8 Step0 对账口径同步）＋`docs/codemap.md`（§2 新增"装数工作集 usedata/harvard"行）| 执行记账（装数工作集拷立批）** | 依据：用户令"你把我们要用的数据copy一份到usedata/harvard下，这样我也知道我们是在使用这些数据。原来那些就当是下载的原数据备份"＋落点确认"/mnt/wd61workmetadata/usedata/harvard"。**执行**：拷前探查定写侧——32 镜像脚本实核 `rsync -a --delete` 纯镜像语义（写镜像侧会被每日 12:00 同步冲掉）→**落点＝NAS 侧**，32 镜像盘读→NAS 写单程；预检 guard 发现目标目录**用户已手建且为空**（owner zky，22:55）→核实无物后续行（零偏差）；拷入**数据 7 件＋随附 6 件＝13 件/594MB**（cbdb sqlite/json＋V6 三 zip utf_wgs84＋三 .tab＋EULA/字典/家中验证账），README 出生纸＋SHA256SUMS 同目录。**核验三重全过（新鲜输出在案）**：①逐件 sha256 源（镜像盘）vs 目标（NAS）**双端全同**（8 数据件＋json）；②府点/县点与家中 `SHA256SUMS.txt` 账载值**全同**（`48f6a50b…`/`3b431d2e…`）；③sqlite＝官方发布值 `bde1bb8e…`；另本机（dev 侧挂载）实测 `sha256sum -c` **13/13 OK**——用户可见面复核。**口径改判**：装数唯一工作源＝`usedata/harvard`（cbdb-load §2 表全列改判、§5.1 对账前置改为工作集自检、§8 Step 0 点名改 workdata 路径＋sha -c）；**原树降级＝原数据备份只读（用户令口径）**。落盘纪律：位置系用户明示✓；本工作区零数据副本✓（只记账）。R-06 四项：冗余查（货位权威＝cbdb-load §2；目录出生纸＝NAS 侧 README 供用户浏览、账内只记指针与哈希前缀不复述全表）；矛盾查（F-13"数据在家/NAS"历史述不改——工作集系新增层非否证；§2 旧源路径整表替换无双账残留）；锚点查（594MB/13 件/三组 sha 前缀与执行输出逐项核同）；账目闭环（cbdb-load✓ codemap✓ 本账✓ NAS 侧 README+SHA256SUMS✓；bugs/improvements/features 不动——无新缺陷；F-13 装数指针链经 cbdb-load.md 仍有效）。| commit `ef1a0df`

- **2026-09-24 | `docs/cbdb-load.md`（新立方案）＋`docs/features.md`（F-13 状态行：装数靶机改判＋方案指针）＋`docs/index.md`（实施方案段新条）＋`docs/codemap.md`（§1.2 树新行）| 成案（哈佛资料装数首批→pg32b，候开工口令）** | 依据：用户令"36故障，你在32主机上搞"——36 不可达经用户确认故障，装数靶机由 pg36 改判 **pg32b@32**（F-13② 改判如实记账；36 恢复后迁移/双轨另议不在首批）。成案前补**只读核货两轮**（32 实测）：①V6 三件 zip 内整包 shapefile 四变体俱在（utf_wgs84 选定）；②Hartwell＝两 zip **5,333 件多图层年代包**→缓办第二批（差异项①）；③三小件 `.tab` **实为 TSV**（BOM＋tab＋全引号，表头即列名，书院件含经纬度列与尾部空列名须清洗）——MapInfo 路作废；④`cbdb-project/` **无现成 PG DDL**（find 全空）→建表/索引权威源＝sqlite_master 改写迁移；⑤镜像盘 sqlite 复验 78 表/BIOG_MAIN 661,969/ADDR_CODES 30,157 与官方账全同；⑥家中 `SHA256SUMS.txt`/`VERIFICATION.md` 在镜像盘（县点 **10,522 条标答**＋两 zip sha 可对账）。**方案要点**：通路三零（零传输/零解包〔ogr2ogr `/vsizip/`〕/零中间落盘〔管道直灌；腿 B 仅 zip 暂入容器 /tmp 用毕即删，compose 零变更〕）；新库 `cbdb`（CBDB 表 public 小写名、空间三层 `chgis` schema、仅建 postgis）；原样保真（无 PK/FK/清洗，腿 C 全列 text）；索引官方全量迁移＋FK 命名列目测补建＋不兼容句进人工清单不静默丢；对账验收七项（78/78 行数＋总数 5,623,075＋县点标答＋GiST＋桥验证两发：`chgis_pt_id` join＋ST_Within 人→府面）；回滚一句 `DROP DATABASE cbdb`；资源会话级 maintenance_work_mem 512MB、总预算 ≤2h（2C4T）。**差异项九条列 §9 候裁**（默认皆按本案）。**R-01 边界：只成案未施工**（32/36/本机零改动，全部动作只读）。R-06 四项：冗余查（装数方案唯一展开＝cbdb-load.md，F-13/index/codemap 皆摘要＋指针；核货事实记 §0 不重复入账）；矛盾查（F-13②"靶＝pg36"→改判如实推进不掩旧账；F-13④通路缺口对本批自解〔32 本地直读〕；pg32b"演练台"角色→兼营装数靶机注记在案 §1）；锚点查（§2 货位七件逐一 stat 核实在位；10,522/sha 前缀与家中验证文件核同）；账目闭环（cbdb-load✓ features✓ index✓ codemap✓ 本账✓；bugs/improvements 不动——无新缺陷无改进项）。| commit `6c81b28`

- **2026-09-24 | `docs/pg32b.md`（§11 执行记录新立）＋`docs/codemap.md`（pg32b 实例行推进）| 执行记账（pg32b 装载器补装批）** | 依据：用户令"那就装上吧"（承 §10"真缺口①装载器两无"之补法呈报）。**范围只 pg32b**——用户前令"现在不要考虑pg36"，**36 同构联动同建挂起候另令**（谱系分叉一层如实注记 README/codemap/§11）。**方法＝增量层重建**：基座钉锚 tag `pg32b-base:18-pgdg`（＝36 拷贝原件 06ad5ef6…，防同名自叠层）→Dockerfile `FROM` 锚＋`gdal-bin`（**PGDG 官方源浮动**，实装 3.13.2+dfsg-1.pgdg12+1 与 F-13 探查候选逐字全同）＋LABEL 来源注记→compose 增 `build:` 段（自包含）＋`.dockerignore` 排除 `.env/data/README`（凭据不入构建上下文）。**核查九点全过**：build 成→`up -d`→**healthy t=6s**；新镜像 `d562e436…`＝基座＋1 层（**+42MB**，"libgdal 已在基座"预判兑现）；`ogr2ogr`＝GDAL 3.13.2；**驱动三件套实测**（ESRI Shapefile rw＋uv 含 `.shp.zip` 直读／CSV／**PostgreSQL/PostGIS rw**）＝**§10 真缺口①销案、CHGIS 空间腿入口已通**；服务器面分毫未变（18.6／available 81／仅 plpgsql／512MB／datadir 55M／TZ 逐秒一致／基座 label `pg36.*` 完整）；shp2pgsql 仍无（单件政策，如需另裁）。**观察注记（非本批所致，如实不猜因）**：收尾时 **36 不可达**（ping 100% 丢＋ssh No route to host，22:22 两度实测）——本批对 36 **零写操作**（仅 17:10 只读 save/inspect 当时核实无恙）；pg36 运行态待 36 恢复后核验（核验项列 §11）。R-06 四项：冗余查（执行事实唯一展开 §11，codemap 行摘要＋指针）；矛盾查（"与 pg36 同字节"旧判词→谱系分叉如实推进不掩旧账；F-13 ③(a) 于 36 侧仍挂——36 未动无矛盾）；锚点查（镜像 ID/gdal 版本/驱动清单与实测输出逐项核同）；账目闭环（pg32b✓ codemap✓ 本账✓；bugs 不动——36 不可达系观察项非本项目缺陷，归用户/homelab 知悉）。| commit `e6ab9ef`

- **2026-09-24 | `docs/pg32b.md`（§10 完备性复勘新立）| 复勘（建成后台次批——演练台缺项审计）** | 依据：用户令"就pg32b这个库，你现在查一下，有什么缺少的吗？"。**全程只读**（库内九查＋宿主五查两轮 ssh；32/36/本机零改动）。**要点**：① 箱内健全（设置/hba/角色/客户端 18.6 四件套/available 81 项 34 点名命中/日志 4 条 ERROR 系我方查询引号笔误自伤非服务器病——如实自报）；② **通路实测意外已通**——32 有 cron `/opt/mirror-nas.sh` 每日镜像 NAS→本地大盘，`cbdb_20260919.sqlite3` sha 前缀 `bde1bb8e…` 与官方全同、`harvard-full` 374 件全目、`chgis-v6` 在，**CBDB 灌入腿零缺口**（python3 sqlite 模块＋容器内 psql）；③ **真缺口三条**：shapefile 装载器镜像宿主两无（补法 gdal-bin 重建，**与 pg36 同构性联动须同日加建**）／pg_hba 无远端复制行（仅流式备库架构才需，归备份 agent）／无目标库（--create 即成，运行态非缺陷）；④ 备注 archive_mode=off（WAL 归档裁决项）、pointcloud 未装（21 包终选未含，pg36 同，非缺）。R-06 四项：冗余查（审计唯一落 §10，F-13 通路三候选暂不动——镜像发现待用户口令装数批再引用，避免双账）；矛盾查（"partman 缺失"查询笔误当场自纠为实名 pg_partman；4 条 ERROR 定性有日志原文为证）；锚点查（sha/字节数/文件数与实测输出核同）；账目闭环（pg32b✓ 本账✓；bugs 不动——缺口均为待裁事项非缺陷）。| commit `71d62c8`

- **2026-09-24 | **`docs/pg32b.md`（§9 执行记录新立＋§2 前向注＋状态→已建成）**＋`docs/codemap.md`（§2 新增"32 之 pg32b 实例"行＋§1.2 树行推进）＋`docs/index.md`（pg32b 条目→已执行）＋`docs/features.md`（F-14→已建成）| 执行记账（pg32b 施工批）** | 依据：用户"同意"令承接"好，那就听你的建议在32机上装吧"——`docs/pg32b.md` §4 Step 0–5 执行完毕，差异项六条全数默认照案。**要点**：Step 0 复核五空全绿＋基线快照；Step 1 镜像管道移植 **12.5 秒本机零落盘、指纹两端全同 `06ad5ef6a527…`**、32 retag `pg32b-full` 无杂 tag、36 原件核实无恙；Step 2 三件套落盘（密码 32 现算、明文零外落）；**执行偏差一处（草案自身缺陷，如实记）**：§2 草案 YAML 漏 `env_file:` 行→首起官方镜像缺密码拒 initdb 即退（**副作用零**，数据目录未触），补行重建 **6s healthy**（续修 README 块重定向小疵）；Step 4 **核查九项全过**：PG 18.6 与 pg36 全同／冒烟库 postgis＋address_standardizer 建删净（移植完整性复验）／参数九条合案／**时区 §9 全规格三挂载实挂逐秒一致（32 无 B-02，与 pg36 裁决相反相成）**／hba scram 末行／远程负向拒正向通（借 pg32 客户端答 18.6＝端口归属版本铁证）／运行时五查合案／**现有服务零影响**（基线 11 容器不漂、pg32 healthy 依旧、ss 恰＋5433、空闲 79MiB、盘＋~1G 即镜像）；Step 5 收口＋交接素材（`32:5433`/scram、凭据位置、回滚三步）在 §9。**边界自检**：32 改动仅 pg32b 一处、pg32 零接触；36 只读；本机零落盘零安装；端口登记归另派 agent（同 pg36 口径）。R-06 四项：冗余查（建成事实唯一展开 §9，codemap/index/F-14 皆摘要＋指针）；矛盾查（施工批所引镜像批内容 commit 实核 `eb64bdd` 在账；nginx32 unhealthy 维持观察项不入账——homelab 域）；锚点查（§9 数字与执行输出逐项核同：12.5s/6s/21 包/79.3MiB/端口表）；账目闭环（pg32b✓ codemap✓ index✓ features✓ 本账✓；bugs 无新增——env_file 漏行系本案草案缺陷当场修正并记录在案，非遗留缺陷）。| commit `0e25d65`

- **2026-09-24 | **`docs/pg32b.md`（新立方案）**＋`docs/index.md`（实施方案段新条）＋`docs/codemap.md`（§1.2 树新条）＋`docs/features.md`（F-14 状态→已裁主机并成案）| 成案（pg32b 施工方案批，候开工口令）** | 依据：用户令"好，那就听你的建议在32机上装吧。"——**择机＝32 裁决落地**，建议路线（复用镜像/5433/缩配/新凭据）随令成案。成案前补**只读侦察**（32：`/opt/mydocker` 目检、pg32 inspect〔挂载/网络 postgres_pg_net/5432 绑定账〕、5433 空闲、pg32b 名/目录/网络空闲、时区两文件；36：`docker image inspect` arch；36↔32 ssh 互探＝**不能直连**（host key 未建，属系统配置不建不动）→ 传输定案走本机管道 `docker save|ssh→ssh|docker load` 零落盘）。**方案要点**：复用 `pg36-full:18-pgdg`→retag `pg32b-full`（同字节拷贝＝与 pg36 全同构，验货台本义）／口 **5433**／参数缩配 512MB/8MB/128MB/2GB/50conn（有意不同于 pg36——让 32 现有七容器）／时区 `/etc/timezone:ro` **全规格照挂**（32 无 B-02，与 pg36 §11⑤ 相反相成据实处置）／新凭据 32 现算不出 32／回滚＝down+删目录+删镜像零残留。**差异项六条列 §7 候裁**（命名/端口/缩配/同构策略/时区/label 冠名——均默认照案，口令时可一并改裁）。**R-01 边界：只成案未施工**（36/32 零改动，全部动作为只读探测）。R-06 四项：冗余查（七要素权威处＝pg32b.md §2；镜像事实全走指针→pg36.md §13/codemap/F-14 三账，零复述）；矛盾查（F-14"挂账"→"成案"状态推进如实；pg32-upgrade.md 搁置案不复活——本案系旁添第二实例非升级；codemap"32 暂不升级"旧判与本案无涉——不动 pg32 本体）；锚点查（§0 四指针逐一核实在位；5433/名/目录空闲与实测输出核同）；账目闭环（方案✓ index✓ codemap树✓ features✓；bugs/improvements 不动——无新缺陷无改进项）。| commit `7ec94d4`

- **2026-09-24 | `docs/features.md`（F-14 补"镜像复用核证"条）| 修订（讨论期事实补记——第三实例镜像复用问答批）** | 依据：用户问"36上的那个镜像可以直接用吧？"。**只读核查**（36 上 `docker image inspect` 取 arch/os/size；32/33 各一眼 `/etc/timezone`＋`date +%Z`＋`docker --version`；三机零改动）。**要点**：① 镜像 `pg36-full:18-pgdg`＝amd64/linux（压缩层集≈251MB／展开 1.04GB）——32/33 均 bookworm＋Docker 29.7+，**可直接跑**；② **32/33 的 `/etc/timezone` 均系正常文件（Asia/Shanghai）——无 36 之 B-02 缺陷**，标准 §9 时区挂载两行可全须照挂（此系顺带排雷：若 32/33 同坑，复用镜像仍要面对挂载裁决）；③ **分发法定调＝`docker save`→LAN→`docker load`**——异机重建 Dockerfile 虽殊途同归，但备份/演练实例与 pg36 **同镜像＝pg_dump/restore 工具版本全同**，且裁减裁决与浮动版本策略天然继承，故优；④ **复用≠照抄**四件：retag（镜像名系 36 冠名，随新实例名走）／compose 七要素另立（容器名／32 则 5433 让口／数据目录／新 .env 新密码不复用 36 凭据）／参数缩配（演练机 shared_buffers 256–512M，勿照搬 1GB）／时区挂载在新机无 B-02 障碍可全规格。R-06 四项：冗余查（事实唯一落 F-14 新条，codemap/pg36.md 不动——无部署变化）；矛盾查（与 pg36.md §5"不钉版本"政策一致——save/load 恰冻结当前构建，与浮动政策不冲突，复用后镜像独立存在；与 F-14 前条"次选 33 则精简版"兼容——复用全功能镜像亦得精简之实，扩展按库启用）；锚点查（arch/size/时区路径与实测输出逐项核同）；账目闭环（features✓ 本账✓；bugs 不动——32/33 无缺陷反证）。| commit `eb64bdd`

- **2026-09-24 | `docs/codemap.md`（§2 新增"第三实例选型三机实勘"行）＋`docs/features.md`（F-14 状态推进＋新增"三机比较"条）| 实勘＋比较结论（第三实例择机批）** | 依据：用户令"那你看一下32，33，35那个更适合？"。**全程只读**（33 首勘／32 复勘各一轮 ssh——35 系同日前批已勘；未装未改未起停，三机零改动）。**要点**：① 33（`lx01`）＝i3-4000M（与 35 同款）／可用 4.8G／SATA 余 **71G 三机最小**／无 PG、5432 空闲／12 个检索翻译系容器密度高／pg33 名与目录空闲；② 32 复勘＝i7-6567U 单核最强／可用 **5.5G 三机最多**／`/` 余 196G＋本地大盘 nas-mirror 余 **829G**／pg32(18.4) 空闲 72.9MiB、load 0.11——**但 5432 已被 pg32 占用（0.0.0.0 绑定），第二实例须让口（如 5433）**；③ 观察项顺报：**nginx32 unhealthy**（只报未动）。**比较结论（落 F-14，均待用户裁决、不成案不动手）**：推荐＝32（备份/演练实例常态极轻＋内存磁盘单核三机之最＋唯一常年跑 PG 主机；机器级容灾归备份文件层，与生产同宿主可接受），次选＝33（真隔离＋5432 直用，但磁盘最小），末选＝35（Gitea＋Dify 命脉机，最不宜添库）。R-06 四项：冗余查（三机全貌唯一展开于 codemap 新行，F-14 只落结论＋指针；35 细节认同前批行不复述）；矛盾查（F-14 原"意向 35"与三机比较并列如实——不改写诉求原话；与 F-13"32 暂不升级"不冲突——彼指改造 pg32 本体，此系旁添第二实例）；锚点查（codemap↔F-14 互指；版本/端口/磁盘数字与实测输出逐项核同）；账目闭环（codemap✓ features✓ 本账✓；bugs 不动——nginx32 系 homelab 域观察项非本账缺陷）。| commit `e4e0258`

- **2026-09-24 | `docs/codemap.md`（§2 新增"主机 35 实勘"行）＋`docs/features.md`（新立 F-14）| 实勘＋挂账（主机 35 勘察批）** | 依据：用户令"你上35主机上看看，我想pg在35上也装一个"。**全程只读**（三轮 ssh：系统/硬件/磁盘/GPU/docker 全盘/端口/NAS 挂载/PG 存在性与冲突五项核查/`docker system df`/gitea DB 配置探查；**未装未改未起停，35/本机零改动**）。**要点**：① 35（主机名 `lx02`）＝**家庭基础设施宿主——Gitea 代码服务（:17080/:17022，healthy，本项目远程仓所在主机）＋Dify 1.16.1 全家＋portainer-agent**，四台 docker 机中硬件最弱（i3-4000M 2核4线程／7.5G RAM 可用 4.1G／SATA SSD 余 88G，另有 19.8G docker 构建缓存可回收＝标准 §15 维护域未动）；② **PG 冲突排查＝零冲突**：宿主 5432 空闲（机上唯一 PG＝dify-db-postgres (15-alpine) 实测 `5432/tcp:null` 无宿主绑定、仅内网 `dify_dify-net`），pg35 名／pg35-full 镜像名／`/opt/mydocker/postgres` 目录／postgres 网络名全空闲；③ 35 系 Debian 12 bookworm——与 pg36 基座 `postgres:18-bookworm` 同款可径用；未挂 workmetadata（仅 NFS workcenter，同 36 格局）。**"35 装 PG"意向按 R-02 挂账 F-14**——定位（是否即 09-23 口径"专门备份用"那个实例）与规模（全功能 vs 精简）待用户明示，本批不成案不动手。R-06 四项：冗余查（35 全貌唯一展开于 codemap 新行，F-14 只留摘要＋指针）；矛盾查（"两实例格局"旧口径与 F-14 三实例意向并列如实、不擅改 F-13 历史判词，关系字段标"待明示"）；锚点查（codemap↔F-14 互指；标准 §15/§2、N-01 §四、pg36.md 骨架指针核实）；账目闭环（codemap✓ features✓ 本账✓；index 无新文档不动）。| commit `da91196`

- **2026-09-24 | `docs/features.md`（F-13 新增"装数前置实测"条）| 修订（讨论期事实补记——哈佛资料×pg36 配合问答批）** | 依据：用户问"哈佛那块的资料，我们用那些？哈佛的东西与我们的 pg 可以配合吗？"。**只读探查**（pg36 镜像内 `find /` 全路径＋`dpkg -L` 包清单＋基座容器 `apt-cache search/policy` PGDG 源；NAS `harvard-full/datasets.tsv` 65 集全目核读——DOI/件数/字节/标题；36/32/本机零改动、未导入任何数据、未装任何工具）。**要点**：① **pg36 镜像缺 `shp2pgsql`/`raster2pgsql`**——scripts 包（946 文件）只含 SQL 脚本，PGDG 现时 PG18 打包不含装载器二进制，`apt-cache search shp2pgsql` 空；CHGIS→PostGIS 腿差装载工具一步；② 补法三候选入 F-13（倾向＝镜像加装官方 `postgis` 装载器包及/或 `gdal-bin` 3.13.2 后重 build——仍全官方源、数据目录不动）；③ CBDB sqlite 腿**零新工具**（本机导出 CSV→stdin 管道 `\copy`，纸面通待实测）；④ 数据通路缺口仍在（36 未挂 workmetadata）三候选并列。**答复口径**：入库推荐清单（CBDB 主体用家中 0919 件而非档案件／CHGIS V6 选件／Hartwell／三小件随行；栅格扫描件·LoGaRT·版本堆不入库）与配合路径逐腿给出——**均为讨论期意见，待用户裁决，未立项**（R-02；四问＋通路问未决）。R-06 四项：冗余查（事实唯一落 F-13 新条，未在 codemap/pg36.md 复述）；矛盾查（与 pg36.md §13"扩展覆盖 34 项"不冲突——彼系 `pg_available_extensions` 扩展面，此系装载器二进制面，两面各自如实）；锚点查（cbdb.md §九／datasets.tsv DOI 与答复互核一致）；账目闭环（features✓ 本账✓；index/codemap 不动——无新文档、部署事实未变）。| commit `2fd851c`

- **2026-09-24 | `docs/pg36.md`（状态行→已执行＋§5/§6 前向注两处＋§11⑤ 新增＋**§13 执行记录新立**）＋`docs/bugs.md`（B-02 新立）＋`docs/codemap.md`（§2 新增"36 之 pg36 实例"行＋主机 36 行尾状态推进＋1.2 树行）＋`docs/features.md`（F-13 状态行→已建成）＋`docs/index.md`（pg36.md 条目→已执行）| 执行记账（pg36 施工批）** | 依据：用户裁决"同意。开工"（差异项四条默认通过＋远程连通法①为主/②辅、③不取）——`docs/pg36.md` §8 Step 0–7 执行完毕。**要点**：PG **18.6**（基座 digest `3725f4e2…`、PGDATA/Volumes 与 §1 核对 9 吻合）；镜像 `pg36-full:18-pgdg` 1.04GB＋label 四枚（21 包版本全表，差异项①兑现）；t=15s healthy、initdb 如预期；**Step 5 装后核查全过**（§4 参数全生效／UTF8·en_US.utf8／pg_hba 末行 scram／冒烟库建删净／pg_available_extensions 34 项全命中＋wal2json·decoderbufs .so 在位／远程连通：负向无密码被拒 `fe_sendauth`＋正向①自 32 借 pg32 客户端回 **18.6**（版本差异铁证应答者系 pg36）＋正向②容器经宿主 IP 自连=1／现有服务零影响（运行集未变、五路探针活、`ss -tln`＝勘察基线＋5432）／空闲 CPU 0.02%·MEM 84MiB／容器内外时刻一致／标准 §13 清单十二项✅＋端口登记项归另派 agent）；Step 6 交接素材已录 §13。**执行偏差二处（均有据）**：① 首建失败→按 §10 预案裁 `postgresql-18-address-standardizer`（PGDG 仓错配：其 3.7.0 `Breaks postgis-3-scripts(<<3.7)` 而 postgis-3 3.6.4 硬 Depends scripts——`apt -s` 双向实测证实不可同装）；续发现扩展本体随 postgis 族包已在镜像——`address_standardizer` 3.6.4 功能实测通过，**裁包未丢能力**（后补路径在 Dockerfile 头注）；② 首 `up -d` 失败→诊断宿主 36 `/etc/timezone` 系**空目录**（mtime 2026-07-29、早于开工、**非本次所致**，被运行中 speaches/weaviate 钉住当日不可修；32 同款路径正常）→**入 B-02**（先记不动手）＋呈报四选项→**用户裁决 B**：compose 去该行、保 TZ＋localtime（§11⑤）。**纪律自检**：密码明文零外落（`.env` 36 生成 36 用、输出仅键名长度）；36 改动仅方案边界内（现有容器/ollama/挂载/系统配置零触碰）；32 只读借客户端；开发机零改动；部署标准 §13 以只读令牌临时拉 /tmp 核毕即删（落盘纪律）。| commit `c8bc666`

- **2026-09-24 | 本批五文件（pg36/bugs/codemap/features/index）| 对齐核查（pg36 施工批收口）** | R-06 四项：冗余查（建成事实唯一展开于 pg36.md §13——codemap 新行＝事实摘要＋指针、F-13/index＝状态指针、B-02＝缺陷唯一账，无第二处展开）；矛盾查（活文档"执行待开工口令"字样全数前向替换——全库 grep 命中三处：codemap 树行已改、pg32-upgrade.md 系存照自注"原状态"、log.md 历史行不回改，均合规；§5/§6 草案正文未改写、偏差以原位前向注＋§13 指路）；锚点查（§11⑤↔§13↔B-02↔标准 §9 互指真实存在；README"版本记录"栏与镜像 label 同文、与 dpkg-query 实测输出一致；36/32 路径与 stat 证据吻合）；账目闭环（bugs✓ codemap✓ features✓ index✓ pg36✓ 本账✓；homelab 登记与备份＝另派 agent 职责、交接素材已录 §13；F-13 装数四问与数据通路仍待议，未擅自推进）。结论：**干净** | commit `c8bc666`

- **2026-09-23 | `docs/pg36.md`（开设）＋`docs/pg32-upgrade.md`（状态→搁置）＋`docs/index.md`（实施方案节改判＋阅读顺序行）＋`docs/codemap.md`（目录树两行＋主机 36 行指针）＋`docs/features.md`（F-13 状态行／②目标机改判＋新增"两实例格局与 pg36 裁决"条） | 开设＋修订（pg36 方案成文批）** | 依据：用户三连令——"以后，我会有两个pg实例在跑，其它一个专门备份用。现在你不用升级32的，就在36建立我们刚才说的新pg实例。名字就叫pg36。先别动手，我说开工在干。"＋"写之前你先登录gitea。看看这个文档（homelab 仓 docker-deploy-standard.md），看完后告诉我，可行不可行"＋"备份的部分不用你管。你只管安装是否符合要求，或者与我们的容器有冲突的地方，你就提出来。你只管安装与安装完成后的核查。其它的我安排别的agent去做。"**只读查证**（Gitea API/raw 以 git 凭据令牌读 `zky/homelab`：部署标准全文 552 行、backups/PLAN.md、docker/by-host/36-one.md；令牌权限实测 **pull=True/push=False**；36 上冲突排查一轮 ssh＋32 上 uid 核证——**两机零改动、未拉镜像、未落任何文件**）。**结论入账**：① 标准**可行且高度对齐**——pg32 即按此标准部署（标准 §0 已实例正是 08-10 的 PG18/PGDATA/shm_size 案）；pg36 方案逐条合规比对 22 项（§2），差异项 4 条列出待定夺（§11：扩展版本浮动折衷/自建镜像先例/command 注入标注/shm_size 128mb）；② **冲突排查零冲突**（5432/目录/容器名/镜像名/网络名全空闲、compose v5.3.1、NVMe 余 268G、GPU 零争用、uid 999 活体核证）；③ **职责划定**：本项目只管安装＋装后核查（Step 5 清单为交付物）；备份与 homelab 登记＝另派 agent（Step 6 备交接素材；push=False 故登记本就非我职责）；④ **前向改判**：前批"pg32 换镜像"路线被取代，pg32-upgrade.md 搁置存照，官方核对十项与 Dockerfile 迁 pg36.md（R-04 单一源）。R-06 四项：冗余查（方案唯一展开于 pg36.md，F-13/index/codemap 皆指针）、矛盾查（F-13 三处前向改判、阅读顺序行同步、搁置缘由照录用户原话）、锚点查（pg36.md↔pg32-upgrade.md↔F-13↔codemap↔index 互指核实；外部锚点 homelab 仓三文件路径实测可达）、账目闭环（新文档 index 登记✓ codemap 树✓ F-13✓ 本账✓） | commit `8045253`

- **2026-09-23 | `docs/codemap.md`（§2 新增"主机 36 实勘"行）＋`docs/features.md`（F-04 现状对照 ⚠未实测→✅已实测 前向补正） | 修订（主机 36 实勘批）** | 依据：用户令"我现在要你上 36 主机上看看"。**只读实勘**（两轮 ssh：系统/硬件/GPU/磁盘/挂载/docker/端口/服务/ollama 模型/数据卷/compose 落点；**未装、未改、未起停任何物**）。**要点**：① 36（主机名 `one`）＝**AI/GPU 主力机**——Debian 13／Ryzen 5 5600（12 线程）／15G RAM／**RTX 4070 12G（勘时闲置）**／NVMe 余 268G；**无 PostgreSQL**（与 F-13 之 32 主机方案无涉、不冲突）；② **F-04 关键销账**：ollama 中 `bge-m3:latest` 实测在列（8 周前入库）＋ **weaviate 1.38.8 向量库在线**（数据/备份卷位置已勘明）＋ dify-TEI embedding/reranker 容器在机（已停）——**向量层基础设施已半在位**，F-04 原"⚠ 本会话未实测"就此改判 ✅；③ NAS 四路挂载勘明（NFS workcenter＋CIFS×3）；④ 在线容器 speaches(:8000)／weaviate(:8085,:50051)／perplexica(:3000)／portainer-agent(:9001)，ollama 原生 :11434。R-06 四项：冗余查（主机全貌唯一展开于 codemap §2 新行，F-04 只留结论＋指针）、矛盾查（F-04 补正系前向改判非回改历史；与 N-01 纪要"bge-m3 在 36 已在线"相符、与 F-13"PG 在 32"分工不冲突）、锚点查（codemap 行名／F-04 指针互指核实）、账目闭环（codemap✓ features✓ 本账✓；index 无新文档不动） | commit `618e3c7`

- **2026-09-23 | `docs/pg32-upgrade.md`（开设）＋`docs/index.md`（新设"实施方案"节＋阅读顺序行）＋`docs/codemap.md`（目录树行）＋`docs/features.md`（F-13 状态行指针） | 开设（pg32 升级方案成文批）** | 依据：用户令"你再查一下官方资料，确定一下我们理解有没有错。然后制作 pg32 的升级方案"。**只读查证两轮**（curl：docker-library/postgres `18/bookworm/Dockerfile`、postgresql.org `upgrading`／`sql-createextension`／`pgstatstatements`、postgis.net `install`／`postgis_installation`、citusdata/pg_cron README、PGDG Packages 索引；**未拉镜像、未在 32 落任何文件、未改 compose、未动 pg32**）。**核对结果**：十项理解**全数 ✅**（详见方案 §1；两项新硬证——官方镜像 `ENV PGDATA /var/lib/postgresql/18/docker`＋`VOLUME /var/lib/postgresql` **与 pg32 现有挂载一致**；*"Minor releases never change the internal storage format and are always compatible with earlier and later minor releases"* ＝升级与**回退**双向皆安全）。**补正一处**：官方镜像把 PG 二进制钉在镜像构建时点小版本 → "拿到 18.6"靠升级当日重新 pull 基础镜像；届时若已 18.7 照走并如实记录。**方案要点**：自建 `pg32-full:18.6-pgdg`（`FROM postgres:18-bookworm`＋20 个 PGDG 包、扩展不钉版本）；preload 最小集仅 `pg_stat_statements`；扩展全装**零启用**；六步实施（预检→构建冒烟→三份备份→换镜像→五项验收→收尾记账）＋回滚三场景＋风险对策表。**执行待开工口令**（R-01）；CBDB／CHGIS 装数不在方案内（F-13 后续另议）。R-06 四项：冗余查（升级实施唯一展开于新文档，F-13 只留指针不复述）、矛盾查（与 F-13"单实例／只取官方源／钉大版本浮小版本"三口径一致）、锚点查（index 新节／codemap 树行／F-13 指针均指实文件实章节）、账目闭环（index 登记✓ codemap✓ features 指针✓ 本账✓） | commit `8794f74`

- **2026-09-23 | `docs/features.md`（F-13 全功能条追加"用户口径：只取官方源"及三项改判） | 修订（官方源口径批）** | 依据：用户令"肯定要官方支持的，不能搞一些第三方，不受官方推荐与支持的东西，到时候一升级全是问题"。**只读查证**（curl postgresql.org 的 Debian 下载页；**未拉镜像、未写 Dockerfile、未改 compose**）。**入账要点**：① **依赖来源只取官方源**——上列 20 项已全数出自 PGDG，官方页原话"This repository will integrate with your normal systems and patch management"为用户之虑的官方背书；② **三项改判**：(a) 镜像走**自建 Dockerfile**（Docker 官方 `postgres:18-bookworm` ＋ 仅 PGDG 包），而非 `postgis/postgis`（PostGIS 项目自建镜像，成分同源但非 Docker 官方库）；(b) 版本策略＝**钉 PG 大版本、扩展随 PGDG 浮动**（钉死小版本会因官方源清旧版而致日后构建失败）；(c) 升级动作＝重建镜像后逐库 `ALTER EXTENSION … UPDATE`，`shared_preload_libraries` 保持最小（预加载库缺失将致 PG 起不来）；③ 源内无者一律不装，用替代路线。**纪律位说明**：本条具项目级纪律性质，但 `rules.md` 载明 R-07 起待项目定型后由用户口令开设，故**只入 F-13、未擅开 R-07**（AGENTS §3 冲突报告不猜测）。R-06 四项：冗余查（并入 F-13 全功能条，不另开条目）、矛盾查（本批修正前批"落地方式"中"钉版本"表述——原文泛指钉版本，现明确为钉大版本、扩展浮动）、锚点查（官方页引文与 curl 回文一致）、账目闭环（features✓ 本账✓；rules.md 未动已说明理由） | commit `835f2b9`

- **2026-09-23 | `docs/features.md`（F-13 新增"全功能候选清单核证"条） | 开设（全功能扩展清单批）** | 依据：用户令"现在 postgreSQL 不是说能干很多事吗？这次最好装个全功能的"。**只读查证**（curl PGDG bookworm-pgdg 的 Packages 索引，按 6 大方向逐个核 PG18 包名与版本；**未拉镜像、未写 Dockerfile、未改 compose**）。**入账要点**：① 概念先立——PG"能干很多事"＝**精简核心＋扩展生态**，核心自带 JSONB／全文检索／分区／并行／逻辑复制／FDW 框架／多样索引，无需安装；② **官方源现成可用 20 项**（postgis 3.6.4 含 raster/topology/sfcgal、pgrouting 4.0.1、h3 4.2.3、address-standardizer 3.7.0、pgvector 0.8.6、rum 1.3.15、pg_ivm 1.15、hypopg 1.4.3、pg_cron 1.6.8、pg_repack 1.5.3、pg_partman 5.5.0、pgaudit 18.0、wal2json 2.6、pg_stat_kcache、mysql_fdw、plpython3u、pgtap、pglogical、decoderbufs、pointcloud）；contrib 一族**已含于主包**（实测 pg32 现有 btree_gin／btree_gist／hstore／pg_trgm／unaccent 即为证）；③ **官方源内无**：`pg_bigm`／`zhparser`／`pgroonga`（中文分词三项）、`sqlite-fdw`、`mongo-fdw`、`plv8`、`vectorchord`、`pg_search`、TimescaleDB／Citus 系——**勿顺手装**；中文分词替代＝应用层分词后存 `tsvector` 走核心 FTS（文本检索另有 F-04／F-08 规划）；④ 代价三条（镜像变大／预加载类需改 `shared_preload_libraries` 并重启／升级须重建镜像），装而不用几乎不占运行资源；⑤ 落地＝自建 Dockerfile 钉版本置于 `/opt/mydocker/postgres/`（可重建可回滚）。R-06 四项：冗余查（并入 F-13，与合并路线核证条互补不重复）、矛盾查（与"单实例优先"口径一致——扩展装在同一个实例上）、锚点查（包名与版本逐一对应 PGDG 索引回文；"源内无"亦为实测）、账目闭环（features✓ 本账✓） | commit `271ff3d`

- **2026-09-23 | `docs/features.md`（F-13 合并路线核证条追加"用户口径：单实例优先"） | 修订（单实例口径批）** | 依据：用户令"postgreSQL 还应付不了这点东西吗？没必要建立这么多实例，那更费资源"。**本批零动作**（未拉镜像、未改 compose、未动 pg32；纯口径入账）。**入账要点**：① **单实例优先**——数据量（CBDB 5.62M 行／~600MB，CHGIS 点十万级、面万级）对 PG 属"小库"，**不存在扛不住**；多实例＝多份 shared_buffers／WAL／后台进程＋多一份备份升级维护，白费资源；② **路线收敛**：pg32 换上含 PostGIS 的镜像 → 建 `cbdb` 库 → CBDB 与 CHGIS 同库直连 join（无 fdw、无跨库拼、端口与连法不变）；"另起 PostGIS 容器"**退为备选**（仅不愿动 pg32 时考虑）——此为用户裁决，改判前批准我此前"另起容器最省心"的说法；③ 澄清：**单实例≠单库**（同实例多库是 PG 常规用法）；④ 真正成本在**空间索引（几何列须自建 GiST）与 work_mem（默认 4MB 偏小，可会话级调）**，不在数据量。R-06 四项：冗余查（并入 F-13 合并路线核证条，不另开条目）、矛盾查（**本批即修正**前批"取舍"表述中隐含的双实例倾向，以用户口径为准；与"PostGIS 可得性"段不冲突）、锚点查（体量与 §四／§九 实测一致）、账目闭环（features✓ 本账✓） | commit `3d073c9`

- **2026-09-23 | `docs/features.md`（F-13 新增"合并路线核证"条＋修复前批一处排版断裂） | 修订（合并路线核证批）** | 依据：用户提案"那我把 pg32 升级的同时，重做镜像，把 PostGIS 放进去不就可以了吗？"。**只读查证**（GitHub API 取 `postgis/docker-postgis` 目录与 `18-3.6/Dockerfile` 全文；**未拉镜像、未改 compose、未动 pg32**）。**核证结论**：路线**成立**且优于"运行中容器内 apt 装"（后者随 compose 重建即失效）。实测：`postgis/postgis:18-3.6`＝`FROM docker.io/postgres:18-trixie` ＋ `apt-get install postgresql-18-postgis-3=3.6.4+dfsg-2.pgdg13+1`，即"官方 PG 镜像＋PostGIS"一体件；欲保持 bookworm 或另捎扩展则自建 `FROM postgres:18-bookworm`＋pgdg12 版 postgis。**四个必知前提入账**：(a) 装包≠启用（按库 `CREATE EXTENSION postgis`；initdb 钩子仅空目录首次初始化触发，已有数据不自动启用）；(b) PGDATA／挂载路径须一致，否则**误初始化空集群**（施工前 `docker image inspect` 核 `Env.PGDATA`／`Volumes`）；(c) bookworm→trixie 不影响数据目录；(d) 先备份（bind mount 仅 ~395MB）＋留停机窗口（重建＝库停数十秒）。附带收益：18.4→18.6 同步完成。取舍：单实例 join 最顺 vs 与现有服务共享 4 核／7G。**排版修复**：前批插入"合并路线核证"时误吞"实勘 32 主机"小标题致该条与前行并线，本批补回标题并理正一处标点（"）。，"→"）；"），属本会话自造瑕疵之更正，非改历史结论。R-06 四项：冗余查（并入 F-13，与实勘行／PostGIS 段不重复）、矛盾查（与"PostGIS 可得性"及"另起容器"三途并存不冲突——本提案即路径①的正当做法）、锚点查（Dockerfile 基镜像／包版本与实测回文一致）、账目闭环（features✓ 本账✓） | commit `841b24e`

- **2026-09-23 | `docs/features.md`（F-13 实勘行补"18.4 系当时最新／现落后两小版本"） | 修订（版本时点补记）** | 依据：用户问"我装的不是官方最新版本吗？"。**只读取证**（32 上 `docker inspect pg32` 取容器创建时间；Docker Hub API 取 `library/postgres` 的 18.x 标签更新时间；**未拉镜像、未重建容器、未动数据**）。**结论**：容器创建 **2026-08-10T07:28Z**，而 Hub `18.4-bookworm` 更新于 **2026-08-05**、`18.4` 于 08-06 ⇒ **用户当时装的正是官方最新**；Hub 浮动标签 `18-bookworm` 已于 **2026-09-19** 重建指向 **18.6**，容器不自跟进而停在 18.4（**落后两个小版本**）。概念澄清入账：**"最新"有三层**——上游/PGDG 现为 18.6、Hub 浮动标签现指 18.6、手上快照为 18.4。R-06 四项：冗余查（并入 F-13 实勘行，不另开条目）、矛盾查（修正"即 18.4"之表述为"当时最新、现落后"，非改写历史）、锚点查（容器创建时刻／标签更新时间与 API 回文一致）、账目闭环（features✓ 本账✓） | commit `eb19b50`

- **2026-09-23 | `docs/features.md`（F-13 PostGIS 段追加"镜像实测 `postgis/postgis:18-3.6`"） | 修订（镜像可得性补记）** | 依据：用户问"（另起 PostGIS 容器那条）这个是什么意思？"——属解释性追问，本批只补一条实测事实供其理解。**只读查证**：Docker Hub API 取 `postgis/postgis` 标签表，实测含 **`18-3.6`**（PG18＋PostGIS 3.6，与现 pg32 同版本线）及 `18-3.6-alpine`、`19beta1-*`、`17-*` 等；**未拉镜像、未起容器、未改 32 任何物**。**记账口径**：该标签存在 ⇒"另起 PostGIS 容器"一路**无需版本折衷**，决策依据更实；仍在 R-01 边界内（讨论期事实补记，非开工）。R-06 四项：冗余查（并入 F-13 PostGIS 段，不另开条目）、矛盾查（与"另起容器"三途之一一致）、锚点查（标签名与 Docker Hub 回文一致）、账目闭环（features✓ 本账✓） | commit `2d78ac2`

- **2026-09-23 | `docs/features.md`（F-13 实勘行追加"PostGIS 可得性查证"） | 修订（PostGIS 可得性查证批）** | 依据：用户问"地图肯定要用的啊。你的意思pg少扩展？"。**只读查证**（32 上 `docker images`／容器内扩展目录／容器 apt 源；本机 curl PGDG 索引；**未装任何包、未起停容器、未改配置**）。**结论**：① 现实例确无 PostGIS——容器内 `postgresql/18/extension/` 无 postgis 文件、32 无 postgis 镜像，故几何类型装不上；② **但可装**：PGDG 源有 `postgresql-18-postgis-3` **3.6.4**（及配 PG17 同款），pg32 容器内 `/etc/apt/sources.list.d/pgdg.list` 在位；③ 辨析：宿主 apt 的 `postgis 3.3.2` 系 Debian bookworm **PG15** 包，与 PG18 无关（用户"pg少扩展"之疑在此澄清——**PG 本体不缺扩展，缺的是 PostGIS 这套附加件**）；④ 三途入档（补装现实例／另起 PostGIS 容器并存／不用 PostGIS 走外部引擎）＋CHGIS 学术用禁商用禁再分发红线复述。R-06 四项：冗余查（PostGIS 明细唯一在 F-13 实勘行）、矛盾查（与既记"可用扩展无 PostGIS"一致递进，非改写）、锚点查（包名／版本 3.6.4／pgdg.list 与实测输出对应）、账目闭环（features✓ 本账✓；未动 F-09 CHGIS 条目——启用属另一桩） | commit `2965a4d`

- **2026-09-23 | `docs/features.md`（F-13 ② 目标机项改记"已实勘确认"＋新增"实勘 32 主机"行） | 修订（32 主机实勘批）** | 依据：用户令"是啊。你上去看看啊。"——**授权只读实勘**。**全程只读**（ssh 上去后仅 `docker ps/inspect`、容器内 `psql` 查 `version/show/pg_database/pg_available_extensions/pg_hba`、`df`／`du`／`mount`／读 compose；**未起停容器、未建库、未装扩展、未改任何配置或数据**）。**实勘结论**：32＝`lx03`（Debian 12／4 核／7G）；PG＝**容器 `pg32`／镜像 `postgres:18-bookworm`／版本 18.4**，5432 发布至 0.0.0.0，数据实体 `/opt/mydocker/postgres/data/postgres`（395M，宿主 /dev/sdb1 余 196G）；默认参数 128MB shared_buffers／4MB work_mem／100 连接；UTF8、collate `en_US.utf8`；**可用扩展无 PostGIS**（仅 plpgsql 已装）；现有库 nbdc／n8n／chainlit 等，**无 CBDB/CHGIS 数据**；**32 自挂 NAS**（`//192.168.3.61/workmetadata`→`/mnt/wd61workmetadata`，cifs rw）→ 源件可就地读，无需经本机搬运；同机多 live 容器共用该 PG 实例。**记账口径**：本条把"32 上有 PG"由"未实测线索"升为"已实勘事实"；措辞仍守 R-01——**实勘≠开工**，入库四问照旧不在此提。R-06 四项：冗余查（实勘明细唯一在 F-13，codemap 未改）、矛盾查（与前批"未实测"标注一致递进，非改写；与 AGENTS §1 不冲突）、锚点查（容器名／镜像／版本／参数／库名与实勘输出逐一对应）、账目闭环（features✓ 本账✓） | commit `635b61c`

- **2026-09-23 | `docs/features.md`（F-13 ② 目标机项补记"32 主机上已有 PostgreSQL"＋原"待用户裁决四事"措辞校正为"届时需定之事（现在不议）"） | 修订（讨论期事实补记）** | 依据：用户于讨论中告知"32主机上有pg"。**本批纯补记，零动作**：未探测 32 主机、未装库、未建库、未导入（用户明示"还没让你动手……只是讨论，我说开工的时候你再干这些活"——R-01 口径重申）。措辞校正：删去催促式"待用户裁决四事"，改为**届时需定之事**，四问不在现在提。事实线索：32 主机已有 PostgreSQL（⚠ **未实测**；与既有的"36 主机 bge-m3 已在线"同属按编号称机惯例，F-04 在册）。R-06 四项：冗余查（仅 F-13 一处记，不另开条目）、矛盾查（与 AGENTS §1"目标未定义前不做开发动作"、R-01 一致——本批只记账）、锚点查（32 主机为新增未实测线索故标 ⚠；编号未与 36 混淆）、账目闭环（features✓ 本账✓） | commit `39c0ae7`

- **2026-09-23 | `docs/features.md`（新立 F-13"CBDB（及 CHGIS）入 PostgreSQL 数据底座"） | 开设（PG 入库准备批）** | 依据：用户令"让你查数据库的原因，就是为入PostgreSQL做准备"——**首次点明本数据线用途**。按 R-02 只入账不开发（"现在就做"须另令），故本批**零环境改动**（未装 PostgreSQL、未建库、未导入）。**入账内容**：诉求原话照录＋能力性质（新能力·数据底座）＋标尺归属 `待判`（AGENTS §1 项目核心定位未定型）＋现状对照（数据在手：0919 件 586MB／78 表／5,623,075 行；环境未备：Debian 13 trixie／WSL2，apt 可装 PostgreSQL **17**，`psql`／`pgloader`／`ogr2ogr`／`docker` 全无，`mdbtools` 已装，本地盘余 953G）＋**待裁决四事**（入库范围／目标机／保真策略／索引与外键策略）＋派生件落盘位置待指定。**关键判定**：索引缺项（370→0）在 PG 路径**不阻塞**（PG 侧另建索引，见 `docs/cbdb.md` §9.4(1) 路径 A）。R-06 四项：冗余查（F-13 为唯一展开，`cbdb.md` §九 只作数据边界指针，不重复方案）、矛盾查（与 AGENTS §1"目标未定义前不做开发动作"一致——本批只记账）、锚点查（F-13 编号接 F-12 未复用；环境与体量数字与 §四／§九一致）、账目闭环（features✓ 本账✓；bugs／improvements 零涉） | commit `a8a11ef`

- **2026-09-23 | `docs/cbdb.md`（§9.4(1) 追加"成因＋处理路径"／§9.5 索引行细化／§五 4 增后处理仓指针）＋ NAS 仓 `README_provenance.md`（④ 索引项补成因与后处理说明） | 修订（索引成因与处理管线查证批）** | 依据：用户令"HuggingFace 快车道版把索引全丢了。这个是什么情况，你到网上查查资料。这个怎么处理？"。**全程只读查证**（读本地两份 sqlite ＋ 上游 `github.com/cbdb-project/cbdb_sqlite` 仓／脚本／issue 原文；**未跑任何脚本、未建索引、未改数据文件**）。**上游查证结论**：① 索引非"被删"，系**两代管线不同**——370 条来自 Access 转换血统（索引名 `*_PrimaryKey`／`*_Belongs`／`*_ZZZ_*`、以唯一索引代主键＝转换器特征），HF 快车道件系活库直出、只带原生主键；② 上游 issue #17（2026-02-09 关闭）已取消"注入主键"流程，**二级索引本就不在新管线产出内**；③ **SQLite 不为外键列自动建索引**。**处理路径三条（未执行）**：A 入库 PG（索引改造为 PG 索引；PG 亦不自动为 FK 列建索引）／B 直接查 SQLite（**本地副本**跑官方 `add_foreign_keys.py`(36 表) ＋ `create_views.sh`(18 视图) ＋ `create_addresses_table.py`(ADDRESSES)，索引自建——本批实测官方 **370 条 DDL 中 307 条可直接套用**本件 schema，47 条引用的表本件没有、16 条列名漂移，合计 370）／C 提上游 issue（待口令）。**落点（R-04）**：明细唯一在 `docs/cbdb.md` §9.4(1)，§五 只留仓指针；NAS 出生纸同步，codemap 无需改（③ 已为状态＋指针）。**与前批关系**：前批（对质结案批）已录"370→0"事实与性能代价（相关子查询 60s 未出 vs 改写 0.07s），本批补其**成因与处理**，不冲突不重复。**三本专职账零涉**：用户未下令补索引，未立项；真要做按 R-02 挂 F/I 账待口令（且派生件落盘位置须用户指定，联动落盘纪律）。R-06 四项：冗余查（§9.4(1) 唯一展开，§五／§9.5／README 三处指针）、矛盾查（全库复验：存活正文无"0.79／3.13"表述，仅存于 §9.1 作废块与历史 log 行，符合 R-04）、锚点查（issue 编号／脚本名／307＋47＋16＝370 与实测吻合）、账目闭环（cbdb✓ 本账✓；codemap 不改） | commit `ee6dc61`

- **2026-09-23 | `docs/cbdb.md`（§九 9.1 判词作废重写＋新增 9.4"结构缺项与质量坑"／§四·§五·§八 三处前向补正）＋ `docs/codemap.md`（哈佛仓行 ③ 挂查项结案改写）＋ `docs/index.md`（cbdb.md 条目 §九 说明同步）＋ NAS 仓 `README_provenance.md`（"待查口径"与"硬缺口"两条同步结案，消矛盾查残留） | 修订（0919 件对质结案批）** | 依据：用户令"分析一下这个数据库，把情况给我汇报一下"。**只读体检＋解包对质，全程未改数据本体**。核心结论：① **§九 9.1 前批判词作废**——前批"本件每任职至多 0.79 条地点链接、官方 ≈3.13 条，故任职地点不可单用"**系跨制式算错**（拿官方 `POSTED_TO_ADDR_DATA` 行数 ÷ *本地* `POSTING_DATA` 行数）；本轮解包官方 2025-05 Access 件（5 卷 mdb，解包 5.0GB）实测其自身分母 = 1,852,415 任职、1,853,262 行，**每任职 1.000 条**，与本地件同；成因为 **`c_textid=67147`《清代量化数据库(縉紳錄)》2024-08 入档制式**——Access 存逐年名录（1,581,438 行＝其仕历表 79.9%，一行＝一人一职一年），本地 SQLite 归并为任期区间（同批 197,232 行／103,769 人）；实例陳國贊(669624) 318 行→1 行(1864–1909)，蘇軾(3767) 官方 36 条＝18 地点 vs 本地 34 条＝18 地点，**地点信息不丢**。**两制式不可按行数互校**。② 新增实测入档：真实地点覆盖率 **65.2%**（78,921 行 `c_addr_id=0` `[未詳]` 哨兵，前批 78.6% 系含哨兵毛数）／**显式索引 370→0**（2024-02 版 370 条，本地件 0 条，关联子查询实测 60s 未出 vs 改写 0.07s）／表集合 −21＋9／`0` 哨兵四字段／越界脏值／同名 77,790 组／坐标污染按 `addr_id`（徐州 11242、登州 11167 已污染宋人记录）／参考完整性全清。③ **落点判定（R-04）**：明细唯一全量展开于 `docs/cbdb.md` §九，codemap ③ 与 §八 只留状态＋指针；`ADDR_XY` 勘正（非 MDB 独有，2024-02 sqlite 亦有）同收 §九 9.2。**三本专职账零涉**：用户未提补数或建索引需求，未立项；后续若"补索引／补 ADDR_XY／取 ZZZ"按 R-02 挂 F/I 账待口令。R-06 四项：冗余查（§九 9.1 单一展开，codemap／§八／index 三处指针一致）、矛盾查（**本批即修正前批矛盾两处**：3.13 算法错、ADDR_XY 措辞不全；§四 口径纪律同步前向补正）、锚点查（index §九 说明、codemap ③、§八 指针、§九 9.1–9.6 互指一致）、账目闭环（cbdb✓ codemap✓ index✓ 本账✓）。临时件处置：用户令"两个都删"——`/tmp/cbdb_ro.db`（586MB）与 `/tmp/cbdb_mdb/`（解包 4.7GB）已清，原始件仍在 NAS 可复拉 | commit `79c9bb4`

- **2026-09-23 | `docs/cbdb.md`（新增 §九"本地 0919 件缺项与使用边界"＋§八 重复句收敛为指针）＋ `docs/codemap.md`（③ 挂查项由明细收敛为状态＋指针）＋ `docs/index.md`（cbdb.md 条目补 §九 说明）＋ NAS 仓 `README_provenance.md`（随货同步缺项实测） | 修订（缺项落档批）** | 依据：用户令"硬缺口与两个小缺项，记录到文档里"。**落点判定**：缺项属"本地件使用边界"，按 R-04 择一处全量展开——全量落 `docs/cbdb.md` §九（对比表／影响判定／补数路径／状态汇总／一句话边界），codemap ③ 与 cbdb.md §八 由明细收敛为**状态＋指针**（冗余查：同一事实不再两处展开；上批 log 行所述"补入 codemap ③"由本行前向取代，旧行不回改）。**三本专职账零涉**：用户未提"补数"需求，未立项；后续若要补官方件或查口径，按 R-02 挂 F/I 账待口令。实测依据同上批（sha256 全同、5,623,075 行、0.79 vs 3.13 条每任职、78.6% 覆盖、126,805 条零链接、无 `ADDR_XY`／ZZZ）。R-06 四项：冗余查（明细唯一在 cbdb.md §九，codemap／§八 双指针）、矛盾查（与三次修正定骨及体检测量批不冲突）、锚点查（§九、§八、index 条目、codemap ③ 四处互指一致）、账目闭环（index✓ codemap✓ 本账✓；NAS 出生纸同步） | commit `c6b2fc4`

- **2026-09-23 | `docs/codemap.md`（哈佛仓行 ③ 挂查项追加实测补证） | 修订（CBDB 入库体检测量批）** | 依据：用户问"导入 PostgreSQL 得多长时间""用家中 0919 件就可以吧"。**只读体检**（未装库、未导入、未改动数据本体）：sha256 与官方 `latest.json` 记载逐字节全同（`bde1bb8e…`，本轮复验）；全库 78 表/736 字段/76 索引/**5,623,075 行**；提取基准 368k 行/s（全库文本化 840MiB／15.3s）→ COPY 端推估 37–112 秒，端到端（含装 PG、建库、建索引）15–30 分钟；NAS 读 115MB/s（库应建本地盘，本机余 953G）；`POSTED_TO_ADDR_DATA` **结构性**差异实测（0.79 vs 3.13 条/任职、78.6% 覆盖、126,805 条零链接）与 `ADDR_XY`/ZZZ 缺项一并补入 codemap ③；家中件自带 `ADDR_CODES.CHGIS_PT_ID` 桥（与 chgis-v6 可直配）。R-06 四项：冗余查（时延与缺项事实入 codemap 挂查项，`docs/cbdb.md` 不重复展开）、矛盾查（与三次修正定骨不冲突，属细化）、锚点查（sha256／表数／行数与实测一致）、账目闭环（codemap✓ 本账✓；三本专职账零涉——**PG 入库未立项**，待用户口令再按 R-02 挂 F 账） | commit `b6caacf`

- **2026-09-23 | 新立 `docs/cbdb.md` ＋ `docs/index.md`（新开"外部数据源档案"节＋阅读顺序延伸）＋ `docs/codemap.md`（§1.2 目录树增行）＋ `rules.md`（R-04 单一源清单增行） | 开设（外部数据源档案批）** | 依据：用户令"这个介绍记录到一个文档里"。**落点判定**：`memos/memos.md` 定性为"用户交录纪要的唯一照录存档处（不改写/不评注）"，本轮 CBDB 介绍系助手调查产物，不入 memos；按 R-04 新立单一源 `docs/cbdb.md`（项目身份／沿革／规模口径／数据模型／获取通道／外部评测），同批四文件登记（index 新节＋阅读顺序、codemap 树、R-04 清单）。**内容证据均为 2026-09-23 实测**：Dataverse 社区 API 回文、PAGGQS DataCite 导出、HF API／卡片／`history/` 实测、RDH 2026-07 评审全文（Wenyi Shang，含 Peter K. Bol 撰项目综述）、cssn 2024 李斌文、Wikipedia 摘要；官方站 Akamai 403 限制与"官方人数口径 ≠ 本地表行数口径"纪律一并入档；`POSTED_TO_ADDR_DATA` 遗留疑点以指针联动 codemap。R-06 四项：冗余查（项目身份事实仅此一处展开，本地落点全指 codemap 不重复）、矛盾查（规模按时点分列无冲突）、锚点查（index／codemap 树／R-04 清单三处登记与实体文件一致）、账目闭环（index✓ codemap✓ R-04✓ 本账✓；三本专职账零涉） | commit `e9db270`

- **2026-09-23 | NAS 仓 `README_provenance.md`（新旧关系节追加复核批补记＋外部背书定骨两行）＋ 本账一行 | 修订（CBDB 复核批·直连补齐）** | 依据：用户告外网已通、令"再去看一次"。HF 直连实证全闭环：org 页题"cbdb (China Biographical Database Project (CBDB))"、官方外链＝projects.iq.harvard.edu/cbdb；卡 CC BY-NC-SA、开设 2024-04-16、lastModified 2026-09-19T19:16:54Z（构建→上传间隔一分钟）；仓根 latest.json 与家中 JSON 逐字节全同（385B）；history/ 实测 2026-02→09 逐月一档（缺 04）、此前 2019→2025 零散八档——官方"weekly"系线上库节奏、HF 月度出档，前批勘注就此落实；另见 latest_ZZZ_tables.7z（215M，ZZZ 宽表快车道亦有单独包）。第三方全文核讫：RDH 2026-07 同行评审项目评审（Shang Wenyi @ Missouri；Directors＝Peter Bol/Hongsu Wang@Harvard、官网 cbdb.hsites.harvard.edu、657,909 人@2026-05、2005 起三机构联合；判词"数字人文基础平台"＋最大挑战＝结构化与史料偏向张力）、Wikipedia 条目摘要（口径滞于 2015-04 约 36 万，仅存目）——上条"第三方仅线索未获全文"状态由本条取代（历史行不回改，前批所指 pubpub/kci/huggingface/wikipedia DNS 阻断今已解除）。codemap 零改动（结论未变、证据升级）；NAS 写经提权一次批准（上批拒批先例在册） | commit `953a0a5`

- **2026-09-23 | NAS 仓 `README_provenance.md`（新旧关系节追加"佐证补强"一行）＋ 本账一行 | 修订（CBDB 官方出身复核批）** | 依据：用户令"cbdb 到底是不是哈佛官方发布的？去官方查，也看第三方"。网络实证两重（均取 dataverse.harvard.edu 官方接口回文）：① cbdb 社区 affiliation＝IQSS、联系人 hongsuwang@fas.harvard.edu、主题站链回哈佛 CBDB 官网；② PAGGQS DataCite 导出（v8.0，creator Wang, Hongsu @ Harvard University、producer "China Biographical Database project"）摘要**原文点名 HF 仓为每周更新 SQLite 官方下载源**——与当日补课批 `dd8daf3` 的 sha256 逐字节全同合为独立证据链闭环：**家中 CBDB＝哈佛官方发布，定骨**。小勘注：官方口径 weekly、账上"月度滚动"系 history/ 节奏措辞，并存不矛盾。**自查**：前轮答用户时误引已作废的二次修正措辞（"线上库自导出/非官方打包"）作答，本轮起一律以三次修正为准；codemap 零改动（结论未变、仅证据增强）。第三方线索随报：中国社会科学网 2024-10-14 李斌文（CBDB 作结构化知识库正面典型＋概念层短板批评，全文取讫）；RDH v7n7/v2n11 评审 PDF、KCI 韩文介绍文、JHNR 引文——三条仅存链接（本会话沙箱 DNS 阻断 pubpub/kci/huggingface/wikipedia 直连，未获全文，如实入账不作证实引用）。写 NAS 仓文件经 workspace-write 一次拒批、提权一次批准 | commit `5d915df`

- **2026-09-23 | `docs/codemap.md`（哈佛仓行对账段三次修正）＋ NAS 仓 `README_provenance.md` 补新旧关系节 | 修订（官方自述补课批）** | 依据：用户痛斥"下的时候没看官方说明？去官方查！"——**认账：前几轮全部靠行数实测推断，没先读货自带的官方文档，绕了远路**。补课结果：① 家中 `cbdb_20260919.sqlite3` 的出处证物（JSON）就在它旁边——**CBDB 官方 HuggingFace 仓 `cbdb/cbdb-sqlite`（org 名"China Biographical Database Project (CBDB)"、档案链回哈佛官网、CC BY-NC-SA）的最新滚动构建，latest.json 逐字节匹配＋sha256 实算全同**；前判"线上库自导出件"作废；② CBDB 官方两速：HF 快车道（月度）vs Dataverse 档案道（bi2025 比家中旧一年）；③ MDB 说明书（HelpFiles 内中英文 User's Guide，pdftotext 读讫）证实多卷 DATA 制式与 Relink 更新机制；④ 遗留疑点：POSTED_TO_ADDR_DATA 两官方制式 46.5 万 vs 185.3 万（mdb-count 复核非假象），启用前查清口径；⑤ CHGIS 官方 README/EULA 原文家中 VERIFICATION.md 早有摘录、本次新仓同款再证。上条（`15fcf64`）内"仕宦地点分析必以官方件补"措辞随 ④ 降为待查，不再作为判词 | commit `dd8daf3`

- **2026-09-23 | `docs/codemap.md`（哈佛仓行对账段二次修正定稿）| 修订（新旧关系实测定骨批）** | 依据：用户问"旧的不是哈佛官方吗？新旧什么关系"。三实测：① 旧 CHGIS 证实官方同源——家中 V6 点表两件与 harvard-full 同款 **MD5 字节全同**（976b2a07/f77d218b），严格子集；② 开包 2025 官方 MDB（新装 mdbtools 可读，DATA1 抽验）逐表计数：家中件人/锚/亲/社/仕五线微强，**唯 POSTED_TO_ADDR_DATA 官方 185.3 万 vs 家中 46.5 万（4 倍）**——CBDB 官方近两年补完任地链接未入线上导出；③ 诗库二件系社区产物与哈佛无干、本批零增量。**判词修正：首轮"家中 sqlite 单库最强"作废，改"同源两式互补短板，仕宦地点分析必以官方件补"**。验毕临时 MDB 已清（落盘纪律）。R-06：features 扫"最强"字样无残留 | commit `15fcf64`

- **2026-09-23 | `docs/codemap.md`（挂载行转正＋哈佛仓行构成实账）＋ `docs/features.md`（路径两处替换）＋ `memos/memos.md`（N-01 §一 前向勘正注）＋ 系统层：`/mnt/wd` 软链删除、`ops/harvard/*.py` DEST 改新路径（git 外）| 改道（路径转正＋构成实账批）** | 依据：用户令"以后不用 wd，用 wd61workmetadata"+"必须搞清楚 10G 与几百兆的区别"。路径侧：唯一正路径转正、软链删、活账全换、历史账（N-01 正文/log 旧行）不回改仅前向注、复跑脚本 DEST 改齐、新路径三验（可读/可写/内容全）。体量侧：按数据集标题六分实测——CBDB 历年发布 3.51G（九版堆叠，净新增仅 2025 快照）、栅格地图 2.72G（真新增）、CHGIS 历代向量 1.93G（V2–V5 旧版）、LoGaRT 软件 1.86G、Hartwell 等 0.19G、杂集 0.43G；**10.64G 中真正的净新知识约 3.5G**。R-06 自查两处当场纠：构成第一次口算分桶错（已按脚本实测数修正）；脚本 DEST 漂移隐患（已改）。translator MCP 用户口令复开，无涉 | commit `15fcf64`

- **2026-09-23 | `docs/codemap.md`（哈佛仓行货单段改写）| 修订（出生纸批）** | 依据：用户追问"下的文件可记录否"。清点答：三层记录俱在（逐件凭据/树货单/拉取流水）；自查瑕疵一处——manifest 标题字段抓取不全显 `?`。补齐两件并**随货入 NAS 仓**：`README_provenance.md`（出生纸：来源、验证法、复拉方法、许可、瑕疵注）＋`datasets.tsv`（65 数据集总账：DOI/树/件数/字节/**标题**/目录，标题经 search API 补全 0 缺）。codemap 该行货单段同步四件实账。落盘纪律自查：数据本体仅存用户裁定仓，工作区 `ops/harvard/` 只存元数据与脚本，合规。另：translator MCP 经用户口令关→复开，无涉本批 | commit `9a529ec`

- **2026-09-23 | `docs/codemap.md`（哈佛仓行勘正重写）| 修订（哈佛货不对账勘正批）** | 依据：用户问"前后为什么差这么多"，开包实测（p7zip 新装）。三发现：① `.7z` 305M 系压缩壳，解包 5.01GB（MDB 为主）；② 官方 2024-02 SQLite 各表行数**全面低于**家中 `cbdb_20260919.sqlite3`（人物 53.5万 vs 66.2万等四项）——N-01 之"子集"判名不准，旧货实为线上库级全量、家中单库仍最强（N-01 照录原文不回改，勘正寄于本行与 codemap）；③ 官方件夹带 ACCESS 残表 21 张，但补 `ADDR_XY`（今地坐标）、`DATABASE_LINK_*`、`CBDB_NAME_LIST` 等旧货所无实表。harvard-full 内 CBDB 件定位改为：官方快照＋版本序列＋说明书。`/tmp/cbdb_ro.db` 反成最优工作副本——处置请示依旧悬着 | commit `8a68743`

- **2026-09-23 | NAS 新仓 `/mnt/wd/harvard-full/`（370 件/10.64 GiB/65 数据集，含随货 `harvard_manifest.txt`＋`harvard_dl.tsv`）＋ `docs/codemap.md`（"哈佛全量仓"行）＋ `docs/features.md`（F-09～F-12 开批＋F-08 进展注）＋ `.gitignore`（`ops/` 节）| 开设（哈佛全量采购批）** | 依据：用户令"把哈佛的货全部拿过来，不能少"。路线：官方站 Akamai 拒机器→转 Harvard Dataverse API（UA 过 contents 门，逐数据集 versions 列件）→ 4 路并发拉（约 11MB/s，断点续传＋尺寸核对＋MD5）；.tab 六件因 original/converted 双制式卡尺寸，定点换制式补拉，终 **370/370 尺寸全吻合**；.part 残片清零。过程坑三条入记忆：沙箱内外 `/tmp` 视图不通（脚本改驻 ops/）、改 NAS ACL 须重挂（前批已记）、Dataverse 清单尺寸＝converted 制式。本批未逐件解压验内容（静态入仓），启用时按 N-01 验单规矩逐件核；许可红线（CHGIS 禁商用/再分发）已注 codemap。R-06 本轮：codemap 行数实测勘正 62→65；features 活指针同步；ops/ 归节说明。另：`/tmp/cbdb_ro.db` 旧副本删除请示**仍悬** | commit `d917619`

- **2026-09-23 | `docs/codemap.md`（"数据 NAS 挂载"行状态更新）| 改道（写通道激活批）** | 依据：用户于 NAS 端给 admin 开写（B 案）后口令复测。实测坑一条：**服务端改 ACL 后旧 cifs 树连接缓存旧权限，须 umount/mount 重挂生效**；重挂后 root/zky/子目录三写探针全通、探针净清理。上条"待裁决"就此闭环（旧行不回改，本行为指）；guest 可写事实保留为历史记录，不再作为方案选项 | commit `2b69d8e`

- **2026-09-23 | `/etc/fstab`（系统文件，用户亲给定条目）＋ 挂载 `/mnt/wd61workmetadata` ＋ `/mnt/wd` 空壳目录转软链 ＋ `docs/codemap.md`（"数据 NAS 挂载"行）| 开设（NAS 常驻挂载批）** | 依据：用户令立 fstab 常驻并给定 cifs 条目（尾随粘贴引号已剔）。实测：挂载 rw/SMB3.1.1、四库读取通（含 560M CBDB 副本）；**权限反常识发现**：条目内 admin 凭据服务端**只读**（root/zky 写皆拒），guest 匿名**可读写**（探针证实，合 N-01 旧语义）——写通道走 guest 还是 NAS 端修权限，**待用户裁决**；WSL 无 systemd，`x-systemd.*` 选项休眠。另悬处置：`/tmp/cbdb_ro.db`（586M 实验副本，重启即失，挪/删待定）。系统文件写入经沙箱提权一次性批准 | commit `0fa8dbd`

- **2026-09-22 | 新立 `.secrets/gitea_account`（600，git 外）＋ `docs/codemap.md`（"真账密保管"行）| 修订（凭据收编批）** | 依据：用户令"这个key你以后用"——key.txt 账密自附件档验指纹后收编 `.secrets/`，用途纪律入册（仅特权操作：铸/撤令牌、建仓、账号设置；日常仍令牌流）。`.gitignore:.secrets/` 上批已护，本批零新增跟踪面 | commit `df28d28`

- **2026-09-22 | `docs/codemap.md`（凭据行拆"推送/工单"两行）＋ `.gitignore`（增 `.secrets/`）＋ 新立 `.secrets/gitea_token`（600，git 外）| 改道（工单通道打通批）** | 依据：用户交 key.txt（账密），按最小权限路径处置——真密码仅用于一次性铸造 `xiangrugu-issues` 令牌（id=7，`write:issue` 含读），铸后真密码日常弃用；实测三件套全通：读列表 200／建验收单 #1 201／关单复确 state=closed。上条旧行"issues ❌ 须 read:issue"就活指针义已由本行前向取代（旧行不回改）| commit `f392ebe`

- **2026-09-22 | `docs/codemap.md`（凭据行扩写）| 修订（凭据权限实测落册）** | 用户裁决该凭据长期留用；端点实测：git 读写、仓信息/提交史/分支/PR/releases 读 ✅；建仓 ❌（须 `write:user`/`write:organization`，新仓走网页手建）、issues ❌（须 `read:issue`，记账以三本专职账为准）。R-06 本轮：codemap 注记与实际权限面一致，锚点无破损 | commit `250ac25`

- **2026-09-22 | `docs/codemap.md`（§2 远程仓库/推送方向/凭据三行由"待裁决"转"已定"）＋ Git remote origin 建立 | 改道（事实落定）：香如故接入远程 Gitea** | 依据：用户令"用 deepseekharness 建项目"→ 该 token 仅 `write:repository`、无建仓权，三径（user/org API、push-to-create）实测皆阻 → 用户网页端手建空私有仓 `deepseekharness/xiangrugu`（默认 `main`）→ 助手配本仓 local credential.helper 全量推送 14 commit，`git ls-remote` 与本地 HEAD 同为 `50dd95c`；远程树 12 项=10 文档＋2 目录、`.agents/skills` 经 API 核确未入库 | commit `b5dd93c`

- **2026-09-22 | 本批（`.gitignore`/`docs/codemap.md`/`docs/log.md`）＋ 跟踪清单 10 项 | 对齐核查（Skill 改道批收口）** | R-06 四项：冗余查干净（"Skill 不进 git"事实仅两处——`.gitignore` 本体＋codemap 树注，本行为动作依据记录）；矛盾查通过（B-01 闭案注"留用"指盘上保留，与本批仅撤 git 跟踪不冲突；装入批旧行"曾入库"由本批行"不抹史"注覆盖）；锚点查通过（`.gitignore` 存在、`fe18aa2`/`e978fba` 真实、树注 19–20 行与实际一致）；账目闭环（codemap✓ log✓ 三本账不涉） | commit `e978fba`

- **2026-09-22 | 新立 `.gitignore`（`.agents/`）＋ `.agents/skills/` 解除跟踪（`git rm --cached`，盘上文件保留）＋ `docs/codemap.md`（树注两行） | 改道（纪律）：Skill 不进 git** | 依据：用户口令"Skill不进git"；与 translate 先例一致（其 `.gitignore:24` 即 `.agents/`）；历史 commit `fe18aa2` 曾入库 skill，不抹史——自本批起脱离跟踪 | commit `e978fba`

- **2026-09-22 | skill 装入批（`.agents/skills/` ＋ codemap ＋ 本账） | 对齐核查（装入批收口）** | R-06 四项：冗余查干净（skill 正文零复制展开，codemap 只登目录行）；矛盾查通过并勘 1 处于入库前（本行上一条初稿误记"本会话不受影响"，热挂载实测证伪，就地勘正——历史未入库，合规）；锚点查通过（`.agents/skills/` 两目录三文件与树注一致，diff 复核零差异）；账目闭环（codemap 已登记、本账两行、三本账与 index 不涉 skill 属结构面外） | commit `fe18aa2`

- **2026-09-22 | `.agents/skills/`（新目录：`karpathy-guidelines/`、`frontend-design/`，自 translate 项目复制，diff 校验零差异）＋ `docs/codemap.md`（树登记） | 开设（skill 装入批）** | 依据：用户口令"给这个项目装些skill"；装 translate 专属两件，用户级 14 件 superpowers 全局共享不重复装（R-04 精神）；**实测：装入即热挂载本会话 skill 目录，无须开新会话**（原记"本会话不受影响"系错误，开账即勘）；两 skill 与 rules.md 无冲突（编码行为面互补：karpathy 管怎么写码，R 系列管怎么记账收口） | commit `fe18aa2`

- **2026-09-22 | 迁移批五文件（memos/rules/index/codemap/features）| 对齐核查（迁移批收口）** | 按 R-06 四项交叉核查：冗余查干净（纪要正文整档随迁，无新增复制；仅卷首性质表述按用户定性更新）；矛盾查通过（卷首"权威结论"旧措辞已降格为"备忘级实测结论"，与 index/R-04 同批对齐）；锚点查通过（全库 grep `docs/notes.md` 仅命中本账历史行 3 处＋index/memos 源注 2 处，均属合规非活指针；目录树 9 个 md 与实际一致）；账目闭环（迁移行已记、三本账与 bugs/improvements 零波及属实）。结论：干净 | commit `8010622`

- **2026-09-22 | `docs/notes.md` → `memos/memos.md`（git mv，历史随迁）＋ `rules.md`（R-04 行改道）＋ `docs/index.md`（新立"备忘"节＋阅读顺序行）＋ `docs/codemap.md`（目录树两处）＋ `docs/features.md`（指针 4 处） | 迁移（更名）·纪要账降级为专用备忘目录** | 依据：用户指令——纪要属**项目讨论与分析备忘、非项目实施指导**，移入专用目录 `memos/` 更名 `memos.md`；本账旧行及三本账旧行中出现的 `docs/notes.md` 系历史指针**不回改**（R-04），核查 grep 全库应仅命中本条所述历史行 | commit `8010622`

- **2026-09-22 | N-02 挂账批三文件（notes/features/index）＋ 提交说明笔误 amend | 对齐核查（挂账批收口）** | 按 R-06 四项交叉核查：冗余查干净（资源事实唯一展开在 notes，F-08 指针式入账；与 N-01 交叠处只做对照注、不重复展开）；矛盾查通过（F-08 内 N-01/N-02 对照注与两纪要各自在册事实一致）；锚点查通过（N-01/N-02/F-08/R-xx/文件路径目标全存在）；账目闭环（index 在册行已更新、本核查行为挂账批补上收口）。顺带：本批 commit 说明笔误"纪要么"以 amend 修正（未推送，改说明非改账本正文） | commit `20ae0f2`

- **2026-09-22 | `docs/notes.md`（N-02 照录入档）＋ `docs/features.md`（挂账 F-08）＋ `docs/index.md`（在册行更新） | 挂账批（N-02 文言语料资源池归档）** | 依据：用户交录另一 AI 会话之文言语料/工具书四档资源回答；四档资源只存档不逐条挂账（存档≠证实、全单未实测，实测前不拉数），其中"组合建议"按 R-02 指针式挂 F-08 | commit `20ae0f2`

- **2026-09-22 | 本批五文件（notes/rules/index/codemap/features）| 对齐核查（纪要归档批收口）** | 按 R-06 四项交叉核查：冗余查干净（纪要权威展开唯一在 notes N-01，F-01～F-07 均指针式；R-04 清单同步增行）；矛盾查修正 1 处（codemap §1.1"本批开设"措辞滞后→"骨架开设批"，入库前收敛）；锚点查通过（F/N/R 指针目标全存在，codemap 目录树与实际 9 文件一致）；账目闭环（bugs/improvements 本轮零新增属实、index 已登记、codemap 已刷新）。结论：除入库前 1 处外干净 | commit `5befc99`

- **2026-09-22 | `docs/notes.md`（新立）＋ `rules.md`（R-04 清单增行）＋ `docs/index.md`（新账登记＋阅读顺序延伸）＋ `docs/codemap.md`（目录树刷新）＋ `docs/features.md`（挂账 F-01～F-07） | 开设（纪要归档批）** | 依据：用户交录《会话纪要 · 历史数据资源盘点（2026-09-22）》；纪要照录入 N-01，§四方向备忘七条按 R-02 指针式挂账（权威展开唯一在 notes） | commit `5befc99`

- **2026-09-22 | 全套八件（`AGENTS.md`、`rules.md`、`docs/index.md`、`docs/log.md`、`docs/codemap.md`、`docs/bugs.md`、`docs/improvements.md`、`docs/features.md`）＋ Git 仓库初始化 | 开设（骨架建立批）** | 依据：用户指令"三个专职文档＋rules.md＋AGENTS.md＋index.md＋log.md＋codemap（含部署位置）＋每轮文档对齐核查"，设计经用户确认"同意，动手"；条款移植适配自 translate 项目同日纪律（R-01～R-09→本项目 R-01～R-06，分治编号） | commit `d44d5c4`

- **2026-09-22 | 同上 | 对齐核查（骨架开设批收口）** | 按 R-06 四项交叉核查本批文档 vs 工作区实况；结论见下一条修正注 | commit `d44d5c4`

- **2026-09-22 | `rules.md` | 修正注** | 核查中修正 1 处：R-03 两处"improvement s"笔误收敛为"improvements"（锚点查命中，写后即查，未入库）。其余：冗余查干净（纪律仅 rules.md 展开，他文均指针）；矛盾查干净（八文件互指锚点全部真实存在）；账目闭环：三本账零条有注、index 已登记八件、codemap 目录树与实际文件一致 | commit `d44d5c4`
