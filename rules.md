# rules.md —— 香如故项目纪律总纲（元纪律）

> **定位（2026-09-22 用户指令"一个纪律文档 rules.md"）**：本文件是**做事方法级纪律**（怎么干活 / 怎么记账 / 怎么写文档 / 怎么收口）的唯一权威展开处。AGENTS.md 及相关文档对应条款一律留指针不复述（R-04）。
> 条款移植适配自翻译器项目（`/root/translate`）2026-09-22 同日定型的 R-01～R-09，逐条溯源注于各条；两项目纪律分治，互不混用编号。
> 开发标尺（需求与改动的归属判定之尺）＝`AGENTS.md` §1 项目目标与核心思想，**当前待用户定义**；尺子未立前 R-03 的归属判定按各条临时规则执行。

---

## R-01 动手前须口令

发现的缺陷、验收期实报、测试暴露的问题，一律**先入账、不动手**：缺陷记录（症状复现/根因/影响面/候选方案）记入 `docs/bugs.md`；**未经用户口令"修"，不得改代码、发版、部署**。修复时机、是否随批合并、发布与回滚取舍，一律用户决定。

> 溯源：2026-09-22 用户指令（"我没让你动手前你先别动手"，translate 项目定型后移植）。

## R-02 需求挂账纪律

用户每提出新功能或改进需求，默认**只入账、不当场开发**——新功能记 `docs/features.md`、改进记 `docs/improvements.md`，写清指令日期、用户诉求原意与现状对照；等需求积累到用户认为够的程度，由用户口令进行**集中设计与开发**。仅当用户明确说"现在就做/启用"，才进入当次任务。挂账即完成本次动作：不立项、不改代码、不占版本号。

> 溯源：2026-09-22 移植（translate 项目 09-18 裁决、09-22 改道专职账）。

## R-03 三本专职账与归属判定

**bug＝`docs/bugs.md`、改进＝`docs/improvements.md`、新功能＝`docs/features.md`**——三类新记一律进这三本：

- **缺陷 → bugs 账**（配合 R-01：先记后修）；
- **不新增能力的"现状不对" → improvements账**；
- **新来源 / 新能力 → features 账**。

一句话分工：**bugs 装坏、improvements 装不对、features 装未有**。

临时规则：项目核心思想（AGENTS §1）未定义前，features/improvements 条目的"标尺归属"字段一律记 `待判`，不得自行虚构标尺；标尺由用户定型后写入 AGENTS §1，本条同步改为指针。

> 溯源：2026-09-22 用户指令"要有三个专职文档"，移植适配。

## R-04 文档单一源纪律

同一事实只允许一处权威展开，他处一律指针。当前单一源清单（新增事实入表时同批改本行）：

```text
项目目标与核心思想      AGENTS.md §1（唯一记载处）
做事方法纪律            rules.md（本文件，R-xx 唯一展开处）
文档索引与权威优先级    docs/index.md
代码地图与部署位置      docs/codemap.md
缺陷账                  docs/bugs.md
改进账                  docs/improvements.md
新功能账                docs/features.md
文档变动日志            docs/log.md
跨会话纪要备忘          memos/memos.md（N-编号，照录不改写；讨论与分析备忘，非实施指导；方向条目挂账指向此处展开）
外部数据源档案（CBDB）  docs/cbdb.md（2026-09-23 开设；项目身份、沿革、规模口径、外部评测唯一展开处；本地落点与货单指针 codemap）
已持有资料索引          docs/holdings.md（2026-09-25 用户定开设、2026-09-26 补登记于此；**"已经落到盘上的资料"的唯一索引账**——本地路径／体量／上游出处／**以本地读到的许可为准**／完整性凭据／当前状态六格；与 wiki 的分工＝**发现新源只往 wiki《中国古典语料资源索引》记一笔且不下载，真下成一件才回本账加一行**；本账开设时漏改本行，2026-09-26 按 R-06 矛盾查补齐）
外围知识层（Gitea wiki） Gitea `deepseekharness/xiangrugu` 仓的 Wiki 单元（2026-09-25 用户定性开设；**非权威层**，边界见下段）
（预留）API 契约 / 数据库 / 决策记录 / 版本记录 ＝ 项目定型后入账登记
```

**外围知识层的边界（2026-09-25 用户定性："既然是百科，当然不会是项目离开它它就不能工作……总之是项目的非必要材料。用学习与了解"）**：

- 该层承载**学习性与讨论性材料**——数据源的来龙去脉叙述、学界谁在用、名词背景、可讨论的好主意；项目**不依赖它运行**，故**不入必读链、不入权威优先级**；
- **凡本清单已登记的事实口径（数字、许可、缺项、判词、方案），wiki 侧只许摘要＋指针，不得第二处展开**；两侧表述冲突一律以本清单所指权威处为准，wiki 随后修订；
- 页面正文唯一存于 wiki 仓（`xiangrugu.wiki.git`），**工作区不另存副本**（联动落盘纪律：外部介质之物不留本地）；
- **资源索引页的条目格式**（2026-09-25 用户指令："某地址／关于这个地址的或者说项目的介绍／有什么资源／可用性分析，然后下一个，以此类推。其它的那些废话不要。没有必要一次搞完，那也办不到，发现一个以这种方式记录一次"）：wiki 侧承载"发现了哪些源"的页（当前为《**中国古典语料资源索引**》）**一律一条一源、四栏固定**，栏序不得增删调换：
  1. **地址**——可机读的真实入口 URL（多个并列；死链、仅 HTTP 通、或须走快照的，在本项内直说）；
  2. **介绍**（页面标签作`**是什么**`）——这是谁的、什么性质的一件东西，一两句；
  3. **有什么资源**（标签`**有什么**`）——**里面到底有什么**：规模给原值并带**时点与口径**，内容给类型（全文／结构化数据／影像／规范库／模型／工具）；
  4. **可用性分析**（标签`**能不能用**`）——**我们能不能用、用到哪一层**：许可证原文（**读过仓内文件才算数，GitHub 标签不算**）／能否批量下载及其大小／有无机读端点／"可阅览"与"可再分发"的分界／禁抓取禁商用的原文条款。
  - **版式（2026-09-25 二批修订，用户令"写得可读性强一点，版也排得方便阅读一些"）**：四个标签**各独占一行**（`**地址**`／`**是什么**`／`**有什么**`／`**能不能用**`，依次即规范四栏，标签用短问句以利扫读）；地址一行内多源以 `·` 分隔并置入行内代码；「有什么」「能不能用」**一条一换行成列表项**，且**分号只在括号外拆分**（防断句）；**每条末尾必给一行 `> **结论**：…`**，用四色标起头——🟢 许可干净可自由用｜🟡 能下能用但带 NC 或 SA｜🔴 只能看不能拿｜⚫ 许可或通道未证实。长句不得用逗号串到底。**改版须逐字符比对，证明未丢内容**（本轮比对结果：丢失字符 0）。
  - **禁则**：不写排序表、分级表、避雷段、观感语、待办与请示——一切限制**归进第 4 项**，不外溢成第二段文字（**每条一行的"结论"属第 4 项的收束，不算分级表**）。页首只留**格式说明＋结论色标定义＋指向细节页的指针**，不写导论。
  - **增量制**：**发现一个记一条，新条目加在最上面**；不求一次写完，未成条者只落页尾「待补条目」名单（列名不写内容），**不得当作已登记**。
  - **一条一校**：四栏内每个数字与条款须可判"本人直取"或"代理实访、我未复验"；许可取不到原文者写【未找到】，**不许以推测填栏**。
- 想法写进 wiki 讨论区即止；要落地，须由用户口令回主仓挂三本专职账（R-01／R-02）。

**历史账本不回改**（已结条目、日志旧行），后案以"补正注"前向指路。新增内容禁止复制展开既有事实。

> 溯源：2026-09-22 移植（translate 项目 09-18 裁决、ADR-049 配套）。

## R-05 文档变动日志

一切文档**结构性动作**——开设、迁移、退役、总纲搬家、成批修订、账本改道——须在 `docs/log.md` 顶部追加一行（日期｜文件｜变动性质｜依据｜commit 指针）。日常逐条记账（某账添一条、状态勾一格）**不回填** log——那是各账自身的格式。

> 溯源：2026-09-22 用户指令"加一个 log.md 用来跟踪文档变动情况"，移植适配。

## R-06 文档对齐核查（每轮必查，本轮才算收口）

每轮工作收尾（任何产生实质变更的轮次）在汇报前，对**文档 vs 当前开发**做一次交叉核查，四项全查：

1. **冗余查**：同一事实是否只在一处权威展开（对照 R-04 清单）——多处展开＝收敛为一处＋他处指针；
2. **矛盾查**：文档 vs 既有文档 vs 开发实况（代码/配置/版本/部署状态）三方互证——滞后处修订，后案以"补正注"前向指路，历史账不回改；
3. **锚点查**：所有"见 X / → X"指针的目标真实存在且语义正确（含 R-xx、B/I/F-编号、章节号、文件路径）；
4. **账目闭环查**：本轮该记的账全数落位——三本专职账、index 登记、log 动作行、codemap 刷新（涉及时）。

**修正边界**：本核查只允许修**文档**（对齐已定事实）；发现的矛盾若牵涉代码/配置/部署，**不改代码**——记 `docs/bugs.md` 待口令（R-01 联动）。核查产出：在 `docs/log.md` 追加一行"对齐核查"，注明结论（干净／修正 N 处及位置）；**有未修正矛盾的本轮不算收口**。

> 溯源：2026-09-22 用户指令"每次工作一轮后，就要对文档与当前的开发做冗余与矛盾核查。保持项目与开发文档的一致性与对齐。避免因为文档的问题导致开发歧义"，移植适配。

---

## 预留条款位（不预设空条，只占号防撞）

R-07 起为**项目专属纪律**预留（如版本与发版、人工验证闭环、一次一个任务、技术执行类规则）。待项目定型（AGENTS §1 落笔）后，由用户口令逐条开设于此，届时补写正文与本行说明。
