# log.md —— 文档变动日志（R-05）

> **定位（2026-09-22 用户指令"加一个 log.md 文档用来跟踪文档变动情况"）**：跟踪文档本身的**结构性变动**——开设、迁移、退役、成批修订、账本改道、每轮对齐核查结论。日常逐条记账（某账添一条 B/I/F）**不回填本文件**，那是各账自身的格式。
> 格式一行一事，**新条目加最上面**；旧行不回改，勘误以补正注前向指路（R-04）。
> 行格式：`日期 | 文件 | 变动性质 | 依据 | commit 指针`。

---

## 变动记录

- **2026-09-23 | `docs/features.md`（F-13 ② 目标机项补记"32 主机上已有 PostgreSQL"＋原"待用户裁决四事"措辞校正为"届时需定之事（现在不议）"） | 修订（讨论期事实补记）** | 依据：用户于讨论中告知"32主机上有pg"。**本批纯补记，零动作**：未探测 32 主机、未装库、未建库、未导入（用户明示"还没让你动手……只是讨论，我说开工的时候你再干这些活"——R-01 口径重申）。措辞校正：删去催促式"待用户裁决四事"，改为**届时需定之事**，四问不在现在提。事实线索：32 主机已有 PostgreSQL（⚠ **未实测**；与既有的"36 主机 bge-m3 已在线"同属按编号称机惯例，F-04 在册）。R-06 四项：冗余查（仅 F-13 一处记，不另开条目）、矛盾查（与 AGENTS §1"目标未定义前不做开发动作"、R-01 一致——本批只记账）、锚点查（32 主机为新增未实测线索故标 ⚠；编号未与 36 混淆）、账目闭环（features✓ 本账✓） | commit （本批，收口回填）

- **2026-09-23 | `docs/features.md`（新立 F-13"CBDB（及 CHGIS）入 PostgreSQL 数据底座"） | 开设（PG 入库准备批）** | 依据：用户令"让你查数据库的原因，就是为入PostgreSQL做准备"——**首次点明本数据线用途**。按 R-02 只入账不开发（"现在就做"须另令），故本批**零环境改动**（未装 PostgreSQL、未建库、未导入）。**入账内容**：诉求原话照录＋能力性质（新能力·数据底座）＋标尺归属 `待判`（AGENTS §1 项目核心定位未定型）＋现状对照（数据在手：0919 件 586MB／78 表／5,623,075 行；环境未备：Debian 13 trixie／WSL2，apt 可装 PostgreSQL **17**，`psql`／`pgloader`／`ogr2ogr`／`docker` 全无，`mdbtools` 已装，本地盘余 953G）＋**待裁决四事**（入库范围／目标机／保真策略／索引与外键策略）＋派生件落盘位置待指定。**关键判定**：索引缺项（370→0）在 PG 路径**不阻塞**（PG 侧另建索引，见 `docs/cbdb.md` §9.4(1) 路径 A）。R-06 四项：冗余查（F-13 为唯一展开，`cbdb.md` §九 只作数据边界指针，不重复方案）、矛盾查（与 AGENTS §1"目标未定义前不做开发动作"一致——本批只记账）、锚点查（F-13 编号接 F-12 未复用；环境与体量数字与 §四／§九一致）、账目闭环（features✓ 本账✓；bugs／improvements 零涉） | commit `a8a11ef`

- **2026-09-23 | `docs/cbdb.md`（§9.4(1) 追加"成因＋处理路径"／§9.5 索引行细化／§五 4 增后处理仓指针）＋ NAS 仓 `README_provenance.md`（④ 索引项补成因与后处理说明） | 修订（索引成因与处理管线查证批）** | 依据：用户令"HuggingFace 快车道版把索引全丢了。这个是什么情况，你到网上查查资料。这个怎么处理？"。**全程只读查证**（读本地两份 sqlite ＋ 上游 `github.com/cbdb-project/cbdb_sqlite` 仓／脚本／issue 原文；**未跑任何脚本、未建索引、未改数据文件**）。**上游查证结论**：① 索引非"被删"，系**两代管线不同**——370 条来自 Access 转换血统（索引名 `*_PrimaryKey`／`*_Belongs`／`*_ZZZ_*`、以唯一索引代主键＝转换器特征），HF 快车道件系活库直出、只带原生主键；② 上游 issue #17（2026-02-09 关闭）已取消"注入主键"流程，**二级索引本就不在新管线产出内**；③ **SQLite 不为外键列自动建索引**。**处理路径三条（未执行）**：A 入库 PG（索引改造为 PG 索引；PG 亦不自动为 FK 列建索引）／B 直接查 SQLite（**本地副本**跑官方 `add_foreign_keys.py`(36 表) ＋ `create_views.sh`(18 视图) ＋ `create_addresses_table.py`(ADDRESSES)，索引自建——本批实测官方 **370 条 DDL 中 307 条可直接套用**本件 schema，47 条引用的表本件没有、16 条列名漂移，合计 370）／C 提上游 issue（待口令）。**落点（R-04）**：明细唯一在 `docs/cbdb.md` §9.4(1)，§五 只留仓指针；NAS 出生纸同步，codemap 无需改（③ 已为状态＋指针）。**与前批关系**：前批（对质结案批）已录"370→0"事实与性能代价（相关子查询 60s 未出 vs 改写 0.07s），本批补其**成因与处理**，不冲突不重复。**三本专职账零涉**：用户未下令补索引，未立项；真要做按 R-02 挂 F/I 账待口令（且派生件落盘位置须用户指定，联动落盘纪律）。R-06 四项：冗余查（§9.4(1) 唯一展开，§五／§9.5／README 三处指针）、矛盾查（全库复验：存活正文无"0.79／3.13"表述，仅存于 §9.1 作废块与历史 log 行，符合 R-04）、锚点查（issue 编号／脚本名／307＋47＋16＝370 与实测吻合）、账目闭环（cbdb✓ 本账✓；codemap 不改） | commit `ee6dc61`

- **2026-09-23 | `docs/cbdb.md`（§九 9.1 判词作废重写＋新增 9.4"结构缺项与质量坑"／§四·§五·§八 三处前向补正）＋ `docs/codemap.md`（哈佛仓行 ③ 挂查项结案改写）＋ `docs/index.md`（cbdb.md 条目 §九 说明同步）＋ NAS 仓 `README_provenance.md`（"待查口径"与"硬缺口"两条同步结案，消矛盾查残留） | 修订（0919 件对质结案批）** | 依据：用户令"分析一下这个数据库，把情况给我汇报一下"。**只读体检＋解包对质，全程未改数据本体**。核心结论：① **§九 9.1 前批判词作废**——前批"本件每任职至多 0.79 条地点链接、官方 ≈3.13 条，故任职地点不可单用"**系跨制式算错**（拿官方 `POSTED_TO_ADDR_DATA` 行数 ÷ *本地* `POSTING_DATA` 行数）；本轮解包官方 2025-05 Access 件（5 卷 mdb，解包 5.0GB）实测其自身分母 = 1,852,415 任职、1,853,262 行，**每任职 1.000 条**，与本地件同；成因为 **`c_textid=67147`《清代量化数据库(縉紳錄)》2024-08 入档制式**——Access 存逐年名录（1,581,438 行＝其仕历表 79.9%，一行＝一人一职一年），本地 SQLite 归并为任期区间（同批 197,232 行／103,769 人）；实例陳國贊(669624) 318 行→1 行(1864–1909)，蘇軾(3767) 官方 36 条＝18 地点 vs 本地 34 条＝18 地点，**地点信息不丢**。**两制式不可按行数互校**。② 新增实测入档：真实地点覆盖率 **65.2%**（78,921 行 `c_addr_id=0` `[未詳]` 哨兵，前批 78.6% 系含哨兵毛数）／**显式索引 370→0**（2024-02 版 370 条，本地件 0 条，关联子查询实测 60s 未出 vs 改写 0.07s）／表集合 −21＋9／`0` 哨兵四字段／越界脏值／同名 77,790 组／坐标污染按 `addr_id`（徐州 11242、登州 11167 已污染宋人记录）／参考完整性全清。③ **落点判定（R-04）**：明细唯一全量展开于 `docs/cbdb.md` §九，codemap ③ 与 §八 只留状态＋指针；`ADDR_XY` 勘正（非 MDB 独有，2024-02 sqlite 亦有）同收 §九 9.2。**三本专职账零涉**：用户未提补数或建索引需求，未立项；后续若"补索引／补 ADDR_XY／取 ZZZ"按 R-02 挂 F/I 账待口令。R-06 四项：冗余查（§九 9.1 单一展开，codemap／§八／index 三处指针一致）、矛盾查（**本批即修正前批矛盾两处**：3.13 算法错、ADDR_XY 措辞不全；§四 口径纪律同步前向补正）、锚点查（index §九 说明、codemap ③、§八 指针、§九 9.1–9.6 互指一致）、账目闭环（cbdb✓ codemap✓ index✓ 本账✓）。临时件处置：用户令"两个都删"——`/tmp/cbdb_ro.db`（586MB）与 `/tmp/cbdb_mdb/`（解包 4.7GB）已清，原始件仍在 NAS 可复拉 | commit `79c9bb4`

- **2026-09-23 | `docs/cbdb.md`（新增 §九"本地 0919 件缺项与使用边界"＋§八 重复句收敛为指针）＋ `docs/codemap.md`（③ 挂查项由明细收敛为状态＋指针）＋ `docs/index.md`（cbdb.md 条目补 §九 说明）＋ NAS 仓 `README_provenance.md`（随货同步缺项实测） | 修订（缺项落档批）** | 依据：用户令"硬缺口与两个小缺项，记录到文档里"。**落点判定**：缺项属"本地件使用边界"，按 R-04 择一处全量展开——全量落 `docs/cbdb.md` §九（对比表／影响判定／补数路径／状态汇总／一句话边界），codemap ③ 与 cbdb.md §八 由明细收敛为**状态＋指针**（冗余查：同一事实不再两处展开；上批 log 行所述"补入 codemap ③"由本行前向取代，旧行不回改）。**三本专职账零涉**：用户未提"补数"需求，未立项；后续若要补官方件或查口径，按 R-02 挂 F/I 账待口令。实测依据同上批（sha256 全同、5,623,075 行、0.79 vs 3.13 条每任职、78.6% 覆盖、126,805 条零链接、无 `ADDR_XY`／ZZZ）。R-06 四项：冗余查（明细唯一在 cbdb.md §九，codemap／§八 双指针）、矛盾查（与三次修正定骨及体检测量批不冲突）、锚点查（§九、§八、index 条目、codemap ③ 四处互指一致）、账目闭环（index✓ codemap✓ 本账✓；NAS 出生纸同步） | commit `c6b2fc4`

- **2026-09-23 | `docs/codemap.md`（哈佛仓行 ③ 挂查项追加实测补证） | 修订（CBDB 入库体检测量批）** | 依据：用户问"导入 PostgreSQL 得多长时间""用家中 0919 件就可以吧"。**只读体检**（未装库、未导入、未改动数据本体）：sha256 与官方 `latest.json` 记载逐字节全同（`bde1bb8e…`，本轮复验）；全库 78 表/736 字段/76 索引/**5,623,075 行**；提取基准 368k 行/s（全库文本化 840MiB／15.3s）→ COPY 端推估 37–112 秒，端到端（含装 PG、建库、建索引）15–30 分钟；NAS 读 115MB/s（库应建本地盘，本机余 953G）；`POSTED_TO_ADDR_DATA` **结构性**差异实测（0.79 vs 3.13 条/任职、78.6% 覆盖、126,805 条零链接）与 `ADDR_XY`/ZZZ 缺项一并补入 codemap ③；家中件自带 `ADDR_CODES.CHGIS_PT_ID` 桥（与 chgis-v6 可直配）。R-06 四项：冗余查（时延与缺项事实入 codemap 挂查项，`docs/cbdb.md` 不重复展开）、矛盾查（与三次修正定骨不冲突，属细化）、锚点查（sha256／表数／行数与实测一致）、账目闭环（codemap✓ 本账✓；三本专职账零涉——**PG 入库未立项**，待用户口令再按 R-02 挂 F 账） | commit `b6caacf`

- **2026-09-23 | 新立 `docs/cbdb.md` ＋ `docs/index.md`（新开"外部数据源档案"节＋阅读顺序延伸）＋ `docs/codemap.md`（§1.2 目录树增行）＋ `rules.md`（R-04 单一源清单增行） | 开设（外部数据源档案批）** | 依据：用户令"这个介绍记录到一个文档里"。**落点判定**：`memos/memos.md` 定性为"用户交录纪要的唯一照录存档处（不改写/不评注）"，本轮 CBDB 介绍系助手调查产物，不入 memos；按 R-04 新立单一源 `docs/cbdb.md`（项目身份／沿革／规模口径／数据模型／获取通道／外部评测），同批四文件登记（index 新节＋阅读顺序、codemap 树、R-04 清单）。**内容证据均为 2026-09-23 实测**：Dataverse 社区 API 回文、PAGGQS DataCite 导出、HF API／卡片／`history/` 实测、RDH 2026-07 评审全文（Wenyi Shang，含 Peter K. Bol 撰项目综述）、cssn 2024 李斌文、Wikipedia 摘要；官方站 Akamai 403 限制与"官方人数口径 ≠ 本地表行数口径"纪律一并入档；`POSTED_TO_ADDR_DATA` 遗留疑点以指针联动 codemap。R-06 四项：冗余查（项目身份事实仅此一处展开，本地落点全指 codemap 不重复）、矛盾查（规模按时点分列无冲突）、锚点查（index／codemap 树／R-04 清单三处登记与实体文件一致）、账目闭环（index✓ codemap✓ R-04✓ 本账✓；三本专职账零涉） | commit `e9db270`

- **2026-09-23 | NAS 仓 `README_provenance.md`（新旧关系节追加复核批补记＋外部背书定骨两行）＋ 本账一行 | 修订（CBDB 复核批·直连补齐）** | 依据：用户告外网已通、令"再去看一次"。HF 直连实证全闭环：org 页题"cbdb (China Biographical Database Project (CBDB))"、官方外链＝projects.iq.harvard.edu/cbdb；卡 CC BY-NC-SA、开设 2024-04-16、lastModified 2026-09-19T19:16:54Z（构建→上传间隔一分钟）；仓根 latest.json 与家中 JSON 逐字节全同（385B）；history/ 实测 2026-02→09 逐月一档（缺 04）、此前 2019→2025 零散八档——官方"weekly"系线上库节奏、HF 月度出档，前批勘注就此落实；另见 latest_ZZZ_tables.7z（215M，ZZZ 宽表快车道亦有单独包）。第三方全文核讫：RDH 2026-07 同行评审项目评审（Shang Wenyi @ Missouri；Directors＝Peter Bol/Hongsu Wang@Harvard、官网 cbdb.hsites.harvard.edu、657,909 人@2026-05、2005 起三机构联合；判词"数字人文基础平台"＋最大挑战＝结构化与史料偏向张力）、Wikipedia 条目摘要（口径滞于 2015-04 约 36 万，仅存目）——上条"第三方仅线索未获全文"状态由本条取代（历史行不回改，前批所指 pubpub/kci/huggingface/wikipedia DNS 阻断今已解除）。codemap 零改动（结论未变、证据升级）；NAS 写经提权一次批准（上批拒批先例在册） | commit `953a0a5`

- **2026-09-23 | NAS 仓 `README_provenance.md`（新旧关系节追加"佐证补强"一行）＋ 本账一行 | 修订（CBDB 官方出身复核批）** | 依据：用户令"cbdb 到底是不是哈佛官方发布的？去官方查，也看第三方"。网络实证两重（均取 dataverse.harvard.edu 官方接口回文）：① cbdb 社区 affiliation＝IQSS、联系人 hongsuwang@fas.harvard.edu、主题站链回哈佛 CBDB 官网；② PAGGQS DataCite 导出（v8.0，creator Wang, Hongsu @ Harvard University、producer "China Biographical Database project"）摘要**原文点名 HF 仓为每周更新 SQLite 官方下载源**——与当日补课批 `dd8daf3` 的 sha256 逐字节全同合为独立证据链闭环：**家中 CBDB＝哈佛官方发布，定骨**。小勘注：官方口径 weekly、账上"月度滚动"系 history/ 节奏措辞，并存不矛盾。**自查**：前轮答用户时误引已作废的二次修正措辞（"线上库自导出/非官方打包"）作答，本轮起一律以三次修正为准；codemap 零改动（结论未变、仅证据增强）。第三方线索随报：中国社会科学网 2024-10-14 李斌文（CBDB 作结构化知识库正面典型＋概念层短板批评，全文取讫）；RDH v7n7/v2n11 评审 PDF、KCI 韩文介绍文、JHNR 引文——三条仅存链接（本会话沙箱 DNS 阻断 pubpub/kci/huggingface/wikipedia 直连，未获全文，如实入账不作证实引用）。写 NAS 仓文件经 workspace-write 一次拒批、提权一次批准 | commit `5d915df`

- **2026-09-23 | `docs/codemap.md`（哈佛仓行对账段三次修正）＋ NAS 仓 `README_provenance.md` 补新旧关系节 | 修订（官方自述补课批）** | 依据：用户痛斥"下的时候没看官方说明？去官方查！"——**认账：前几轮全部靠行数实测推断，没先读货自带的官方文档，绕了远路**。补课结果：① 家中 `cbdb_20260919.sqlite3` 的出处证物（JSON）就在它旁边——**CBDB 官方 HuggingFace 仓 `cbdb/cbdb-sqlite`（org 名"China Biographical Database Project (CBDB)"、档案链回哈佛官网、CC BY-NC-SA）的最新滚动构建，latest.json 逐字节匹配＋sha256 实算全同**；前判"线上库自导出件"作废；② CBDB 官方两速：HF 快车道（月度）vs Dataverse 档案道（bi2025 比家中旧一年）；③ MDB 说明书（HelpFiles 内中英文 User's Guide，pdftotext 读讫）证实多卷 DATA 制式与 Relink 更新机制；④ 遗留疑点：POSTED_TO_ADDR_DATA 两官方制式 46.5 万 vs 185.3 万（mdb-count 复核非假象），启用前查清口径；⑤ CHGIS 官方 README/EULA 原文家中 VERIFICATION.md 早有摘录、本次新仓同款再证。上条（`15fcf64`）内"仕宦地点分析必以官方件补"措辞随 ④ 降为待查，不再作为判词 | commit `dd8daf3`

- **2026-09-23 | `docs/codemap.md`（哈佛仓行对账段二次修正定稿）| 修订（新旧关系实测定骨批）** | 依据：用户问"旧的不是哈佛官方吗？新旧什么关系"。三实测：① 旧 CHGIS 证实官方同源——家中 V6 点表两件与 harvard-full 同款 **MD5 字节全同**（976b2a07/f77d218b），严格子集；② 开包 2025 官方 MDB（新装 mdbtools 可读，DATA1 抽验）逐表计数：家中件人/锚/亲/社/仕五线微强，**唯 POSTED_TO_ADDR_DATA 官方 185.3 万 vs 家中 46.5 万（4 倍）**——CBDB 官方近两年补完任地链接未入线上导出；③ 诗库二件系社区产物与哈佛无干、本批零增量。**判词修正：首轮"家中 sqlite 单库最强"作废，改"同源两式互补短板，仕宦地点分析必以官方件补"**。验毕临时 MDB 已清（落盘纪律）。R-06：features 扫"最强"字样无残留 | commit `15fcf64`

- **2026-09-23 | `docs/codemap.md`（挂载行转正＋哈佛仓行构成实账）＋ `docs/features.md`（路径两处替换）＋ `memos/memos.md`（N-01 §一 前向勘正注）＋ 系统层：`/mnt/wd` 软链删除、`ops/harvard/*.py` DEST 改新路径（git 外）| 改道（路径转正＋构成实账批）** | 依据：用户令"以后不用 wd，用 wd61workmetadata"+"必须搞清楚 10G 与几百兆的区别"。路径侧：唯一正路径转正、软链删、活账全换、历史账（N-01 正文/log 旧行）不回改仅前向注、复跑脚本 DEST 改齐、新路径三验（可读/可写/内容全）。体量侧：按数据集标题六分实测——CBDB 历年发布 3.51G（九版堆叠，净新增仅 2025 快照）、栅格地图 2.72G（真新增）、CHGIS 历代向量 1.93G（V2–V5 旧版）、LoGaRT 软件 1.86G、Hartwell 等 0.19G、杂集 0.43G；**10.64G 中真正的净新知识约 3.5G**。R-06 自查两处当场纠：构成第一次口算分桶错（已按脚本实测数修正）；脚本 DEST 漂移隐患（已改）。translator MCP 用户口令复开，无涉 | commit `15fcf64`

- **2026-09-23 | `docs/codemap.md`（哈佛仓行货单段改写）| 修订（出生纸批）** | 依据：用户追问"下的文件可记录否"。清点答：三层记录俱在（逐件凭据/树货单/拉取流水）；自查瑕疵一处——manifest 标题字段抓取不全显 `?`。补齐两件并**随货入 NAS 仓**：`README_provenance.md`（出生纸：来源、验证法、复拉方法、许可、瑕疵注）＋`datasets.tsv`（65 数据集总账：DOI/树/件数/字节/**标题**/目录，标题经 search API 补全 0 缺）。codemap 该行货单段同步四件实账。落盘纪律自查：数据本体仅存用户裁定仓，工作区 `ops/harvard/` 只存元数据与脚本，合规。另：translator MCP 经用户口令关→复开，无涉本批 | commit `9a529ec`

- **2026-09-23 | `docs/codemap.md`（哈佛仓行勘正重写）| 修订（哈佛货不对账勘正批）** | 依据：用户问"前后为什么差这么多"，开包实测（p7zip 新装）。三发现：① `.7z` 305M 系压缩壳，解包 5.01GB（MDB 为主）；② 官方 2024-02 SQLite 各表行数**全面低于**家中 `cbdb_20260919.sqlite3`（人物 53.5万 vs 66.2万等四项）——N-01 之"子集"判名不准，旧货实为线上库级全量、家中单库仍最强（N-01 照录原文不回改，勘正寄于本行与 codemap）；③ 官方件夹带 ACCESS 残表 21 张，但补 `ADDR_XY`（今地坐标）、`DATABASE_LINK_*`、`CBDB_NAME_LIST` 等旧货所无实表。harvard-full 内 CBDB 件定位改为：官方快照＋版本序列＋说明书。`/tmp/cbdb_ro.db` 反成最优工作副本——处置请示依旧悬着 | commit `8a68743`

- **2026-09-23 | NAS 新仓 `/mnt/wd/harvard-full/`（370 件/10.64 GiB/65 数据集，含随货 `harvard_manifest.txt`＋`harvard_dl.tsv`）＋ `docs/codemap.md`（"哈佛全量仓"行）＋ `docs/features.md`（F-09～F-12 开批＋F-08 进展注）＋ `.gitignore`（`ops/` 节）| 开设（哈佛全量采购批）** | 依据：用户令"把哈佛的货全部拿过来，不能少"。路线：官方站 Akamai 拒机器→转 Harvard Dataverse API（UA 过 contents 门，逐数据集 versions 列件）→ 4 路并发拉（约 11MB/s，断点续传＋尺寸核对＋MD5）；.tab 六件因 original/converted 双制式卡尺寸，定点换制式补拉，终 **370/370 尺寸全吻合**；.part 残片清零。过程坑三条入记忆：沙箱内外 `/tmp` 视图不通（脚本改驻 ops/）、改 NAS ACL 须重挂（前批已记）、Dataverse 清单尺寸＝converted 制式。本批未逐件解压验内容（静态入仓），启用时按 N-01 验单规矩逐件核；许可红线（CHGIS 禁商用/再分发）已注 codemap。R-06 本轮：codemap 行数实测勘正 62→65；features 活指针同步；ops/ 归节说明。另：`/tmp/cbdb_ro.db` 旧副本删除请示**仍悬** | commit `d917619`

- **2026-09-23 | `docs/codemap.md`（"数据 NAS 挂载"行状态更新）| 改道（写通道激活批）** | 依据：用户于 NAS 端给 admin 开写（B 案）后口令复测。实测坑一条：**服务端改 ACL 后旧 cifs 树连接缓存旧权限，须 umount/mount 重挂生效**；重挂后 root/zky/子目录三写探针全通、探针净清理。上条"待裁决"就此闭环（旧行不回改，本行为指）；guest 可写事实保留为历史记录，不再作为方案选项 | commit `2b69d8e`

- **2026-09-23 | `/etc/fstab`（系统文件，用户亲给定条目）＋ 挂载 `/mnt/wd61workmetadata` ＋ `/mnt/wd` 空壳目录转软链 ＋ `docs/codemap.md`（"数据 NAS 挂载"行）| 开设（NAS 常驻挂载批）** | 依据：用户令立 fstab 常驻并给定 cifs 条目（尾随粘贴引号已剔）。实测：挂载 rw/SMB3.1.1、四库读取通（含 560M CBDB 副本）；**权限反常识发现**：条目内 admin 凭据服务端**只读**（root/zky 写皆拒），guest 匿名**可读写**（探针证实，合 N-01 旧语义）——写通道走 guest 还是 NAS 端修权限，**待用户裁决**；WSL 无 systemd，`x-systemd.*` 选项休眠。另悬处置：`/tmp/cbdb_ro.db`（586M 实验副本，重启即失，挪/删待定）。系统文件写入经沙箱提权一次性批准 | commit `0fa8dbd`

- **2026-09-22 | 新立 `.secrets/gitea_account`（600，git 外）＋ `docs/codemap.md`（"真账密保管"行）| 修订（凭据收编批）** | 依据：用户令"这个key你以后用"——key.txt 账密自附件档验指纹后收编 `.secrets/`，用途纪律入册（仅特权操作：铸/撤令牌、建仓、账号设置；日常仍令牌流）。`.gitignore:.secrets/` 上批已护，本批零新增跟踪面 | commit `df28d28`

- **2026-09-22 | `docs/codemap.md`（凭据行拆"推送/工单"两行）＋ `.gitignore`（增 `.secrets/`）＋ 新立 `.secrets/gitea_token`（600，git 外）| 改道（工单通道打通批）** | 依据：用户交 key.txt（账密），按最小权限路径处置——真密码仅用于一次性铸造 `xiangrugu-issues` 令牌（id=7，`write:issue` 含读），铸后真密码日常弃用；实测三件套全通：读列表 200／建验收单 #1 201／关单复确 state=closed。上条旧行"issues ❌ 须 read:issue"就活指针义已由本行前向取代（旧行不回改）| commit `f392ebe`

- **2026-09-22 | `docs/codemap.md`（凭据行扩写）| 修订（凭据权限实测落册）** | 用户裁决该凭据长期留用；端点实测：git 读写、仓信息/提交史/分支/PR/releases 读 ✅；建仓 ❌（须 `write:user`/`write:organization`，新仓走网页手建）、issues ❌（须 `read:issue`，记账以三本专职账为准）。R-06 本轮：codemap 注记与实际权限面一致，锚点无破损 | commit `250ac25`

- **2026-09-22 | `docs/codemap.md`（§2 远程仓库/推送方向/凭据三行由"待裁决"转"已定"）＋ Git remote origin 建立 | 改道（事实落定）：香如故接入远程 Gitea** | 依据：用户令"用 deepseekharness 建项目"→ 该 token 仅 `write:repository`、无建仓权，三径（user/org API、push-to-create）实测皆阻 → 用户网页端手建空私有仓 `deepseekharness/xiangrugu`（默认 `main`）→ 助手配本仓 local credential.helper 全量推送 14 commit，`git ls-remote` 与本地 HEAD 同为 `50dd95c`；远程树 12 项=10 文档＋2 目录、`.agents/skills` 经 API 核确未入库 | commit `b5dd93c`

- **2026-09-22 | 本批（`.gitignore`/`docs/codemap.md`/`docs/log.md`）＋ 跟踪清单 10 项 | 对齐核查（Skill 改道批收口）** | R-06 四项：冗余查干净（"Skill 不进 git"事实仅两处——`.gitignore` 本体＋codemap 树注，本行为动作依据记录）；矛盾查通过（B-01 闭案注"留用"指盘上保留，与本批仅撤 git 跟踪不冲突；装入批旧行"曾入库"由本批行"不抹史"注覆盖）；锚点查通过（`.gitignore` 存在、`fe18aa2`/`e978fba` 真实、树注 19–20 行与实际一致）；账目闭环（codemap✓ log✓ 三本账不涉） | commit `e978fba`

- **2026-09-22 | 新立 `.gitignore`（`.agents/`）＋ `.agents/skills/` 解除跟踪（`git rm --cached`，盘上文件保留）＋ `docs/codemap.md`（树注两行） | 改道（纪律）：Skill 不进 git** | 依据：用户口令"Skill不进git"；与 translate 先例一致（其 `.gitignore:24` 即 `.agents/`）；历史 commit `fe18aa2` 曾入库 skill，不抹史——自本批起脱离跟踪 | commit `e978fba`

- **2026-09-22 | skill 装入批（`.agents/skills/` ＋ codemap ＋ 本账） | 对齐核查（装入批收口）** | R-06 四项：冗余查干净（skill 正文零复制展开，codemap 只登目录行）；矛盾查通过并勘 1 处于入库前（本行上一条初稿误记"本会话不受影响"，热挂载实测证伪，就地勘正——历史未入库，合规）；锚点查通过（`.agents/skills/` 两目录三文件与树注一致，diff 复核零差异）；账目闭环（codemap 已登记、本账两行、三本账与 index 不涉 skill 属结构面外） | commit `fe18aa2`

- **2026-09-22 | `.agents/skills/`（新目录：`karpathy-guidelines/`、`frontend-design/`，自 translate 项目复制，diff 校验零差异）＋ `docs/codemap.md`（树登记） | 开设（skill 装入批）** | 依据：用户口令"给这个项目装些skill"；装 translate 专属两件，用户级 14 件 superpowers 全局共享不重复装（R-04 精神）；**实测：装入即热挂载本会话 skill 目录，无须开新会话**（原记"本会话不受影响"系错误，开账即勘）；两 skill 与 rules.md 无冲突（编码行为面互补：karpathy 管怎么写码，R 系列管怎么记账收口） | commit `fe18aa2`

- **2026-09-22 | 迁移批五文件（memos/rules/index/codemap/features）| 对齐核查（迁移批收口）** | 按 R-06 四项交叉核查：冗余查干净（纪要正文整档随迁，无新增复制；仅卷首性质表述按用户定性更新）；矛盾查通过（卷首"权威结论"旧措辞已降格为"备忘级实测结论"，与 index/R-04 同批对齐）；锚点查通过（全库 grep `docs/notes.md` 仅命中本账历史行 3 处＋index/memos 源注 2 处，均属合规非活指针；目录树 9 个 md 与实际一致）；账目闭环（迁移行已记、三本账与 bugs/improvements 零波及属实）。结论：干净 | commit `8010622`

- **2026-09-22 | `docs/notes.md` → `memos/memos.md`（git mv，历史随迁）＋ `rules.md`（R-04 行改道）＋ `docs/index.md`（新立"备忘"节＋阅读顺序行）＋ `docs/codemap.md`（目录树两处）＋ `docs/features.md`（指针 4 处） | 迁移（更名）·纪要账降级为专用备忘目录** | 依据：用户指令——纪要属**项目讨论与分析备忘、非项目实施指导**，移入专用目录 `memos/` 更名 `memos.md`；本账旧行及三本账旧行中出现的 `docs/notes.md` 系历史指针**不回改**（R-04），核查 grep 全库应仅命中本条所述历史行 | commit `8010622`

- **2026-09-22 | N-02 挂账批三文件（notes/features/index）＋ 提交说明笔误 amend | 对齐核查（挂账批收口）** | 按 R-06 四项交叉核查：冗余查干净（资源事实唯一展开在 notes，F-08 指针式入账；与 N-01 交叠处只做对照注、不重复展开）；矛盾查通过（F-08 内 N-01/N-02 对照注与两纪要各自在册事实一致）；锚点查通过（N-01/N-02/F-08/R-xx/文件路径目标全存在）；账目闭环（index 在册行已更新、本核查行为挂账批补上收口）。顺带：本批 commit 说明笔误"纪要么"以 amend 修正（未推送，改说明非改账本正文） | commit `20ae0f2`

- **2026-09-22 | `docs/notes.md`（N-02 照录入档）＋ `docs/features.md`（挂账 F-08）＋ `docs/index.md`（在册行更新） | 挂账批（N-02 文言语料资源池归档）** | 依据：用户交录另一 AI 会话之文言语料/工具书四档资源回答；四档资源只存档不逐条挂账（存档≠证实、全单未实测，实测前不拉数），其中"组合建议"按 R-02 指针式挂 F-08 | commit `20ae0f2`

- **2026-09-22 | 本批五文件（notes/rules/index/codemap/features）| 对齐核查（纪要归档批收口）** | 按 R-06 四项交叉核查：冗余查干净（纪要权威展开唯一在 notes N-01，F-01～F-07 均指针式；R-04 清单同步增行）；矛盾查修正 1 处（codemap §1.1"本批开设"措辞滞后→"骨架开设批"，入库前收敛）；锚点查通过（F/N/R 指针目标全存在，codemap 目录树与实际 9 文件一致）；账目闭环（bugs/improvements 本轮零新增属实、index 已登记、codemap 已刷新）。结论：除入库前 1 处外干净 | commit `5befc99`

- **2026-09-22 | `docs/notes.md`（新立）＋ `rules.md`（R-04 清单增行）＋ `docs/index.md`（新账登记＋阅读顺序延伸）＋ `docs/codemap.md`（目录树刷新）＋ `docs/features.md`（挂账 F-01～F-07） | 开设（纪要归档批）** | 依据：用户交录《会话纪要 · 历史数据资源盘点（2026-09-22）》；纪要照录入 N-01，§四方向备忘七条按 R-02 指针式挂账（权威展开唯一在 notes） | commit `5befc99`

- **2026-09-22 | 全套八件（`AGENTS.md`、`rules.md`、`docs/index.md`、`docs/log.md`、`docs/codemap.md`、`docs/bugs.md`、`docs/improvements.md`、`docs/features.md`）＋ Git 仓库初始化 | 开设（骨架建立批）** | 依据：用户指令"三个专职文档＋rules.md＋AGENTS.md＋index.md＋log.md＋codemap（含部署位置）＋每轮文档对齐核查"，设计经用户确认"同意，动手"；条款移植适配自 translate 项目同日纪律（R-01～R-09→本项目 R-01～R-06，分治编号） | commit `d44d5c4`

- **2026-09-22 | 同上 | 对齐核查（骨架开设批收口）** | 按 R-06 四项交叉核查本批文档 vs 工作区实况；结论见下一条修正注 | commit `d44d5c4`

- **2026-09-22 | `rules.md` | 修正注** | 核查中修正 1 处：R-03 两处"improvement s"笔误收敛为"improvements"（锚点查命中，写后即查，未入库）。其余：冗余查干净（纪律仅 rules.md 展开，他文均指针）；矛盾查干净（八文件互指锚点全部真实存在）；账目闭环：三本账零条有注、index 已登记八件、codemap 目录树与实际文件一致 | commit `d44d5c4`
