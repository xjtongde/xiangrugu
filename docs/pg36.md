# pg36.md —— pg36 新建实例方案（36 主机 · 全功能 PostgreSQL）

> **状态**：**已执行（2026-09-24 用户开工口令，§8 Step 0–7 走完；装后核查全过）**。建成事实与执行偏差唯一展开于 **§13 执行记录**；§5/§6 草案保成文原样，偏差点原位前向注。
> **用户令（2026-09-23，照录）**："以后，我会有两个pg实例在跑，其它一个专门备份用。现在你不用升级32的，就在36建立我们刚才说的新pg实例。名字就叫pg36。先别动手，我说开工在干。"
> **职责划定（用户令 2026-09-23，照录）**："备份的部分不用你管。你只管安装是否符合要求，或者与我们的容器有冲突的地方，你就提出来。你只管安装与安装完成后的核查。其它的我安排别的agent去做。"——**本方案只管：安装合规比对＋冲突排查＋安装与装后核查**。备份（backups/ 体系、`backup_36_pg36.sh`）与 homelab 仓登记（`docker/by-host/36-one.md`、`docker/containers/36-pg36.md`、`docs/DATABASES.md`、`docs/CHANGELOG.md`、端口登记）＝**用户另派 agent**，本方案仅在 Step 6 备妥交接素材。
> **部署标准（外部权威，只读令牌核过）**：Gitea `zky/homelab` 仓 `docs/docker-deploy-standard.md`（《Docker 容器安装总规则（最终版）》2026-08-08 定稿／08-10 增补，**适用范围含 36-one**）。本方案已按其 §0 流程走完第 1–3 步（查官方文档→逐项比对→差异列出），现处于第 4 步"出方案→用户确认"。
> **前置账目**：F-13（features.md）；36 主机实勘＝codemap §2；32 升级案＝`docs/pg32-upgrade.md`（**已搁置**）。

## §1 官方资料核对（十项，2026-09-23 实测；自 pg32-upgrade.md 迁移，**本处为唯一权威展开**）

| # | 理解 | 官方出处（当日实 fetch） | 结果 |
|---|---|---|---|
| 1 | PGDG 是 PostgreSQL 官方推荐 apt 源，与系统补丁管理集成 | postgresql.org/download/linux/debian/：*"This repository will integrate with your normal systems and patch management"* | ✅ |
| 2 | 小版本升级不改内部存储格式，双向兼容（升/回退皆安全） | postgresql.org/docs/current/upgrading.html：*"Minor releases never change the internal storage format and are always compatible with earlier and later minor releases"* | ✅ |
| 3 | PostGIS 3.6 有配 PG18 的官方源包 | PGDG bookworm-pgdg：`postgresql-18-postgis-3`＝3.6.4+dfsg-2.pgdg12+1（另有 `-scripts`） | ✅ |
| 4 | contrib 已并入主包，无独立包 | PGDG 索引无 `postgresql-contrib-18`（计数 0）；pg32 裸镜像实测有 pg_trgm/hstore 等 | ✅ |
| 5 | 扩展按库启用：装包≠生效，`CREATE EXTENSION` 作用于当前库 | postgresql.org/docs/current/sql-createextension.html：*"loads a new extension into the current database"* | ✅ |
| 6 | PostGIS 二进制升级后须逐库 `ALTER EXTENSION postgis UPDATE;` | postgis.net/docs/postgis_installation.html | ✅ |
| 7 | `pg_stat_statements` 须入 `shared_preload_libraries`，改动须重启 | postgresql.org/docs/current/pgstatstatements.html | ✅ |
| 8 | `pg_cron` 同样需 preload | citusdata/pg_cron README | ✅ |
| 9 | 官方 PG18 镜像数据目录约定：`VOLUME /var/lib/postgresql`、`ENV PGDATA /var/lib/postgresql/18/docker` | docker-library/postgres `18/bookworm/Dockerfile` L191–192 | ✅ |
| 10 | 中文分词扩展（pg_bigm/zhparser/pgroonga）不在官方源 → 不装 | PGDG 索引实测源内无；替代＝应用层分词存 `tsvector` 走核心 FTS | ✅ |

**补正与补充**：
- 官方镜像把 PG 二进制**钉在镜像构建时点的小版本**——"拿到 18.6"靠开工当日重新 pull 基础镜像；届时若已 18.7，**照走**（核对 2 背书），以实际为准并记录。
- PG 容器运行用户 **uid/gid＝999(postgres)**——2026-09-23 于 pg32 活体实测（`id postgres`＝999:999），与部署标准 §3 所述一致；数据目录权限实测 700。

## §2 部署标准逐条合规比对（docker-deploy-standard.md §0–§13）

| 标准条款 | 要求 | 本方案对应 | 判定 |
|---|---|---|---|
| §0.1 先查官方文档 | 镜像 README／compose 示例／发布说明 | 本文件 §1 十项核对（含 PGDATA 变化——标准 §0 所举 PG18 先例正是 32 部署） | ✅ |
| §0.2 逐项比对 | 第 1–13 章 | 本表 | ✅ |
| §0.3 差异列出讨论 | 官方 vs 项目标准差异须定夺 | **§11 差异项四条已列出**，待用户确认（开工时无异议即按方案走） | ⚠ 待确认 |
| §0.4 方案→确认→执行 | 方案标注镜像/版本/数据目录/端口/网络/凭据/备份 | §4 七要素齐 | ✅（确认＝开工口令） |
| §0.5 部署后登记 homelab 仓 | by-host／by-service／CHANGELOG | **别的 agent 职责**；本方案 Step 6 备妥素材 | ➡ 交接 |
| §1.1–1.2 目录 | `/opt/mydocker/<项目>/` 独立目录＋compose＋data/ | `/opt/mydocker/postgres/`（实测该目录名空闲） | ✅ |
| §1.3 README.md 必须 | 按模板、无明文密码 | §7 草稿已备（密码只写"见 .env"） | ✅ |
| §2.1–2.2 存储 | bind mount 本地盘；**DB 严禁跑 NAS**；NAS 仅作备份目的地 | 数据在 36 本地 NVMe（余 268G）；备份归别的 agent | ✅ |
| §2.3 volume | 禁匿名 volume；PG 必须 bind mount | 显式 bind mount，无匿名卷 | ✅ |
| §3 权限 | 禁 777；PG：`chown 999:999`＋`chmod 700` | Step 2 照做（uid 999 已实测核证） | ✅ |
| §4 数据库特殊 | 独立数据目录／定期备份／禁临时目录 | 独立目录 ✅；备份＝别的 agent；无 /tmp | ✅ |
| §5 网络 | 项目独立网络（compose 默认）；禁 host 模式（Garage 唯一例外） | 默认独立网络（`postgres_default`，实测名空闲）；bridge＋端口映射 | ✅ |
| §6 端口 | 同机 DB 不暴露；**跨主机共享 DB 正常暴露 0.0.0.0** | pg36 为全网数据底座→适用 §6.2：`5432:5432`；登记＝别的 agent | ✅ |
| §7 命名 | 核心服务显式 container_name；目录小写 | `container_name: pg36`（用户赐名，合规）；目录 `postgres` | ✅ |
| §8 重启策略 | 默认 unless-stopped，禁 always | `restart: unless-stopped` | ✅ |
| §9 时区 | TZ=Asia/Shanghai＋localtime/timezone 只读挂载 | compose 已含 | ✅ |
| §10 日志 | 核心服务 json-file 50m×5 | compose 已含（PG 在标准点名的核心服务之列） | ✅ |
| §11 健康检查 | 必须配置 | `pg_isready`，interval 10s／timeout 5s／retries 5／start_period 30s | ✅ |
| §12.3 DB 镜像锁版本 | 不用 latest | 基座钉 `postgres:18-bookworm`（大版本系列）；扩展浮动→**差异项①** | ⚠ 见 §11 |
| §13 检查清单 | 逐项核对 | Step 5 装后核查含清单逐项 | ✅ |
| §14 引擎 | 官方 apt 仓六件套 | 36 已装（compose v5.3.1 实测，符合 §14.5 v5.x） | ✅ 无需动 |

## §3 与 36 现有容器冲突排查（2026-09-23 实测，只读）

| 项 | 实测 | 结论 |
|---|---|---|
| 端口 5432 | `ss -tln` 无监听（现占用：111/22/3000/50051/8000/8085/9001/11434） | **空闲 ✅** |
| 目录 `/opt/mydocker/postgres/` | 现有 10 项目目录（garage/weaviate/speaches/tei/tei-embed/perplexica/open-webui/voicebox/portainer-agent/home-cosyvoice），无 postgres | **空闲 ✅** |
| 容器名 `pg36` | `docker ps -a` 无 | **空闲 ✅** |
| 镜像名 `pg36*` | `docker images` 无 | **空闲 ✅** |
| 网络名 `postgres*` | `docker network ls` 无 | **空闲 ✅** |
| compose 插件 | v5.3.1 | **✅** |
| 磁盘 | NVMe 余 268G（镜像 ~1.2G＋数据） | **✅** |
| 内存 | 15G（可用 14G）；PG 初始参数保守（shared_buffers 1GB） | **✅** |
| GPU | PG 不用 GPU；与 RTX 4070 上 ollama/TEI 零争用 | **✅** |
| 已停容器复活情形 | garage36＝host 模式占 3900–3903；open-webui 曾用 3000——均与 5432 无涉 | **✅** |
| NAS 红线（标准 §2.2） | 数据目录在本地 NVMe，不落任何网络挂载 | **✅** |

**结论：零冲突。**

## §4 目标态（标准 §0.4 七要素）

| 要素 | 值 |
|---|---|
| 镜像 | 自建 **`pg36-full:18-pgdg`** ＝ `FROM postgres:18-bookworm`（开工当日 pull，实际小版本以届时为准）＋ PGDG 扩展集（§5） |
| 版本 | PG 18.x（预计 18.6+）；PostGIS 3.6.x；全扩展版本 build 时解析并记录（差异项①折衷） |
| 容器名 | **`pg36`**（用户命名） |
| 数据目录 | 宿主 `/opt/mydocker/postgres/data/postgres` → 容器 `/var/lib/postgresql`（PG18 约定，PGDATA=`/var/lib/postgresql/18/docker`） |
| 端口 | `5432:5432`（0.0.0.0，跨主机共享 DB，标准 §6.2） |
| 网络 | compose 默认独立网络（postgres_default） |
| 凭据 | `.env`（POSTGRES_USER／POSTGRES_PASSWORD／POSTGRES_DB），开工时生成强密码，chmod 600，**留 36 宿主机、不入任何 git、README 不写明文** |
| 备份 | **不在本方案**（别的 agent 按 homelab backups/PLAN.md ③数据库类办理；本方案只保证数据目录/compose/.env 处于可备份形态，Step 6 交接素材含路径清单） |
| 实例定位 | pg32（32，照旧生产，暂不升级）＋ pg36（36，新建）两实例并存；"另一个专门备份用"之指向**待用户明示**，本方案不预设 |
| 初始参数（草案，保守） | `shared_buffers=1GB`、`work_mem=16MB`、`maintenance_work_mem=256MB`、`effective_cache_size=4GB`、`max_connections=100`；preload 仅 `pg_stat_statements`；`shm_size: 128mb`（官方要求＋pg32 先例） |

## §5 Dockerfile（审稿草案；权威副本在此，pg32-upgrade.md 存照不回改）

```dockerfile
# pg36-full —— PG18 ＋ PGDG 官方源全功能集（口径：只取官方源；钉大版本，扩展随 PGDG 浮动）
FROM postgres:18-bookworm

RUN apt-get update && apt-get install -y --no-install-recommends \
      postgresql-18-postgis-3 postgresql-18-postgis-3-scripts \
      postgresql-18-pgrouting postgresql-18-h3 postgresql-18-address-standardizer \
      postgresql-18-pgvector postgresql-18-rum postgresql-18-pg-ivm postgresql-18-hypopg \
      postgresql-18-cron postgresql-18-repack postgresql-18-partman postgresql-18-pgaudit \
      postgresql-18-wal2json postgresql-18-pg-stat-kcache \
      postgresql-18-mysql-fdw postgresql-plpython3-18 postgresql-18-pgtap \
      postgresql-18-pglogical postgresql-18-decoderbufs \
   && rm -rf /var/lib/apt/lists/*
```

注：`pointcloud` 不装（本项目无点云用途）；`pgdg.list` 基础镜像自带（pg32 容器内实测在位）；体积预估基座 629MB→约 1.2GB（build 时实测：成品 **1.04GB**）。

**2026-09-24 执行偏差（前向注，细节 §13）**：`postgresql-18-address-standardizer` 因 PGDG 仓内 Breaks 错配按 §10 预案裁减未装；但 `address_standardizer` **扩展本体随 postgis 族包在镜像内**，功能实测通过（3.6.4）——裁减未丢能力。

## §6 docker-compose.yml（审稿草案）

```yaml
services:
  postgres:
    image: pg36-full:18-pgdg
    build: .                      # 与 image 同置：compose build 产出即该 tag
    container_name: pg36
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      - TZ=Asia/Shanghai
    env_file: .env
    volumes:
      - ./data/postgres:/var/lib/postgresql
      - /etc/localtime:/etc/localtime:ro
      - /etc/timezone:/etc/timezone:ro
    command: >-
      postgres
      -c shared_preload_libraries=pg_stat_statements
      -c shared_buffers=1GB
      -c work_mem=16MB
      -c maintenance_work_mem=256MB
      -c effective_cache_size=4GB
    shm_size: 128mb
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    logging:
      driver: "json-file"
      options:
        max-size: "50m"
        max-file: "5"
```

**2026-09-24 执行偏差（前向注）**：`/etc/timezone:/etc/timezone:ro` 一行经用户裁决 B 略去（宿主 36 该路径系空目录——既有缺陷＝bugs 账 B-02；见 §11⑤/§13）；`TZ=Asia/Shanghai` 与 `/etc/localtime` 挂载不变，容器时刻已验与宿主一致。

## §7 README.md 草稿（开工时落 `/opt/mydocker/postgres/README.md`，标准 §1.3 模板）

```markdown
# postgres（pg36）

## 基本信息
- 镜像来源：自建 pg36-full:18-pgdg ＝ docker.io/postgres:18-bookworm（Docker 官方）＋ apt.postgresql.org（PGDG 官方源）扩展集，Dockerfile 在本目录（可复建）
- 镜像版本：PG 18.x（build 时实际小版本见下"版本记录"）；PostGIS 3.6.x
- 用途：香如故项目数据底座（CBDB／CHGIS 等，装数另行）；全功能 PG（空间/向量/运维扩展备齐，按库启用）
- 部署机器：36-one（192.168.3.36）

## 配置
- 端口：5432→5432（0.0.0.0，跨主机共享 DB，标准 §6.2）
- 数据目录：/opt/mydocker/postgres/data/postgres → 容器 /var/lib/postgresql（PGDATA=/var/lib/postgresql/18/docker）
- 网络：postgres_default（compose 独立网络）
- 时区：TZ=Asia/Shanghai ＋ localtime/timezone 只读挂载

## 数据与备份
- 数据类型：数据库（PostgreSQL）
- 备份方式：由 homelab backups/ 体系负责（③数据库类，另办）；本目录 compose/.env 属④容器/⑤凭据类备份范围

## 维护
- 升级注意：小版本升级＝重 build/pull 后 up -d（数据目录不动，官方背书双向兼容）；PostGIS 二进制升级后逐库 ALTER EXTENSION postgis UPDATE；preload 仅 pg_stat_statements，增删须重启
- 依赖关系：被香如故项目（家中开发机/32）跨主机访问；不依赖 36 上其他容器
- 凭据：见本目录 .env（600；明文不写入本文档）
- 版本记录：（build 当日以 apt 解析结果回填：PG=＿＿ PostGIS=＿＿ pgvector=＿＿ …）
```

## §8 实施步骤（开工口令当日，严格按序；只动 36）

**Step 0 预检（只读，~5 min）**：`ss -tln` 复核 5432 空闲；`docker ps -a` 复核 pg36 名空闲；`ls /opt/mydocker/` 复核无 postgres；`docker pull postgres:18-bookworm` → `docker image inspect` 核 `Env.PGDATA`／`Volumes`／实际小版本（记录；若 18.7 照走）

**Step 1 落文件**：`mkdir -p /opt/mydocker/postgres/data/postgres`；落 Dockerfile（§5）、docker-compose.yml（§6）、README.md（§7，版本栏留空待回填）；生成 `.env`（强密码 `openssl rand`，`chmod 600`）

**Step 2 权限（标准 §3）**：`chown -R 999:999 /opt/mydocker/postgres/data/postgres && chmod 700 /opt/mydocker/postgres/data/postgres`

**Step 3 构建＋冒烟（不碰任何现有容器）**：`docker compose build`（产出 `pg36-full:18-pgdg`）；`docker run --rm --entrypoint postgres pg36-full:18-pgdg --version` → 记录实际小版本；`docker run --rm --entrypoint ls pg36-full:18-pgdg /usr/share/postgresql/18/extension/ | grep -c postgis` → >0；回填 README 版本记录

**Step 4 启动**：`docker compose up -d`；`docker logs -f pg36` ——**全新空目录，initdb 属预期**（与 32 升级案相反）；若报"directory not empty"或权限错 → 停，查 Step 2

**Step 5 装后核查（本方案交付物，逐项打勾汇报）**：
- [ ] `docker ps`：pg36 Up (healthy)；`docker inspect pg36` → restart=unless-stopped、挂载/端口/shm_size/logging 与 §6 一致
- [ ] `select version();` → 18.x（Step 0 记录值）；编码 UTF8、datcollate 记录在案
- [ ] 冒烟库：`create database _smoke` → `create extension postgis; select postgis_full_version();` → `create extension pg_stat_statements; select count(*) from pg_stat_statements;` → `drop database _smoke;`
- [ ] `pg_available_extensions` 覆盖 §5 全清单（postgis/pgrouting/h3/address_standardizer/pgvector/rum/pg_ivm/hypopg/pg_cron/pg_repack/partman/pgaudit/wal2json/pg_stat_kcache/mysql_fdw/plpython3u/pgtap/pglogical/decoderbufs＋contrib 族）
- [ ] 远程连通：家中开发机 `psql -h 192.168.3.36 -U <user> -c 'select 1'`（scram 密码）通过
- [ ] **36 现有服务零影响**：weaviate(:8085/:50051)、speaches(:8000)、perplexica(:3000)、portainer-agent(:9001)、ollama(:11434) 全部照常（逐一探测）
- [ ] 标准 §13 新部署清单逐项 ✅（登记项除外——归别的 agent）
- [ ] `docker stats pg36 --no-stream` 空闲占用记录；`df -h /` 余量记录
- [ ] 时区生效：容器内 `date` 与宿主一致（Asia/Shanghai）

**Step 6 交接素材（给别的 agent，本方案到此为止）**：镜像名/实际版本、容器名、端口、数据目录、compose/.env/README 路径、网络名、冒烟结果——供其登记 `docker/by-host/36-one.md`、新建 `docker/containers/36-pg36.md`、更新 `docs/DATABASES.md`、`docs/CHANGELOG.md`、`backups/README.md` 与 `backup_36_pg36.sh`（③数据库类：`docker exec pg_dump -Fc` 逐库＋`pg_dumpall` roles，保留 10 份，NAS 唯一归档；**注意 36 现未挂 61 备份共享**，通路归其定夺）

**Step 7 本项目记账**：codemap §2 部署事实行（pg36）、F-13 状态推进、log.md 一行

## §9 回滚

全新实例、无既有数据——任何一步失败：`docker compose down`；`docker rmi pg36-full:18-pgdg`；`/opt/mydocker/postgres/` 整目录（经用户确认后）删除即净。**36 现有服务全程无涉**。

## §10 风险与对策

| 风险 | 对策 |
|---|---|
| 开工日 5432/目录/名被新占 | Step 0 逐项复核（本次实测皆空闲） |
| 基础镜像已漂 18.7 | 照走并如实记录（§1 核对 2 背书） |
| 某扩展包 build 失败 | PGDG 保证配套，失败即逐包定位；核心＝postgis/pgvector，其余可裁减重试后补 |
| 内存争用（ollama 加载大模型时） | 初始参数保守（shared_buffers 1GB）；重分析错峰 |
| initdb 意外（权限/路径） | Step 2 权限先行；Step 4 盯日志，异常即停即回滚 |
| PGDATA 不符（镜像约定变化） | Step 0 inspect 硬核对（§1 核对 9） |

## §11 差异项（标准 §0.3 要求列出定夺；开工时无异议即按此执行）

1. **扩展版本不钉死**（随 PGDG 浮动）vs 标准 §12.3"锁定小版本"：标准原文对象是**镜像 tag**（禁 latest），基座已钉 `18-bookworm`；扩展若钉死，PGDG 清旧版会致日后 build 失败。**折衷**：build 时 `apt` 解析版本全量回填 README"版本记录"＋镜像 label，保证可追溯。
2. **自建镜像**：标准未禁（36 已有 voicebox 自建先例）；Dockerfile 存项目目录可复建，README 写明基座与包清单。
3. **`command:` 注入调参与 preload**（偏离镜像默认 CMD）：按 §0.4 精神在方案（§4/§6）显式标注；preload 面最小（仅 pg_stat_statements）。
4. **shm_size 128mb**：官方镜像要求＋标准 §0 已实例（32 部署）同款；将来并行大分析如需调大，另行变更并登记。
5. **（执行期新增，2026-09-24 用户裁决 B）compose 略去 `/etc/timezone` 挂载行**：标准 §9 要求 localtime＋timezone 双挂载，但宿主 36 的 `/etc/timezone` 系**空目录**（2026-07-29 既有缺陷＝bugs 账 B-02，被运行中 speaches/weaviate 挂载钉住、当日不可修）致文件类型错配、启动失败；裁决＝去该行、保 TZ＋localtime（容器时刻已验一致），宿主修复（B-02 口令后）加回一行 `up -d` 即恢复标准形态。

## §12 纪律边界

- **执行须用户开工口令**（R-01）；方案成文≠开工
- 36 上改动仅限：新建 `/opt/mydocker/postgres/`、新容器 `pg36`、新镜像 `pg36-full:18-pgdg`；**现有容器/ollama/挂载/系统配置一概不碰**
- 32 零改动（pg32 照旧生产；升级案搁置）
- 备份与 homelab 仓登记＝别的 agent（Step 6 交接）
- CBDB/CHGIS 装数不在本方案（F-13 后续步骤；**36 未挂 workmetadata**，装数前数据通路需先解决——归属待用户安排）

## §13 执行记录（2026-09-24 开工当日；建成事实唯一展开处）

> 口令与裁决链：用户"同意。开工"（2026-09-24 约 02:00 CST）＝差异项四条默认通过＋远程连通核查法裁决（①借 32 的 pg32 客户端为主／②容器自连宿主 IP 为辅；③本机装客户端不取）；执行中 timezone 挂载受阻另得裁决 B（§11⑤）。**密码纪律**：`.env` 于 36 生成、于 36 使用，全程输出仅键名与长度（PWLEN=32），明文未落任何日志/ git/README。

**Step 0 预检**：5432／pg36 名／`/opt/mydocker/postgres`／镜像名／网络名复核**全空闲**（与 §3 勘察一致）；compose v5.3.1；NVMe 余 268G。基座 `postgres:18-bookworm` 开工当日 pull：digest `sha256:3725f4e2…`、镜像构建 2026-09-19、**PG_VERSION=18.6-1.pgdg12+2（实际小版本 18.6，无漂移）**；`PGDATA=/var/lib/postgresql/18/docker`、`Volumes=/var/lib/postgresql`——§1 核对 9 吻合。

**Step 1 落文件**：`/opt/mydocker/postgres/` 新建；Dockerfile（§5＋裁减注）、docker-compose.yml（§6＋裁决 B 偏差）、README.md（§7）落位；`.env`＝`openssl rand -hex 16`（32 位 hex、chmod 600、root:root、未出 36）；`docker compose config` 解析过。**凭据名裁决点**：方案未指定 POSTGRES_USER/DB 取值，按 PG 惯例取 **postgres/postgres** 并已显著报备（装数前可 `ALTER ROLE … RENAME` 改判）。

**Step 2 权限**：数据目录 `700／999:999`、空目录验证（0 项）。注：宿主 36 上 uid 999 的用户名显示为 `ollama`（宿主侧映射巧合），实体仍是数值 999:999、容器内映射 `postgres`——权限按数值判定，无影响。

**Step 3 构建（含一次预案内失败）**：首建**失败**——`postgresql-18-address-standardizer`（仓内仅 3.7.0-1.pgdg12+1）`Breaks: postgresql-18-postgis-3-scripts (<< 3.7)`，而 `postgresql-18-postgis-3`（3.6.4）**硬 Depends** `scripts`（3.6.4）⇒ PGDG bookworm-pgdg 仓内版本错配、二者不可同装（`apt-get -s` 实测双向：剔 address-standardizer → RC=0/125 包净装；剔 scripts → 不可行）。按 §10 预案"逐包定位、裁减重试后补"**裁 `postgresql-18-address-standardizer` 重建成功（RC=0）**。**意外收获**：`address_standardizer` 扩展文件实随 postgis 族包已在镜像内——`pg_available_extensions` 有 `address_standardizer=3.6.4`，冒烟库 `CREATE EXTENSION` 功能实测通过 ⇒ **裁减未丢任何能力**；后补路径＝postgis-3 3.7 入仓后重 build 加装独立包（Dockerfile 头注释在案）。成品：`pg36-full:18-pgdg` **1.04GB**、ID `06ad5ef6a527`、postgis 扩展文件 **669** 个；**label 四枚**（差异项①兑现）：`pg36.versions`（21 包版本全表）／`pg36.base`（基座 digest）／`pg36.built=2026-09-24`／`pg36.trimmed`（裁减注）；版本全表同文回填 README"版本记录"栏。**版本纪要**：PG **18.6**、PostGIS **3.6.4**（raster/topology/scripts 齐）、pgrouting 4.0.1、h3 4.2.3、pgvector 0.8.6、rum 1.3.15、pg_ivm 1.15、hypopg 1.4.3、pg_cron 1.6.8、pg_repack 1.5.3、pg_partman 5.5.0、pgaudit 18.0、wal2json 2.6、pg_stat_kcache 2.3.2、mysql_fdw 2.9.3、plpython3u 18.6、pgtap 1.3.4、pglogical 2.4.8、decoderbufs 3.6.1。无悬空镜像遗留（36 系 containerd 存储、孤儿自动回收——label 补建时旧 ID rmi 报"No such image"即此因，无害）。

**Step 4 启动（含一次宿主缺陷致失败）**：首次 `up -d` **失败**——`/etc/timezone` 挂载报 `OCI runtime … not a directory`。只读诊断：宿主 36 `/etc/timezone` 系**空目录**、mtime **2026-07-29 02:18**（早于本次开工近两月，**非本次所致**；疑 7 月底某部署 docker 对缺失源路径自动建目录）；postgres 镜像内该路径为普通文件⇒类型错配；该目录被运行中 **speaches／weaviate** bind 钉住（另 4 停止容器同挂），原位不可替换（EBUSY）——修复须停生产容器，超 §12 边界。32 同款路径系正常文件（`Asia/Shanghai`）＝36 独有异常。**处置**：入 bugs 账 **B-02**（先记不动手）＋呈报四选项→**用户裁决 B**（compose 去 timezone 行，§11⑤）。改后 `up -d`：**t=15s `running/healthy`**；initdb 如预期于空目录执行；日志 fatal/panic/permission/not-empty **零命中**；`data/postgres/18/docker/PG_VERSION`＝`18`（600/999:999）；容器 `date` 与宿主一致（CST）。

**Step 5 装后核查（§8 清单全过，逐项证据）**：
- **容器配置**：`Up (healthy)`；restart=`unless-stopped`、ShmSize=134217728（128MB）、logging=json-file 50m×5、healthcheck=`pg_isready`（10s/5s/30s/5）、`0.0.0.0:5432->5432`（v4+v6）、网络 `postgres_default`、挂载仅 2 条 bind（数据 rw＋localtime ro）——**无匿名卷**；与 §6 一致（timezone 行经裁决略去）。
- **服务端**：`version()`＝**PostgreSQL 18.6**（==Step 0 记录值）；三库 **UTF8／en_US.utf8**（与 pg32 同口径，记录在案）；`data_directory=/var/lib/postgresql/18/docker`；§4 参数全数生效：preload=`pg_stat_statements`、shared_buffers=**1GB**、work_mem=**16MB**、maintenance_work_mem=**256MB**、effective_cache_size=**4GB**、max_connections=100；`/dev/shm`=128M；`pg_hba` 末行 `host all all all scram-sha-256`（远程强制密码）、本地 trust（同 pg32 形态）。
- **冒烟库**：`_smoke` 建→`CREATE EXTENSION postgis`＝**3.6.4**（GEOS 3.11.1／PROJ 9.8.1／raster+topology 可用）→`pg_stat_statements` 建＋count=8→删；`_smoke2` 验 `address_standardizer`（3.6.4）→删；终库列表＝`postgres template0 template1`（无残留）。
- **覆盖**：`pg_available_extensions` **34 项全命中**（postgis 族三件／pgrouting／h3／vector／rum／pg_ivm／hypopg／pg_cron／pg_repack／pg_partman／pgaudit／pg_stat_kcache／mysql_fdw／plpython3u／pgtap／pglogical／address_standardizer＋contrib 族 15 项）；`wal2json.so`／`decoderbufs.so` 在位（二者系逻辑解码插件、非扩展，本就不入 available 表——§5 清单语义"全在"成立）。
- **远程连通（裁决法）**：负向——32 上无密码连接被拒 `fe_sendauth: no password supplied`（证 scram 生效非 trust）；**正向①**——32 借 `docker exec pg32 psql -h 192.168.3.36` 带密回 `18.6 | port=5432`（pg32 自身系 18.4，**版本差异铁证应答者为 pg36**——真跨主机＋端口发布＋scram 端到端）；**正向②**——pg36 容器经宿主 IP 自连 `select 1`＝1。清单原文"家中开发机 psql"按裁决以①替代（本机无 psql、③不取），如实记录。
- **36 现有服务零影响**：运行集＝speaches／weaviate／perplexica／portainer-agent（Up 2 hours，未因本次重启）＋新 pg36；停止集六容器原样仍停；探针：ollama 回 `{"version":"0.33.3"}`、weaviate HTTP **200**＋gRPC 50051 TCP-OK、perplexica **200**、speaches 404／portainer-agent 400（agent 类端点对裸 GET 的正常应答，服务活）；`ss -tln`＝勘察基线（22/111/3000/50051/8000/8085/9001/11434）**＋新增 5432**，别无变化。
- **资源**：`docker stats` 空闲 CPU **0.02%**／MEM **84MiB**/15.5GiB；`df /` 余 **267G**（本次净耗 ~1.2G）；内存可用 13Gi。
- **标准 §13 新部署清单 13 项**：①/opt/mydocker 下✅ ②独立目录✅ ③compose✅ ④本地盘 bind 数据目录✅ ⑤README✅ ⑥DB 未放 NAS✅ ⑦无匿名 volume✅ ⑧独立网络✅ ⑨unless-stopped✅ ⑩json-file 限额✅ ⑪healthcheck✅ ⑫时区＝TZ＋localtime 生效、timezone 挂载经裁决 B 略去（§11⑤／B-02）⚠有据 ⑬端口登记➡**别的 agent**（素材见下）。

**Step 6 交接素材（给另派 agent，本方案到此为止）**：容器 `pg36`（Up/healthy、unless-stopped）｜镜像自建 `pg36-full:18-pgdg` ID `06ad5ef6a527`（label 含版本全表／基座 digest／裁减注，可复建）｜**PG 18.6＋PostGIS 3.6.4** 等 21 包（全表：36 上 `/opt/mydocker/postgres/README.md`"版本记录"＋镜像 label）｜端口 `0.0.0.0:5432`（跨主机共享 DB，标准 §6.2，**待登记**）｜网络 `postgres_default`｜数据目录宿主 `/opt/mydocker/postgres/data/postgres`→容器 `/var/lib/postgresql`（PGDATA `…/18/docker`，700/999:999，本地 NVMe）｜文件四件套 `/opt/mydocker/postgres/{Dockerfile,docker-compose.yml,README.md,.env}`（`.env`＝600、POSTGRES_USER/DB＝postgres、**密码明文只在 36**，备份 agent 需上机读取）｜健康检查 `pg_isready`｜日志 json-file 50m×5｜**时区例外**：timezone 挂载经裁决略去（B-02 修复后加回），登记时请注明｜备份归 homelab `backups/` ③数据库类（`docker exec pg36 pg_dump -Fc` 逐库＋`pg_dumpall` roles、保留 10 份、NAS 唯一归档；**36 现未挂 61 备份共享，通路归其定夺**）。

**边界自检（§12 对照）**：36 上改动仅＝新建 `/opt/mydocker/postgres/`、新容器 `pg36`、新镜像 `pg36-full:18-pgdg`——现有容器／ollama／挂载／系统配置**零触碰**（宿主 `/etc/timezone` 缺陷只记账未动）；32 全程只读（仅借 pg32 容器跑 psql 客户端，未装未改未起停）；家中开发机零改动；临时件（部署标准 /tmp 副本）用毕即删（落盘纪律）。
