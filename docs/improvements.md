# improvements.md —— 改进专职账（三本专职账之一）

> **定位（R-03）**：**不新增能力**的"现状不对"类需求记账处（"账装不对"）。默认**只挂账不开发**（R-02），集中设计开发须用户口令"启用"。
> 项目核心思想（AGENTS §1，当前待定义）定型后，"凡现状不符合核心思想的 → 入本账"一并生效；定型前凭用户明示指令入账，不得自行虚构标尺。
> 新条目追加于「在册」最上面；编号 `I-01` 起递增，永不复用。已结条目移入「已结」，正文不回改（R-04）。

**条目格式**：

```text
### I-xx 一句话标题
- 指令日期 / 状态：（挂账 | 已启用 | 已完成 | 作废）
- 诉求原意（用户话照录，不改写）：
- 现状对照（引 codemap 路径或文档锚点）：
- 待裁决面：
- 标尺归属：（核心思想定型前记 `待判`）
```

---

## 在册

无——目前零条（骨架开设于 2026-09-22）。

## 已结

无。
