# features.md —— 新增功能专职账（三本专职账之一）

> **定位（R-03）**：**新能力 / 新来源**需求记账处（"账装未有"）。默认**只挂账不开发**（R-02），"现在就做/启用"须用户明示口令。
> 新条目追加于「在册」最上面；编号 `F-01` 起递增，永不复用。已结条目移入「已结」，正文不回改（R-04）。

**条目格式**：

```text
### F-xx 一句话标题
- 指令日期 / 状态：（挂账 | 已启用 | 已完成 | 作废）
- 诉求原意（用户话照录，不改写）：
- 能力性质：（新来源 | 新能力 | 其他，注明）
- 标尺归属：（项目核心思想 AGENTS §1 未定型前一律记 `待判`，定型后补判）
- 现状对照 / 落地设想（可空，一句带过，不展开方案文档）：
```

---

## 在册

### F-15 Gitea 百科（Wiki）＝外围知识层
- 指令日期 / 状态：2026-09-25 / **已启用**（当日口令"初始化一个吧"并当日建成，无挂账期）
- 诉求原意（用户话照录，不改写）：*"既然是百科，当然不会是项目离开它它就不能工作。主要记载一些外围知识，比如哈佛这个项目什么时候成立的，有那些地方使用过这些项目等等，总之是项目的非必要材料。用学习与了解。"* ＋ *"初始化一个吧。就把哈佛关于这个项目的来龙去脉收集一下记进去。总之项目在成长，相关的知识也在成长，有了好主意也可以放进去大家讨论。"*
- 能力性质：新能力（**文档介质扩展**——在主仓 `docs/` 之外另开一层供阅读与讨论的载体）
- 标尺归属：待判（AGENTS §1 未定型；本条按用户"非必要材料／学习与了解"自述定性，不虚构标尺）
- 现状对照 / 落地设想：远程 Gitea 仓 `deepseekharness/xiangrugu` 原 `has_wiki: true` 但 **wiki 仓从未生成**（2026-09-25 实测：`/wiki` 与 `…/xiangrugu.wiki.git` 皆不可用）→ 当日以 `POST /wiki/new` 生成、**正文一律走 git 通路**（API 写正文有缺陷，另开 B-03 挂账）。**边界已立法**：`rules.md` R-04"外围知识层的边界"段（非权威、不入必读链、主仓已载事实只许摘要＋指针、正文不留本地副本、想法落地须回主仓挂账）；介质登记＝`docs/index.md`"外围知识层"节＋`docs/codemap.md` §2 对应行。**页面清单不在主仓枚举**（R-04 去重：目录唯一展开处＝wiki `Home` 词条目录＋`_Sidebar`；主仓只留指针与规模时点）。规模时点：2026-09-25 当日建成六页，同日两次加页，**末次实测 9 件在册**（含站级 `Home`／`_Sidebar`／`_Footer`）。**后续增长（新词条、想法条目）不另立项、直接在本层加页加行，主仓不随之改动。**

### F-14 35 主机新建 PostgreSQL 实例（暂名 pg35，待用户赐名/确认）
- 指令日期 / 状态：2026-09-24 / **已建成**——用户令"好，那就听你的建议在32机上装吧"→成案→"同意"口令开工→**pg32b 于 32 建成、核查九项全过（2026-09-24）**，唯一展开＝`docs/pg32b.md` §9；本账使命毕（后续备份调度/装数联动＝另派 agent／F-13 各案）。原挂账细节保留备查：（35 实勘＝codemap §2"主机 35 实勘"行；三机比较推荐 32/次选 33/末选 35＝同表"第三实例选型三机实勘"行；镜像复用核证＝本账末条）
- 诉求原意（用户话照录，不改写）："你上35主机上看看，我想pg在35上也装一个"
- 能力性质：新能力（实例格局扩展——第三实例；与 2026-09-23 口径"两个实例在跑，其中一个专门备份用"之关系**待用户明示**，本账不预设）
- 标尺归属：待判（AGENTS §1 未定型）
- 现状对照 / 落地设想：**冲突排查＝零冲突**（实勘实测：宿主 5432 空闲——机上既有 `dify-db-postgres`（postgres:15-alpine）**无宿主端口绑定**、仅内网 `dify_dify-net`，互不相扰；`pg35` 容器名／`pg35-full` 镜像名／`/opt/mydocker/postgres` 目录／`postgres` 网络名全空闲）。**资源现实（四台 docker 机中最弱）**：i3-4000M 2核4线程／7.5G RAM（可用 4.1G，Dify 全家常驻约 3.7G）／SATA SSD 余 88G（另有 19.8G docker 构建缓存可回收——标准 §15 维护域，未动）；**Gitea（:17080/:17022，本项目远程仓所在主机）＋Dify 全家为该机命脉服务**——重载实例会挤占二者（资源红线思路同 N-01 §四教训），**定位决定方案**：备份实例／轻负载副本→适配良好；重分析主力→36 之 pg36 更合适。若成案：复用 `docs/pg36.md` 骨架（官方核对／标准比对／冲突排查／七要素三草案／Steps／回滚风险差异边界全成例）；35 系 Debian 12 bookworm——基座 `postgres:18-bookworm` 与 36 同款可径用；实例名与功能规模（全功能同 pg36／精简版）待用户裁决；35 未挂 workmetadata，若涉装数需另解通路。
- **三机比较（2026-09-24 追加，用户问"你看一下32，33，35哪个更适合？"；实勘账＝codemap §2"第三实例选型三机实勘"行）**：**推荐＝32（lx03）**——若定位系备份/恢复演练实例，常态负载极轻（pg36 空闲 84MiB 为实证），而 32 恰握三机之最：可用内存 5.5G、单核最强（i7-6567U）、磁盘最大（`/` 196G＋本地大盘 `nas-mirror` 829G）、且系唯一常年跑 PG 的主机（pg32 空闲仅 72.9MiB，第二实例空间实测充裕）；**唯一前提＝5432 已被 pg32 占用，新实例须让口（如 5433）**。容灾辨析：备份实例与生产 pg32 同宿主可接受——**备份文件**落独立大盘/NAS 且归 homelab backups（机器级容灾归文件），本实例司**恢复演练/待命**之职。**次选＝33（lx01）**：真机器级隔离＋5432 可直用，但磁盘三机最小（余 71G）、CPU 与 35 同款、机上 12 个检索/翻译服务密度高——若用户要"绝不与生产同机"则取 33。**末选＝35（lx02）**：Gitea（本项目远程仓即在其上）＋Dify 全家属命脉服务、可用内存三机最少（4.1G）——最不宜新添数据库。**待用户裁**：择机＋定位（备份？）＋规模（全功能/精简）＋端口（若 32：5433）＋名。择定后照 pg36 成例出 `docs/pgXX.md` 方案。（顺报观察：nginx32 unhealthy，只报未动，超出本账边界。）
- **镜像复用核证（2026-09-24 追加，用户问"36上的那个镜像可以直接用吧？"；只读 inspect/ls 核查，零改动）**：**结论＝可直接用**——`pg36-full:18-pgdg` 实测 `arch=amd64 os=linux`（size≈251MB 压缩层集/展开 1.04GB）；32/33 均 Debian 12 bookworm＋Docker 29.7.2/29.8.0，**`/etc/timezone` 两机均为正常文件（Asia/Shanghai）——无 36 之 B-02 缺陷**，标准 §9 时区两行挂载在 32/33 可全须照挂。**分发法＝`docker save`→LAN→`docker load`**（优于异机重建：备份/演练实例与 pg36 **同镜像＝pg_dump/restore 工具版本全同**，且天然继承裁减裁决与浮动版本策略）。**复用≠照抄**：镜像名系 36 冠名，异机须 retag（随新实例名定）；compose 七要素须另立——容器名／**32 则端口 5433**（5432 系 pg32 生产口）／数据目录／新 `.env` 新密码（不复用 36 凭据）／参数按用途缩配（演练机 shared_buffers 256–512M 足矣，勿照搬 1GB）。

> **F-13** 为 2026-09-23 第二批（用户令"让你查数据库的原因，就是为入PostgreSQL做准备"）挂账；权威展开＝`docs/cbdb.md` §九（数据边界／缺项／处理路径）＋本条目。

### F-13 CBDB（及 CHGIS）入 PostgreSQL 数据底座
- 指令日期 / 状态：2026-09-23 / **首批已执行完毕（2026-09-25 00:24，验收七项全过——执行记录＝`docs/cbdb-load.md` §13；cbdb 库 84 表/5,686,842 行/索引 321/1,543MB，三方对账零差异，桥验证与 ST_Within 跨腿全过，pg32 生产零感知）**；**目标机已裁决（2026-09-23 用户令）：36 新建 `pg36`，32 暂不升级**——施工方案 → `docs/pg36.md`（**2026-09-24 已执行建成：Step 0–7 走完、装后核查全过，执行记录＝§13**；32 升级案 `docs/pg32-upgrade.md` 同日搁置；CBDB／CHGIS 装数不在方案内，届时另议——**36 未挂 workmetadata，装数前数据通路需先解决**）；**装数靶机改判（2026-09-24 用户令"36故障，你在32主机上搞"）**：36 不可达（用户确认故障，候恢复）→ 首批装数靶＝**pg32b@32**（32 本地镜像盘全货在位＋装载器已补＝通路零缺口，实测 `docs/pg32b.md` §10–11）；**装数方案＝`docs/cbdb-load.md`（同日成案→同夜用户令"同意，另外你要考虑到32主机的压力"开工：差异项九条全按＋压力防护六条入案，首批已执行）**；④通路缺口对本批自解（32 本地直读，零传输）；36 恢复后迁移/双轨另议不在首批；**第二批候另令**（Hartwell 29302／ADDR_XY/ZZZ／CHGIS 其余层——可补桥余 3,871 个 chgis_pt_id）
- 诉求原意（用户话照录，不改写）："让你查数据库的原因，就是为入PostgreSQL做准备。"
- 能力性质：新能力（数据底座／入库管线）
- 标尺归属：待判（AGENTS §1 项目核心定位未定型）
- 现状对照 / 落地设想：**数据在手**——家中 `cbdb_20260919.sqlite3`（586MB／78 表／736 字段／5,623,075 行，sha256 与官方 latest.json 全同；构建 2026-09-19）＋ `harvard-full` 官方 2025-05 Access 件与 CHGIS V2–V6 选件；**环境未备**——本机 Debian 13 trixie／WSL2（无 systemd），apt 可装 **PostgreSQL 17**（Debian main，`Candidate: 17+278`），`psql`／`pgloader`／`ogr2ogr`／`shp2pgsql`／`docker` **全无**，`mdbtools` 已装，python3 可用；本地盘余 953G、内存 15G、32 线程。**索引缺项（370→0）在本路径不阻塞**——索引与约束入 PG 后另建（见 `docs/cbdb.md` §9.4(1) 路径 A；注意 PG 亦不自动为 FK 列建索引）。**届时需定之事（现在不议，等开工口令）**：① 入库范围（0919 单件／＋官方 2025 MDB 核心表／＋CHGIS→PostGIS）；② 目标机——**已裁决（2026-09-23 用户令）：36 新建 `pg36`**（36＝AI/GPU 主力机、无 PG、5432 空闲，实勘见 codemap §2；方案 `docs/pg36.md`）；32 保持现状暂不升级（pg32 实勘见本行末"实勘 32 主机"）；③ 数据保真策略（原样保真＋另建清洗视图／入库即清洗）；④ 索引与外键策略（仅索引／补真 FK 照官方 36 表清单）。**派生件落盘位置须用户指定**（联动落盘纪律）；预估：范围①时端到端含装库 15–30 分钟、纯导入 2–5 分钟。
- **合并路线核证（用户提案，2026-09-23；讨论期只读查证）**：用户问"把 pg32 升级的同时重做镜像、把 PostGIS 放进去不就可以了？"——**成立**，且优于"在运行中的容器里 apt 装"（后者随 compose 重建即失效）。**实测依据**：① 官方一体镜像 `postgis/postgis:18-3.6` 的 Dockerfile 系 `FROM docker.io/postgres:18-trixie` ＋ `apt-get install postgresql-18-postgis-3=3.6.4+dfsg-2.pgdg13+1`（即"官方 PG 镜像＋PostGIS"，由 PostGIS 项目维护）；② 若要保持 bookworm 或另捎扩展（pgvector／pg_cron／zhparser 等），可自建 `FROM postgres:18-bookworm` ＋ PGDG 的 pgdg12 版 postgis 3.6.4。**四个必知前提**：(a) **装包≠启用**——PostGIS 按库启用（`CREATE EXTENSION postgis;`），且官方镜像的 initdb 钩子仅在**空数据目录首次初始化**时触发，已有数据不会自动启用；(b) **PGDATA／挂载路径须一致**（现挂 `/opt/mydocker/postgres/data/postgres`→容器 `/var/lib/postgresql`；官方 PG18 约定 `PGDATA=/var/lib/postgresql/18/docker`），施工前用 `docker image inspect` 核 `Env.PGDATA`／`Volumes`，不一致会**误初始化空集群**；(c) 基镜像 bookworm→trixie **不影响**数据目录（格式随 PG 大版本，与发行版无关）；(d) **先备份（数据目录 bind mount 仅 ~395MB，tar 一份即可）＋留停机窗口**（重建容器＝库停数十秒，n8n／chainlit／graphrag 会闪）。**附带收益**：18.4→18.6 小版本同步完成（二进制兼容，无需 pg_upgrade）。**取舍**：装进 pg32＝单实例 join 最顺，但空间查询与现有服务共享 4 核／7G；另起容器＝资源隔离但双实例。**未执行**（未拉镜像、未改 compose、未动 pg32）。**用户口径（2026-09-23，权威）**：用户令"postgreSQL 还应付不了这点东西吗？没必要建立这么多实例，那更费资源"——**单实例优先**：数据量（5.62M 行／~600MB；CHGIS 点十万级、面万级）对 PG 属"小库"，**不存在扛不住**；不另开实例（多实例 = 多份 shared_buffers/WAL/后台进程 + 多一份备份升级维护）。**故路线收敛：pg32 换上含 PostGIS 的镜像 → 建 `cbdb` 库 → CBDB 与 CHGIS 同库直连 join，无 fdw、无跨库拼、端口与连法不变**；"另起 PostGIS 容器"**退为备选**（仅在不愿动 pg32 时考虑）。注：**"单实例"≠"单库"**——同实例内多库（现有 nbdc／n8n／chainlit ＋新增 cbdb）是 PG 常规用法，不增加维护负担。真正的成本不在数据量，而在**空间索引（几何列须自建 GiST，PG 不自动建）与 work_mem（默认 4MB 偏小，大空间联结可会话级临时调大）**。
- **"全功能"候选清单核证（用户要求，2026-09-23；PGDG 的 PG18 实测，只读）**：用户令"现在 postgreSQL 不是说能干很多事吗？这次最好装个全功能的"。**概念先立**：PG 的"能干很多事"＝**精简核心＋扩展生态**；核心自带（无需装）JSONB／全文检索／分区表／并行查询／窗口与 CTE／UPSERT／逻辑复制／FDW 框架／BRIN-GiST-GIN／生成列——**"全功能"的一半本来就在**。**官方源现成可用（实测版本）**：`postgis` 3.6.4（含 raster／topology／sfcgal；地图必需）、`pgrouting` 4.0.1（路网寻径）、`h3` 4.2.3（六边形网格聚类）、`pointcloud` 1.2.5、`address-standardizer` 3.7.0（**地址标准化，对 CBDB 地名清洗有意**）、`pgvector` 0.8.6（配 F-04 的 bge-m3）、`rum` 1.3.15（全文检索加速）、`pg_ivm` 1.15（增量物化视图）、`hypopg` 1.4.3、`pg_cron` 1.6.8、`pg_repack` 1.5.3、`pg_partman` 5.5.0、`pgaudit` 18.0、`wal2json` 2.6、`pg_stat_kcache` 2.3.2、`mysql_fdw` 2.9.3、`plpython3u`（包名 `postgresql-plpython3-18`，18.6）、`pgtap` 1.3.4、`pglogical` 2.4.8、`decoderbufs` 3.6.1；**contrib 一族已含于主包**（实测 pg32 已有 `btree_gin`／`btree_gist`／`hstore`／`pg_trgm`／`unaccent`，另有 `pg_stat_statements`／`file_fdw`／`postgres_fdw`／`dblink`／`citext`／`ltree`／`pgcrypto`／`fuzzystrmatch`／`earthdistance`／`amcheck` 等）。**官方源内无（勿顺手装，需自编或加第三方源）**：`pg_bigm`／`zhparser`／`pgroonga`（**中文分词三项全无**）、`sqlite-fdw`、`mongo-fdw`、`plv8`、`vectorchord`、`pg_search`，及 TimescaleDB／Citus 系。**中文分词替代路线**：应用层（如 jieba）先分词、存 `tsvector` 走核心全文检索，**不需任何扩展**（文本检索另有 F-04／F-08 规划，不必硬塞 PG）。**代价三条**：装了不用**几乎不占运行资源**（扩展须按库 `CREATE EXTENSION` 才生效），代价在①镜像变大（PostGIS 系最重）②预加载类（`pg_cron`／`pg_stat_statements`／`pgaudit`）须改 `shared_preload_libraries` 并重启③PG 升级时须同步重建镜像。**落地方式（届时）**：自建 Dockerfile（`FROM postgres:18-bookworm` ＋ 上列包、钉版本）置于 `/opt/mydocker/postgres/` 进 compose——**可重建、可回滚**。**未执行**（未拉镜像、未写 Dockerfile、未改 compose）。
- **两实例格局与 pg36 裁决（2026-09-23 用户令，照录）**："以后，我会有两个pg实例在跑，其它一个专门备份用。现在你不用升级32的，就在36建立我们刚才说的新pg实例。名字就叫pg36。"＋"备份的部分不用你管。……你只管安装与安装完成后的核查。其它的我安排别的agent去做。"——**前向改判**：前批"路线收敛：pg32 换上含 PostGIS 镜像"就此**被本裁决取代**（pg32 照旧生产、暂不升级，`docs/pg32-upgrade.md` 搁置；pg36＝36 上全新全功能实例，配方不变，方案 `docs/pg36.md`）；"单实例优先"口径**在 pg36 内部仍然有效**（CBDB＋CHGIS 同库直连、无 fdw）。**"其中一个专门备份用"之指向待用户明示**（谁备谁、方式），本账不预设。**职责划定（用户令）**：本项目只管安装合规＋冲突排查＋安装与装后核查；**备份体系与 homelab 仓登记（by-host/containers/DATABASES/CHANGELOG）＝用户另派 agent**（本项目令牌对 `zky/homelab` 仅只读，实测 pull=True/push=False）。**部署标准挂钩**：`zky/homelab` 仓 `docs/docker-deploy-standard.md`（2026-08-08/08-10 定稿，适用 36）——pg36 方案已逐条合规比对，差异项四条列于方案 §11 待定夺。**冲突排查实测（2026-09-23，只读）**：5432 空闲、pg36 名/镜像名/网络名空闲、`/opt/mydocker/postgres` 目录可用、compose v5.3.1、NVMe 余 268G、GPU 零争用、PG uid=999 于 pg32 活体核证——**零冲突**（明细 `docs/pg36.md` §3）。**用户口径（2026-09-23，权威）**：用户令"肯定要官方支持的，不能搞一些第三方，不受官方推荐与支持的东西，到时候一升级全是问题"——**依赖来源只取官方源**；上列"现成可用"清单已**全数满足**（皆出自 PGDG），且官方下载页对 PGDG 的原话为"**This repository will integrate with your normal systems and patch management**"（与您"升级不出问题"之虑正对）。**据此三项改判**：① **镜像走自建 Dockerfile**（`FROM postgres:18-bookworm`＝Docker 官方 PG 镜像 ＋ 仅 PGDG 包），而非 `postgis/postgis`（后者系 **PostGIS 项目自建镜像**，成分同源但非 Docker 官方库）；② **版本策略——钉 PG 大版本、扩展随 PGDG 浮动**（官方源保证配套；若钉死扩展小版本，日后官方源清旧版会致构建失败）；③ **升级动作**——重建镜像后逐库 `ALTER EXTENSION … UPDATE`；`shared_preload_libraries` 保持最小（`pg_stat_statements`，可选 `pg_cron`——预加载库缺失会导致 PG 起不来）。**源内无者一律不装**（中文分词三项／`sqlite-fdw`／`plv8`／`vectorchord`／`pg_search`／TimescaleDB／Citus），改用替代路线（应用层分词存 `tsvector`；导 sqlite 用 COPY 不用 fdw）。**纪律位说明**：本条实具**项目级纪律**（依赖来源）性质，但 `rules.md` 末"预留条款位"载明 **R-07 起待项目定型后由用户口令开设**，故本批**只入 F-13**、未擅开 R-07（AGENTS §3：冲突不猜测、报告用户）。
- **实勘 32 主机（2026-09-23，用户令"上去看看"；全程只读，未改 32 上任何物）**：主机名 `lx03`，Debian 12 bookworm／4 核／7G（可用 5G）；**PostgreSQL＝Docker 容器 `pg32`（镜像 `postgres:18-bookworm`，即 18.4）**——容器创建 **2026-08-10T07:28Z**，即 18.4 镜像发布（Hub `18.4-bookworm` 更新于 2026-08-05）后四天所拉，**当时确为官方最新**；Hub 浮动标签 `18-bookworm` 已于 **2026-09-19** 重建指向 **18.6**，而本容器不会自行跟进，故现仍 18.4（**落后两个小版本**；PG 小版本升级二进制兼容、无需 dump/restore，是否顺手升级属届时事，勿为此单独停库）；5432 已发布至 `0.0.0.0`，数据目录实体 `/opt/mydocker/postgres/data/postgres`（现占 395M，宿主机 `/dev/sdb1` 余 196G）；默认参数 `shared_buffers=128MB`／`work_mem=4MB`／`maintenance_work_mem=64MB`／`max_connections=100`；库编码 **UTF8**、`datcollate/datctype=en_US.utf8`；**已装扩展仅 `plpgsql`，可用扩展中无 PostGIS**（有 `pg_trgm`／`unaccent`／`btree_gin`／`btree_gist`／`hstore`）；`pg_hba` 本地 trust、末行 `host all all all scram-sha-256`（允许远程密码连接）；现有库 `nbdc`／`nbdc_test`／`n8n`／`chainlit`／`home_graphrag_chainlit`，**无 CBDB／CHGIS 数据**；**32 自身挂着 NAS**（`//192.168.3.61/workmetadata`→`/mnt/wd61workmetadata`，cifs rw、rsize 4MB）→ 源件可**就地读**；同机另有 graphrag／chainlit／calibre／portainer／nginx／n8n 等 live 容器（共用该实例，未见内存/CPU 限额）。**PostGIS 可得性（2026-09-23 只读查证）**：容器内 `/usr/share/postgresql/18/extension/` **无 postgis 文件**、32 上**亦无 postgis 镜像**（镜像表仅 postgres:18-bookworm 等）——故现实例装不了几何类型；**但可装**：PGDG 源内有 **`postgresql-18-postgis-3` 3.6.4**（另有配 PG17 的同款），且 pg32 容器内 `/etc/apt/sources.list.d/pgdg.list` **在位**，容器内 apt 补装之路通；宿主 apt 里那个 `postgis 3.3.2` 系 Debian bookworm 配 **PG15** 的包，与 PG18 无关，勿混。三途：① 给 pg32 补装（动的是 live 共用实例，且容器内安装随 compose 重建而失，须落成自定义镜像才持久）；② 另起 PostGIS 专用容器（如 `postgis/postgis`，另占端口）与 pg32 并存，跨库用 `postgres_fdw` 或应用层拼——对现有服务零风险；③ 不用 PostGIS：坐标/面抽成普通表（经纬度、WKT），点可 SQL 筛，**面包含运算（点落在哪个朝代政区）须外部引擎**（GeoPandas／QGIS／DuckDB spatial）或地图在外部出图。许可红线在册：CHGIS **学术用、禁商用禁再分发**（EULA 见 `/mnt/wd61workmetadata/chgis-v6/CHGIS_V6_EULA.txt`）。**镜像实测**：Docker Hub `postgis/postgis` 有 **`18-3.6`** 标签（PG18＋PostGIS 3.6，与现 pg32 同版本线；另有 `18-3.6-alpine`），故"另起 PostGIS 容器"一路无需版本折衷。
- **装数前置实测（2026-09-24，讨论期事实补记；只读——`docker run --rm` 探查＋NAS 货单核目，未装未改未导入，36/32/本机零改动）**：① **pg36 镜像缺 shapefile 装载器**——`shp2pgsql`／`raster2pgsql` 全路径 `find /` 零命中；`postgresql-18-postgis-3-scripts`（946 文件）实查只含 SQL 脚本（legacy/升级脚本族），**PGDG 现时 PG18 打包本就不含装载器二进制**，`apt-cache search shp2pgsql` 亦空——CHGIS→PostGIS 腿在装载工具上差一步；② **补法三候选（装数时定夺）**：(a) 镜像加装 Debian/PGDG `postgis` 装载器包（shp2pgsql/pgsql2shp 系 libpq 通讯之客户端工具，无版本错配之虞）及/或 `gdal-bin`（ogr2ogr，PGDG 候选 **3.13.2+dfsg-1.pgdg12+1**；libgdal32 已随 postgis 在镜像、增量小）后重 build——仍全官方源、数据目录不动，**倾向案**；(b) 36 宿主层装 gdal-bin（系统改动，超 pg36 边界须另令）；(c) 装数机转换后灌入（本机需装工具）；③ **CBDB sqlite 腿零新工具**：本机 sqlite3/python 导出 CSV → `ssh → docker exec -i pg36 psql \copy` stdin 管道灌入（纸面通，装数时实测）；④ **数据通路缺口仍在**（36 未挂 workmetadata，pg36.md §12 在册）：候选＝36 挂共享／一次性中转搬运／32（已挂 NAS＋有 psql 客户端）就地转换管道灌入——装数成案第一问。入库边界与质量规则（`0` 哨兵／坐标污染按 addr_id／索引 DDL 307 条可套用等）一律 `docs/cbdb.md` §九。

> F-09～F-12 为 2026-09-23 批（用户令"全部拿过来"哈佛 Dataverse 落仓后所见**新货**挂账；权威展开＝`/mnt/wd61workmetadata/harvard-full/harvard_manifest.txt` 货单＋codemap"哈佛全量仓"行，R-04）。

### F-12 全元文索引＋2957 书院＋传教士著作（三小件顺带入账）
- 指令日期 / 状态：2026-09-23（随 F-09 批）/ 挂账
- 诉求原意：harvard-full 内 `DVN/HTGBQ3`（全元文 1–60 册篇目索引）、`DVN/J6XRIV`（书院 2957 条）、`DVN/CE4ZNG`（19c 来华传教士著作清单）——体量小的现成结构化索引，喂"文章总集/教育机构/西学东渐"三条支线
- 能力性质：数据资源
- 现状对照 / 落地设想：随主管线顺带入库；书院表可与地名主表 join（F-01 下游）

### F-11 LoGaRT-BERT 古籍自动标点工具（本地推理，先量占用）
- 指令日期 / 状态：2026-09-23 / 挂账
- 诉求原意：哈佛 CBDB 树内 LoGaRT（BERT 古籍标点/实体定位，模型件 1.86GiB 已入仓）；**硬件红线：4070 12G 串行**，评估显存占用后才定启用与否（N-01 掉卡教训在前）
- 能力性质：分析能力
- 现状对照 / 落地设想：未起；若可用，服务 F-05 草稿区提议层与史部切分预处理
- 前置事实（2026-09-25 本轮实测，不改变挂账状态）：本地已持有的两件即 `DVN/BWIBNL`（官方标题 `LoGaRT Tagging Tool - Based on BERT`；`LGTaggingApp.7z` 1,604,972,078B／md5 `d38e4c0d89ee…` ＋ `models.zip` 391,888,041B／md5 `f869c4dff1dc…`，合计 1.86GiB 与上行相符），**其 Dataverse 条款原值＝CC BY-NC-SA 4.0（NC＝不可商用）**；而**上游代码仓 `zhouliupku/LGTaggingApp` 的 LICENSE 全文＝MIT（可商用）**——**代码与模型·数据两层许可不同，任何"跑通后再决定怎么用"的讨论必须先分清是在说哪一层**。另：马普所 LoGaRT 本体在 GitHub 检索不到（`q=LoGaRT` 八命中无一为该项目），不可据 GitHub 判其在否。详见 wiki《古籍开源项目抄》§七③

### F-10 Hartwell 宋辽金官员 GIS（宋代线第二支柱）
- 指令日期 / 状态：2026-09-23 / 挂账
- 诉求原意：宋辽金宰相与官员任职地理数据集（0.19GiB，`DVN/29302`）——**正补 CBDB 宋代偏薄之缺**（宋 8.3 万 vs 明清 23 万级），与 F-07 钱币经济史、宋诗库同域协同
- 能力性质：数据资源
- 现状对照 / 落地设想：未起；入库时与 CHGIS 点表对齐坐标（复用 ADDR_CODES 桥路径）

### F-09 CHGIS 历代版全量启用（V2–V6 政区面/DEM/GNS/铁路＋日本 GIS＋俄藏地图）
- 指令日期 / 状态：2026-09-23 / 挂账
- 诉求原意：原 N-01 实测只有 V6 点表；今 V2–V6 **历代政区多边形（pgn）、路府时序面、DEM 高程、GNS 地名点、1932 铁路、图幅索引**全套到仓——"政区变迁动画/行迹叠加版图/地形约束分析"从此有面数据；日本历史 GIS 与俄藏中国地图为旁支储备
- 能力性质：数据资源
- 现状对照 / 落地设想：仓件静态已在；启用＝按用例选件灌 PostGIS（**不整仓灌库**），许可逐件核（CHGIS 学术用禁商用红线在册）

### F-08 文言语料与工具书底座选型接入
- 指令日期 / 状态：2026-09-22（N-02 组合建议，用户交引）/ 挂账
- 诉求原意："底座语料＝殆知阁（下载入库，Elasticsearch/向量库）；精确检索与对齐翻译＝ctext API；注释/译文训练数据＝古文岛 API 批量拉『原文↔译文』对；人名/地名实体链＝CBDB dump"
- 能力性质：新来源＋新能力（语料底座）
- 标尺归属：待判
- 现状对照 / 落地设想：四档资源池权威展开见 `memos/memos.md` N-02；**全单未实测**，拉数前按落盘纪律逐条实测（与 N-01 交叠处：ctext 已 ✅、CBDB 官方馆为候选渠道、殆知阁/古文岛/gushi.wiki/Unihan/kanripo 系新增线索）；与 F-01/F-04 底座选型互相衔接。**2026-09-23 进展注**：CBDB 一腿已到位——官方 SQLite 历年全量入仓 `/mnt/wd61workmetadata/harvard-full`（见 F-09 批），余下未实测＝殆知阁/古文岛/gushi.wiki 等文与词条腿。

> F-01～F-07 为同一批挂账（2026-09-22 依 N-01 纪要 §四「方向备忘（本次讨论共识）」入账），批内按转写顺序编号，今后新批仍加最上面。各条**指针式记账**，权威展开唯一处＝`memos/memos.md` N-01 §四（R-04）。

### F-07 钱币/经济史方向（宋代货币空白）
- 指令日期 / 状态：2026-09-22（N-01 §四）/ 挂账
- 诉求原意："钱币方向（本轮新增）：无现成宋代货币库＝空白机会；路径＝经济词表＋物价/铸钱事件表→join 地名主表/人物表/诗库；第一步试验＝从《宋史·食货志》提 200 条样表（ctext 全文）"
- 能力性质：新来源＋新能力
- 标尺归属：待判
- 现状对照 / 落地设想：依赖 F-01 地名主表与人物/诗库 join 键；CBDB 子集无经济/货币表（memos N-01 §一），ctext 全文实测可用（§二）。

### F-06 生成内容面（行迹短片/心声翻译机/故土诗/擦肩/诗星图）
- 指令日期 / 状态：2026-09-22（N-01 §四）/ 挂账
- 诉求原意："生成内容（云上图像/视频/TTS：行迹短片、心声翻译机、故土诗、擦肩、诗星图）"
- 能力性质：新能力（多面混合，启用时逐面拆单独立项）
- 标尺归属：待判
- 现状对照 / 落地设想：纪要次序明确此为模型接入第三层（F-04/F-05 之后）；云上生成，题材面纪要注"诗词题材云可用"。

### F-05 LLM 疑难裁决层（消歧/系年/实体链接）
- 指令日期 / 状态：2026-09-22（N-01 §四）/ 挂账
- 诉求原意："LLM 疑难裁决（地名消歧/系年证据/实体链接，产出只进草稿区）"
- 能力性质：新能力
- 标尺归属：待判
- 现状对照 / 落地设想：红线随条目生效——**产出只进草稿区**；依赖 F-01 的 provenance/草稿区字段设计。

### F-04 向量先行检索层（bge-m3 @36）
- 指令日期 / 状态：2026-09-22（N-01 §四）/ 挂账
- 诉求原意："模型接入次序：向量先行（bge-m3 在 36 已在线：混合检索/双源互校/意象聚类）"
- 能力性质：新能力（检索底座，模型次序第一层）
- 标尺归属：待判
- 现状对照 / 落地设想：纪要称 36 主机 bge-m3 已在线——**✅ 已实测（2026-09-23，用户令勘 36）**：ollama 中 `bge-m3:latest` 在列（8 周前入库，ollama 原生服务 :11434）；同机 **weaviate 1.38.8 向量库在线**（:8085/:50051，数据卷 `/opt/mydocker/weaviate/data`，备份 `/myback/weaviate`），另有已停的 dify-tei-embedding／reranker（TEI 1.9）可复活——**向量层的基础设施已半在位**；主机全貌见 codemap §2"主机 36 实勘"行。硬件红线见 N-01 §四（4070 12G 并发掉卡教训；本次勘时 GPU 闲置）。

### F-03 溯源规范
- 指令日期 / 状态：2026-09-22（N-01 §四）/ 挂账
- 诉求原意："基础工作（不急但先留口子）：……→ 溯源规范"
- 能力性质：新能力（数据治理规范）
- 标尺归属：待判
- 现状对照 / 落地设想：与 F-01 provenance 字段一体设计；纪要"数据落盘纪律"为现成底稿。

### F-02 简繁归一管线（OpenCC s2t）
- 指令日期 / 状态：2026-09-22（N-01 §四）/ 挂账
- 诉求原意："基础工作（不急但先留口子）：地名主表 schema → 简繁归一管线 → 溯源规范"
- 能力性质：新能力（数据处理底座）
- 标尺归属：待判
- 现状对照 / 落地设想：刚性前提——poetry-source 简体库与 chinese-poetry 繁体库互校须先归一（N-01 §一）。

### F-01 地名主表 schema（provenance＋草稿区字段）
- 指令日期 / 状态：2026-09-22（N-01 §四）/ 挂账
- 诉求原意："基础工作（不急但先留口子）：地名主表 schema（含 provenance ＋ 草稿区字段）"
- 能力性质：新能力（核心数据底座）
- 标尺归属：待判
- 现状对照 / 落地设想：上游数据在手（CHGIS V6 点表＋CBDB ADDR_CODES 桥，N-01 §一）；为 F-05/F-07 join 前提。

## 已结

无。
